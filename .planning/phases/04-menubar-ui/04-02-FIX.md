---
phase: 04-menubar-ui
plan: 04-02-FIX
type: fix
---

<objective>
Fix 5 UI issues from issue triage (ISS-004, ISS-006, ISS-007, ISS-008, ISS-009).

These are all UI polish items that improve the menu bar experience:
- Integrate existing ComparisonPicker component
- Improve DriveBar visual design
- Move Quit to Settings (cleaner UI)
- Add right-click menu to menu bar icon
- Redesign buttons as native macOS list style

Source: Issue triage 2026-01-11
Priority: 0 critical, 5 medium, 0 minor
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md
@.planning/ISSUES.md

**Files being modified:**
@Prunr/Views/MenuBarView.swift
@Prunr/Views/ComparisonPicker.swift
@Prunr/Views/DriveBarView.swift
@Prunr/ViewModels/MenuBarViewModel.swift
@Prunr/Services/MenuBarManager.swift
@Prunr/PrunrMenuBar.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Integrate ComparisonPicker into MenuBarView</name>
  <files>Prunr/Views/MenuBarView.swift, Prunr/ViewModels/MenuBarViewModel.swift</files>
  <action>
Add ComparisonPicker above the "What Grew" header in MenuBarView:

1. Add @State var selectedInterval: TimeInterval to MenuBarView (read from UserDefaults)
2. Add ComparisonPicker component in the "What Grew" section header (between HStack and Divider)
3. In MenuBarViewModel, add selectedInterval property and pass to BaselineService.getGrowthList
4. Update BaselineService.getGrowthList to accept optional TimeInterval parameter for time-based comparison

DO NOT modify ComparisonPicker.swift - it already works correctly.
DO create a clean integration in the header row.
  </action>
  <verify>Build succeeds; picker appears above growth list; changing selection persists to UserDefaults</verify>
  <done>ComparisonPicker visible and functional in MenuBarView</done>
</task>

<task type="auto">
  <name>Task 2: Redesign DriveBarView for better clarity</name>
  <files>Prunr/Views/DriveBarView.swift</files>
  <action>
Improve DriveBarView visual design:

1. Simplify gradient - use single blue color instead of multi-stage gradient
2. Add percentage indicator (e.g., "75% used") alongside the free space label
3. Improve label layout - show "X GB free of Y TB (Z% used)" format
4. Add subtle background track for the full bar width
5. Keep corner radius and height the same

Changes to make:
- Replace usedSpaceGradient with solid Color.blue
- Add percentage calculation: (usedBytes / totalBytes) * 100
- Update freeSpaceLabel to include percentage
- Add background RoundedRectangle with gray fill behind the colored bar
  </action>
  <verify>DriveBar shows single blue color with percentage text; visual matches new design</verify>
  <done>DriveBarView redesigned with cleaner visual and percentage indicator</done>
</task>

<task type="auto">
  <name>Task 3: Move Quit button to Settings window</name>
  <files>Prunr/Views/MenuBarView.swift, Prunr/PrunrMenuBar.swift</files>
  <action>
Remove Quit button from MenuBarView footer, add to Settings window:

1. In MenuBarView: Remove the "Quit" button from footer HStack (lines 79-83)
2. In PrunrMenuBar.swift: Update Settings scene to include Quit button:
   - Replace placeholder "Settings coming in Phase 5" text with actual SettingsView
   - Create a basic SettingsView struct with a Quit button
3. Create Prunr/Views/SettingsView.swift with:
   - Title "Prunr Settings"
   - Section with Quit button using .borderedProminent style
   - Placeholder sections for future settings

DO keep Settings button in MenuBarView footer (opens Settings window)
  </action>
  <verify>MenuBarView no longer has Quit button; Settings window opens with Quit button; Quit works correctly</verify>
  <done>Quit moved from MenuBarView to Settings window</done>
</task>

<task type="auto">
  <name>Task 4: Add right-click menu to menu bar icon</name>
  <files>Prunr/Services/MenuBarManager.swift, Prunr/Views/MenuBarView.swift</files>
  <action>
Replace Reset button with reload icon, add right-click menu:

1. In MenuBarView: Replace "Reset Baseline" Button with:
   - Image(systemName: "arrow.clockwise") icon button
   - Same functionality (calls viewModel.resetBaseline)
   - Keep .bordered style and .controlSize(.small)

2. In MenuBarManager: Add right-click menu to statusItem:
   - Implement NSStatusItem.menu property
   - Create NSMenu with items: "Settings...", "Reset Baseline", "-", "Quit Prunr"
   - Wire up each menu item to appropriate actions
   - Left-click still shows the popover (existing behavior)

Use NSMenu delegation pattern - MenuBarManager should create and manage the menu.
  </action>
  <verify>Right-clicking menu bar icon shows menu; menu items work; left-click still shows popover</verify>
  <done>Right-click menu implemented on menu bar icon with Settings, Reset, Quit options</done>
</task>

<task type="auto">
  <name>Task 5: Redesign footer as native macOS list style</name>
  <files>Prunr/Views/MenuBarView.swift</files>
  <action>
Replace button-style footer with native macOS list row style:

1. Change footer HStack to single full-width Button:
   - Remove individual button styling (.bordered, .controlSize)
   - Create a single row that spans full width (minus padding)
   - Use Button with label containing HStack of icons/text
   - Add hover effect using .buttonStyle(.borderless) with custom background

2. Structure:
   ```
   Button(action: { openSettings() }) {
       HStack {
           HStack(spacing: 8) {
               Image(systemName: "gear")
               Text("Settings")
           }
           Spacer()
           Image(systemName: "chevron.right")
               .font(.caption)
               .foregroundStyle(.secondary)
       }
   }
   .buttonStyle(.borderless)
   ```

3. Apply this style to make it look like a native macOS settings list row:
   - Full width
   - Subtle hover background
   - Chevron indicator on right
  </action>
  <verify>Footer looks like native macOS list row; clicking opens Settings; hover effect works</verify>
  <done>Footer redesigned as native macOS-style list row</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ComparisonPicker integrated and functional
- [ ] DriveBarView redesigned with cleaner visuals
- [ ] Quit button moved to Settings window
- [ ] Right-click menu works on menu bar icon
- [ ] Footer redesigned as native list style
- [ ] Build succeeds without errors
- [ ] All UI changes follow macOS design patterns
</verification>

<success_criteria>
- All 5 UI issues (ISS-004, ISS-006, ISS-007, ISS-008, ISS-009) resolved
- Menu bar UI follows native macOS patterns
- Settings window functional with Quit button
- Right-click menu provides standard actions
</success_criteria>

<output>
After completion, create `.planning/phases/04-menubar-ui/04-02-FIX-SUMMARY.md`

Summary should document:
- All 5 UI improvements completed
- Files modified for each task
- Any deviations from plan
- Screenshot-worthy changes (describe visual improvements)
