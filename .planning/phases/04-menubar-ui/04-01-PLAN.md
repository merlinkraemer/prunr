---
phase: 04-menubar-ui
plan: 01
type: execute
domain: swiftui
---

<objective>
Build the menu bar popover UI with drive bar, growth list, and footer buttons.

Purpose: Provide the primary user interface for the menu bar app, showing disk space usage and what grew since baseline.
Output: Working menu bar popover with DriveBarView, GrowthListView, and functional footer buttons.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Auto-selected based on dependency graph (from frontmatter):
@.planning/phases/03-baseline-growth-tracking/03-01-SUMMARY.md
@.planning/phases/05-frontend-redesign/05-05-SUMMARY.md

# Key files from frontmatter (relevant to this phase):
@Prunr/Services/MenuBarManager.swift
@Prunr/Services/BaselineService.swift
@Prunr/ViewModels/MainViewModel.swift
@Prunr/Views/MenuBarView.swift
@Prunr/Views/GrowthBarView.swift
@Prunr/Views/SizeBarView.swift

**Tech stack available:**
- SwiftUI for views
- NSStatusItem + NSPopover for menu bar integration
- @Observable for state management
- BaselineService.getGrowthList() for growth data

**Established patterns:**
- GrowthBarView: Visual bar component with width proportions
- SizeBarView: Relative size bars with min/max width
- MenuBarView: Placeholder in MenuBarManager (needs replacement)
- Actor-isolated services with @MainActor UI state properties
- UserDefaults for persistence

**Constraining decisions:**
- Phase 3: Single baseline design with GrowthItem struct (path, growthBytes, currentSizeBytes, percentOfParent)
- Phase 1: LSUIElement = YES (no Dock icon), .transient popover behavior
- Phase 2: FSEvents integration ready for future auto-rescan triggers
- MVP: View-only (no cleanup) - "Reveal in Finder" only
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create MenuBarViewModel for state management</name>
  <files>Prunr/ViewModels/MenuBarViewModel.swift</files>
  <action>Create @Observable @MainActor MenuBarViewModel with:
- growthItems: [BaselineService.GrowthItem] state property
- isLoading: Bool for scan progress
- errorMessage: String? for error display
- totalBytes: Int64 for total disk size
- usedBytes: Int64 for used disk space
- freeBytes: Int64 for free disk space
- loadGrowthList() method that calls BaselineService.getGrowthList(defaultTrackedPath)
- refreshDiskSpace() method using DiskSpaceService.shared
- resetBaseline() method calling BaselineService.resetBaseline()
- revealInFinder(path:) method using NSWorkspace.shared.activateFileViewerSelecting([])
Use TrackedPath.defaultPaths.first for the tracked path (Home directory is default).</action>
  <verify>Build succeeds, Xcode shows no errors for MenuBarViewModel</verify>
  <done>MenuBarViewModel compiles with all properties and methods defined</done>
</task>

<task type="auto">
  <name>Task 2: Create DriveBarView with visual used/free bar</name>
  <files>Prunr/Views/DriveBarView.swift</files>
  <action>Create DriveBarView struct with:
- totalBytes, usedBytes, freeBytes properties
- HStack with colored bar segments: used (blue/orange gradient), free (gray)
- Labels below showing "XX GB free of YY GB" format
- Use SizeBarView pattern for proportional width calculation
- Include formatter helper (bytesToGBString) for display
- Add Preview showing different states (low space, normal, mostly free)</action>
  <verify>Preview renders with proper proportional bars and labels</verify>
  <done>DriveBarView displays used/free space with colored bar and text labels</done>
</task>

<task type="auto">
  <name>Task 3: Create GrowthListView with clickable items</name>
  <files>Prunr/Views/GrowthListView.swift</files>
  <action>Create GrowthListView struct with:
- growthItems: [BaselineService.GrowthItem] binding
- empty state view when no growth (icon + text "No changes detected")
- For each item: VStack with path name (truncated to ~40 chars), GrowthBarView showing growth proportion, size change text (+1.2 GB), and current size
- Use .buttonStyle(.plain) with .onTapGesture on each row for "Reveal in Finder" functionality
- Include folder icon (SF Symbol "folder.fill") for each item
- Scrollable list with frame constraints (max height ~300)
- Sort by growthBytes descending (already sorted from BaselineService)</action>
  <verify>Preview renders with sample growth items, tapping reveals in Finder</verify>
  <done>GrowthListView displays list with tapping behavior that opens Finder to selected path</done>
</task>

<task type="auto">
  <name>Task 4: Update MenuBarView with full UI layout</name>
  <files>Prunr/Views/MenuBarView.swift</files>
  <action>Replace placeholder MenuBarView content with:
- Header: "Prunr" title with harddrive icon
- DriveBarView section (use @State or ObservedObject for view model)
- Divider
- GrowthListView section (pass growth items from view model)
- Divider
- Footer HStack with "Reset Baseline" button and "Quit" button
- Reset button: calls viewModel.resetBaseline(), disabled during loading
- Quit button: NSApplication.shared.terminate(nil)
- Set popover frame to 320x400 (increase from 320x240)
- Use @StateObject for MenuBarViewModel instance
- Add .task modifier to load growth list on appear
Add loading indicator overlay when isLoading is true.</action>
  <verify>Build succeeds, clicking menu bar icon shows full popover with all sections</verify>
  <done>MenuBarView displays drive bar, growth list, and footer buttons with loading state</done>
</task>

<task type="auto">
  <name>Task 5: Update MenuBarManager to use new MenuBarView</name>
  <files>Prunr/Services/MenuBarManager.swift</files>
  <action>Update MenuBarManager to:
- Pass MenuBarViewModel to MenuBarView (create as @StateObject in MenuBarView itself)
- Update popover contentSize to NSSize(width: 320, height: 420)
- Update free space display to use simplified format (drop decimals as already implemented)
- No other changes needed (NSHostingController already wraps MenuBarView)
- Consider adding updateFreeSpace() call when popover opens for fresh data</action>
  <verify>Build succeeds, menu bar click shows popover with updated dimensions</verify>
  <done>Popover displays at 320x420 with fresh free space data on open</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] `xcodebuild -project Prunr.xcodeproj -scheme Prunr build` succeeds with no errors
- [ ] Clicking menu bar icon shows popover with drive bar, growth list, and buttons
- [ ] Growth items display with proper formatting (sizes in GB/MB, colored bars)
- [ ] Tapping growth items reveals in Finder
- [ ] Reset button clears baseline and reloads
- [ ] Quit button terminates app
- [ ] Empty state displays when no growth detected
</verification>

<success_criteria>

- MenuBarViewModel manages growth list, disk space, and baseline operations
- DriveBarView shows visual used/free space with labels
- GrowthListView displays clickable growth items with reveal-in-Finder
- MenuBarView integrates all components in proper layout
- Popover displays correctly at 320x420 with .transient behavior
- All buttons functional (reset, quit, reveal)
  </success_criteria>

<output>
After completion, create `.planning/phases/04-menubar-ui/04-01-SUMMARY.md`:

# Phase 4 Plan 01: Menu Bar UI Summary

**Implemented complete menu bar popover with drive bar visualization and growth list display.**

## Accomplishments

- Created MenuBarViewModel with growth list, disk space, and baseline management
- Created DriveBarView with visual used/free bar and space labels
- Created GrowthListView with clickable items that reveal in Finder
- Updated MenuBarView with full UI layout including header, sections, and footer
- Updated MenuBarManager with proper popover sizing

## Files Created/Modified

- `Prunr/ViewModels/MenuBarViewModel.swift` - State management for menu bar UI
- `Prunr/Views/DriveBarView.swift` - Visual disk space bar component
- `Prunr/Views/GrowthListView.swift` - Clickable growth list with reveal-in-Finder
- `Prunr/Views/MenuBarView.swift` - Updated with full UI layout
- `Prunr/Services/MenuBarManager.swift` - Updated popover sizing

## Decisions Made

[Document any decisions made during implementation]

## Issues Encountered

[Document problems and resolutions, or "None"]

## Next Step

Ready for Phase 05: Settings & Polish - configurable paths, threshold, boundaries, permissions prompt
</output>
