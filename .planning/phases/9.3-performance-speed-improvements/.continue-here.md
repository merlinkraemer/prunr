---
phase: 9.3-performance-speed-improvements
task: 1
total_tasks: 4
status: in_progress
last_updated: 2026-03-09T07:16:15.149Z
---

<current_state>
Phase 9.3 had previously been marked complete, but it should be treated as reopened for a reliability/stability pass before any more UI polish.

Current repo state is clean on branch `main`. No code changes were made in this handoff session.

Product direction from discussion: stop prioritizing more UI tweaks for now. The critical next step is making growth tracking trustworthy and scan behavior stable on very large paths, including paths with millions of files.

Linear state at handoff:
- No `Prunr` issue is currently in `In Progress`
- Suggested execution order is `MER-145` -> `MER-94` -> `MER-95` -> `MER-147`
- Latest team-wide status update for Prunr in the `Updates` project was March 6, 2026 and still called out scan validation + test action setup as the next work
</current_state>

<completed_work>

- Re-triaged the Prunr queue and project status in Linear
- Confirmed the latest active project context is reliability-sensitive, not feature-blocked by UI
- Decided to anchor the handoff under `.planning/phases/9.3-performance-speed-improvements/` instead of the newer `9.4-settings-redesign` phase, because settings/UI is not the right next focus
- Identified the main correctness and stability hotspots:
  - `Prunr/Services/BaselineService.swift`
  - `Prunr/Services/DatabaseCleanupService.swift`
  - `Prunr/Services/MenuBarManager.swift`
  - `Prunr/Services/FSEventsWatcher.swift`
  - `Makefile`
- Confirmed `Makefile` already expects `xcodebuild test`, but terminal verification is not reliable yet because the scheme test action is not configured
- Confirmed there are no visible shared `.xcscheme` files in `Prunr.xcodeproj`, which is consistent with `MER-145`
</completed_work>

<remaining_work>

- Task 1: Verification foundation and stress harness - In progress conceptually, not started in code
  - Make terminal verification reliable
  - Fix or formalize the scheme/test-action path behind `make test`
  - Add a repeatable large-tree stress harness for scans and repeat scans
- Task 2: Correctness fixes in growth aggregation
  - `MER-94`: category snapshot aggregation / raw-value mismatch causing missing growth indicators
  - `MER-95`: subcategory totals/counts built from partial slices instead of full category totals
  - Re-check `MER-96` while in the same area because pagination logic is tightly related
- Task 3: Autoscan reliability and large-path stability
  - `MER-147`: validate and fix autoscan behavior on real filesystem changes
  - Measure scan duration, memory pressure, DB growth, and false-growth behavior on repeated scans
- Task 4: Resume UI changes only after the above is proven
  - Do not spend agent cycles on more drilldown polish until the scan/delta pipeline is stable on huge trees
</remaining_work>

<decisions_made>

- Prioritize trust over polish: reliable growth tracking matters more than additional UI changes right now
- Use `MER-145` as the first execution gate because all later claims need terminal-verifiable evidence
- Keep the work inside the existing performance/reliability phase (`9.3`) instead of starting from the settings redesign phase
- Use a synthetic stress dataset and repeatable command flow rather than ad hoc manual testing
- Do not introduce new dependencies for the harness unless there is a strong reason; prefer shell/Swift/project-owned tooling
- Keep baseline-vs-current semantics for now; the conceptual consecutive-scan redesign is out of scope for this pass
</decisions_made>

<blockers>
- `make test` is not a trustworthy proof path yet because the `Prunr` scheme test action is not configured (`MER-145`)
- There is no existing project-owned stress harness for “millions of files” validation
- Some current behavior may look “stable enough” on small trees while still failing on huge trees, so local spot checks are not enough
- The planning tree is partly archival, so agents should use this handoff plus `docs/STATE.md` and `docs/OVERVIEW.md` for current intent rather than blindly following older UI plans
</blockers>

<context>
Mental model for the next agents:

The immediate problem is not visual quality. The immediate problem is whether Prunr can be trusted on large, noisy, real developer paths. A wrong growth result, missing indicator, duplicate/partial subcategory totals, or autoscan that quietly fails undermines the whole product more than any remaining UI rough edge.

Code hotspots to inspect first:

- `Prunr/Services/BaselineService.swift`
  - `getSubcategoryBreakdown(for:snapshotId:)`
  - `loadMoreSubcategoryFiles(for:subcategory:snapshotId:totalBytes:offset:limit:)`
  - `detectGrowthTrends(trackedPath:)`
  - Current behavior shows full-table pagination/filtering in Swift, which is a likely correctness and scale problem

- `Prunr/Services/DatabaseCleanupService.swift`
  - `aggregateCategoriesInSQL(for:db:)`
  - This is the most suspicious spot for `MER-94`, because category snapshots written here must align exactly with `GrowthCategory.rawValue`

- `Prunr/Services/MenuBarManager.swift`
  - `configureFileWatcherIfNeeded()`
  - `configureFileWatcher(with:)`
  - recent-change refresh / automatic-scan scheduling around the watcher
  - This is the main place to inspect for `MER-147`

- `Prunr/Services/FSEventsWatcher.swift`
  - Mostly for understanding debounce and event delivery boundaries, not necessarily the first edit target

- `Makefile`
  - `make build`
  - `make test`
  - Current `test` target tries to treat “No test bundles” as acceptable, but that only helps if the scheme can actually run test action cleanly

Known reliability concerns to prove explicitly:

- Large-path scan completes without crash or pathological slowdown
- Repeat scan of unchanged tree produces zero false growth
- Autoscan triggers when real file changes happen under the watched path
- Autoscan does not spam or wedge the UI
- DB size remains bounded across repeated scans and cleanup cycles
- Drilldown totals/counts and growth indicators are consistent with underlying data

Suggested agent split:

- Agent 1: verification + scheme/test-action + harness ownership
  - files/areas: `Makefile`, `Prunr.xcodeproj`, any scripts added for stress data generation or scan verification

- Agent 2: aggregation correctness ownership
  - files/areas: `Prunr/Services/DatabaseCleanupService.swift`, `Prunr/Services/BaselineService.swift`

- Agent 3: autoscan reliability ownership
  - files/areas: `Prunr/Services/MenuBarManager.swift`, `Prunr/Services/FSEventsWatcher.swift`

Verification expectations:

- Show actual command output for build/test/harness runs
- If UI/manual verification is needed, capture concrete evidence and note exact path + dataset used
- Do not claim stability from reasoning alone
</context>

<next_action>
Start with Task 1.

First actions:
1. Inspect `MER-145` and make `make test` produce a project-owned result instead of failing on scheme configuration.
2. Add or document a repeatable large-tree stress workflow that can simulate repeated scans on a very large path.
3. Run baseline verification:
   - `make build`
   - `make test`
   - large-tree scan once
   - unchanged-tree scan again
4. Only after the verification path exists, move to `MER-94` and `MER-95`.

If an agent needs a single concrete file to open first, start with `/Users/merlinkramer/dev/projects/prunr/Makefile`, then `/Users/merlinkramer/dev/projects/prunr/Prunr/Services/BaselineService.swift`.
</next_action>
