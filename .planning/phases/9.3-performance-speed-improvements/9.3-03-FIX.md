---
phase: 9.3-performance-speed-improvements
plan: 03-FIX
type: fix
---

<objective>
Fix 2 UAT issues from plan 9.3-03.

Source: 9.3-03-ISSUES.md
Priority: 1 critical (Blocker), 1 major

**Issues:**
- UAT-001 (Blocker): No progress bar during scans - only spinner shown
- UAT-002 (Major): Documents folder not categorized - shows under "Other"
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/ROADMAP.md

**Issues being fixed:**
@.planning/phases/9.3-performance-speed-improvements/9.3-03-ISSUES.md

**Original plan for reference:**
@.planning/phases/9.3-performance-speed-improvements/9.3-03-PLAN.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Fix UAT-001 - Add visible progress bar during scans</name>
  <files>Prunr/Views/MenuBarView.swift, Prunr/Views/ScanProgressView.swift (or similar)</files>
  <action>
Investigate why the progress bar is not visible during scans. The progress percentage is being calculated in ScanService.swift (lines 193-207) and passed via the progress callback, but the UI only shows a spinner.

Steps:
1. Find the scan modal/progress UI component (likely in MenuBarView.swift or a dedicated view)
2. Check if a ProgressView exists and is bound to scanProgressPercentage
3. Verify the @State variable scanProgressPercentage is properly connected to the UI
4. Add or fix the ProgressView component to show percentage visually

Expected UI: A progress bar with percentage (e.g., "45%") should appear for scans longer than 2 seconds.

Current issue: Only a loading spinner is visible - the ProgressView may be missing, hidden, or not bound to the state.

Fix: Add/fix ProgressView binding to scanProgressPercentage from MenuBarManager.
  </action>
  <verify>
1. Build succeeds
2. Trigger a scan (create baseline or load category growth)
3. Progress bar with percentage appears after 2 seconds
4. Progress updates smoothly during scan
5. Scan completes normally
  </verify>
  <done>
Progress bar with percentage is visible and updates during scans longer than 2 seconds.
</done>
</task>

<task type="auto">
  <name>Task 2: Fix UAT-002 - Add new categories (Documents, Photos, Videos, Apps, System Data)</name>
  <files>Prunr/Models/GrowthCategory.swift</files>
  <action>
The user wants to add 5 new categories to the categorization system:

Current categories (10):
1. Homebrew - Package manager files (Keep)
2. node_modules - JS dependencies (Keep)
3. Library/Caches - App caches (Keep)
4. Downloads - Old installers/archives (Keep)
5. Docker - Images/containers (Keep)
6. Spotify - Local cache (Keep)
7. Browser Cache - Web data (Keep)
8. Mail Attachments - Email files (Keep)
9. Trash - Deleted items (Keep)
10. Other - Catch-all (Keep)

**New categories to add:**
1. **documents** - PDFs/Docs - Pattern: ["Documents", "/Documents"]
2. **photos** - Pictures/media - Pattern: ["Pictures", "Photos", "/Pictures/", "/Photos/"]
3. **videos** - Movies/clips - Pattern: ["Movies", "Videos", "/Movies/", "/Videos/"]
4. **apps** - Unused programs - Pattern: ["/Applications"]
5. **systemData** - Logs/backups - Pattern: ["Library/Logs", "Library/Application Support", "/private/var/log"]

Steps:
1. Add 5 new cases to GrowthCategory enum (before "other")
2. Add displayName for each new category
3. Add icon for each new category (use appropriate SF Symbols)
4. Add color for each new category
5. Add patterns array for each new category
6. Update categorize() function to check for new patterns (before falling back to "other")

Maintain priority order: more specific patterns first (e.g., node_modules before general paths).
  </action>
  <verify>
1. Build succeeds
2. Scan test_data directory with Documents folder
3. Documents folder appears as "Documents" category, not "Other"
4. Test other new categories if applicable
5. All existing categories still work correctly
  </verify>
  <done>
All 5 new categories added and working. Documents folder now shows as "Documents" category.
</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] UAT-001 fixed: Progress bar visible during scans
- [ ] UAT-002 fixed: Documents folder categorized correctly
- [ ] All 5 new categories added (Documents, Photos, Videos, Apps, System Data)
- [ ] Build succeeds with no errors
- [ ] Original functionality still works
</verification>

<success_criteria>

- UAT-001 (Blocker) resolved - progress bar shows during scans
- UAT-002 (Major) resolved - Documents and other user folders categorized
- All tests pass
- Ready for re-verification
</success_criteria>

<output>
After completion, create `.planning/phases/9.3-performance-speed-improvements/9.3-03-FIX-SUMMARY.md`:

# Phase 9.3 Plan 3: FSEvents & Progress Optimization Fix Summary

**Fixed 2 UAT issues: progress bar visibility and new category additions**

## Accomplishments

- Added visible progress bar with percentage during scans
- Added 5 new categories: Documents, Photos, Videos, Apps, System Data

## Files Created/Modified

- `Prunr/Views/[ScanProgressView].swift` - Added/connected progress bar UI
- `Prunr/Models/GrowthCategory.swift` - Added 5 new categories

## Decisions Made

- SF Symbols for new categories: doc.fill for Documents, photo.fill for Photos, film.fill for Videos, app.fill for Apps, gearshape.fill for System Data
- Category check order: specific paths before general to avoid misclassification

## Issues Encountered

None

## Next Step

Re-run UAT to verify fixes, then proceed to Phase 9.4 (Settings Redesign).
</output>
