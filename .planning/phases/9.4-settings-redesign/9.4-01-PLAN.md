---
phase: 9.4-settings-redesign
plan: 01
type: execute
domain: swift-ui
---

<objective>
Simplify Settings to 3 tabs: General, Folders, About. Remove dead code and improve clarity.

Purpose: Current settings has 6 tabs with confusing options (drill-down slider is dead code, boundaries need better UX). Consolidate to essential settings only.
Output: Clean 3-tab settings window with intuitive controls.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@Prunr/Views/SettingsView.swift
@Prunr/ViewModels/SettingsStore.swift
@Prunr/Models/BoundaryConfig.swift

**Current tabs (6):** General, Paths, Boundaries, Data, Debug, About
**New tabs (3):** General, Folder Limits, About

**Key findings:**
- drillDownThreshold setting is DEAD CODE (BaselineService hardcodes 0.70, never reads setting)
- Boundaries concept is useful but needs better naming ("Folder Limits") and clearer explanation
- Data/Debug tabs not needed for production

**Design patterns from recent phases:**
- .switch toggles for modern appearance
- 44pt row height for list items
- 16pt horizontal padding, 12pt between rows
- .help() modifiers for tooltips
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure SettingsView to 3 tabs</name>
  <files>Prunr/Views/SettingsView.swift</files>
  <action>Update the main SettingsView TabView structure:

1. Remove tabs: Data (.tag(3)), Debug (.tag(4))
2. Rename "Boundaries" tab to "Folder Limits" with systemImage "stop.circle"
3. Remove separate Paths tab (will merge into General)
4. Final structure:
   - General (.tag(0)) - systemImage "gear" - Login + Paths
   - Folder Limits (.tag(1)) - systemImage "stop.circle"
   - About (.tag(2)) - systemImage "info.circle"
5. Update window frame to 480x420 (slightly smaller with fewer tabs)
6. Remove DataSettingsTab and DebugSettingsTab structs entirely (delete the code)</action>
  <verify>Build succeeds, only 3 tabs visible: General, Folder Limits, About</verify>
  <done>SettingsView has 3 tabs, Data/Debug tabs removed</done>
</task>

<task type="auto">
  <name>Task 2: Rebuild General tab with Login + Paths</name>
  <files>Prunr/Views/SettingsView.swift</files>
  <action>Rebuild GeneralSettingsTab to include Login and Scan Paths:

1. Remove the "Scanning" GroupBox section containing drill-down slider (dead code)

2. Keep "Startup" section with Login toggle

3. Add "Scan Paths" section (move from PathsSettingsTab):
   - List of tracked paths (default + custom) with .switch toggles
   - Blue folder icon for defaults, orange for custom
   - Path display: name on top, path below in secondary color
   - Add (+) button to add custom path
   - Remove button (red minus) for custom paths only
   - Reset Baseline button

4. Layout:
   - Use Form or VStack with GroupBox sections
   - "Startup" section at top
   - "Scan Paths" section below
   - Action buttons at bottom (Add path, Reset Baseline)

5. Delete old PathsSettingsTab struct after merging</action>
  <verify>Build succeeds, General tab shows Login toggle and Scan Paths list</verify>
  <done>General tab has Login + Scan Paths, drill-down slider removed</done>
</task>

<task type="auto">
  <name>Task 3: Redesign Folder Limits tab (rename from Boundaries)</name>
  <files>Prunr/Views/SettingsView.swift</files>
  <action>Rename and redesign BoundariesSettingsTab to FolderLimitsSettingsTab:

1. Rename struct from BoundariesSettingsTab to FolderLimitsSettingsTab

2. Add clear explanation at top of tab:
   "When drilling into folders, scanning stops at these names. This keeps node_modules, .git, and build folders as single items instead of showing thousands of files inside."

3. List of folder limits with .switch toggles:
   - Gray stop.circle icon for standard limits
   - Orange stop.circle.fill icon for custom limits
   - Monospace font for folder names
   - Toggle ON = stop at this folder (default)
   - Toggle OFF = drill through this folder

4. Group by category (optional, or alphabetical):
   - Package Managers: node_modules, Pods, vendor, .cargo
   - Version Control: .git
   - Build Output: build, DerivedData, target, .build
   - Virtual Environments: .venv, venv, .conda

5. Add custom folder limit section at bottom:
   - Text field for folder name
   - Add (+) button
   - Custom limits show with remove button

6. Delete old BoundariesSettingsTab struct after renaming</action>
  <verify>Build succeeds, Folder Limits tab has clear explanation and intuitive toggles</verify>
  <done>Folder Limits tab redesigned with clear explanation</done>
</task>

<task type="auto">
  <name>Task 4: Polish About tab with app icon</name>
  <files>Prunr/Views/SettingsView.swift</files>
  <action>Update AboutSettingsTab:

1. Replace placeholder icon (systemImage "internaldrive") with actual app icon:
   - Use Image(nsImage: NSApp.applicationIconImage) for the app icon
   - Size: 64x64 or 80x80

2. Update layout:
   - App icon at top, centered
   - "Prunr" title below icon
   - Version text (keep "Version 1.0")
   - Spacer
   - Quit button at bottom, centered

3. Clean typography:
   - Title: .title, .bold
   - Version: .caption, .secondary

4. Remove unnecessary divider</action>
  <verify>Build succeeds, About tab shows app icon from asset catalog</verify>
  <done>About tab shows app icon, clean layout with Quit button</done>
</task>

<task type="auto">
  <name>Task 5: Clean up SettingsStore - remove drillDownThreshold</name>
  <files>Prunr/ViewModels/SettingsStore.swift</files>
  <action>Remove dead drillDownThreshold code from SettingsStore:

1. Remove from Keys enum:
   - static let drillDownThreshold = "drillDownThreshold"

2. Remove property:
   - var drillDownThreshold: Double { didSet... }

3. Remove from init():
   - let threshold = UserDefaults.standard.double(forKey: Keys.drillDownThreshold)
   - self.drillDownThreshold = threshold > 0 ? threshold : 0.7

This is safe cleanup - the value was never read by BaselineService.</action>
  <verify>Build succeeds, no references to drillDownThreshold remain in Settings</verify>
  <done>drillDownThreshold removed from SettingsStore</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Build succeeds: `xcodebuild -project Prunr.xcodeproj -scheme Prunr build`
- [ ] Only 3 tabs: General, Folder Limits, About
- [ ] General tab: Login toggle + Scan Paths list (no drill-down slider)
- [ ] Folder Limits tab: Clear explanation + toggles for each folder limit
- [ ] About tab: App icon displayed, version, Quit button
- [ ] All functionality preserved (paths, folder limits still work)
- [ ] No compiler warnings about unused drillDownThreshold
</verification>

<success_criteria>
- 3 tabs: General, Folder Limits, About
- General tab has Login + Scan Paths
- Dead code removed (drillDownThreshold, Data tab, Debug tab)
- Folder Limits has clear explanation of what it does
- App icon visible in About tab
- All path and boundary functionality preserved
- Clean, intuitive settings experience
</success_criteria>

<output>
After completion, create `.planning/phases/9.4-settings-redesign/9.4-01-SUMMARY.md`
</output>
