---
phase: 9.4-settings-redesign
plan: 01
type: execute
domain: swift-ui
---

<objective>
Redesign the Settings window with modern macOS design patterns to match the visual quality of the main popup.

Purpose: The current Settings window uses dated SwiftUI patterns (GroupBox, checkbox toggles) that don't match the polished visual design of the main popup. This phase brings the Settings up to the same visual standard.
Output: A modernized Settings window with better spacing, typography, and controls matching macOS 26 design patterns.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/9.4-settings-redesign/CONTEXT.md
@Prunr/Views/SettingsView.swift
@Prunr/Views/MenuBarView.swift

**Tech stack available:** SwiftUI, AppKit, macOS 14+
**Established patterns:**
- macOS design guide: 6pt radius, 5pt inset, 28pt rows (Phase 5)
- Icon-only toolbars with subtle hover states (Phase 9.1, 9.2)
- Secondary gray for subtle elements (Phase 9.1, 9.2)
- .plain button style for native appearance (Phase 9.1)
- .help() modifiers for tooltips (Phase 9.1)
- 16pt horizontal padding, 12-14pt vertical padding (Phase 9.2)

**Constraining decisions:**
- Phase 5: Use native macOS design patterns (GroupBox, checkbox toggles were acceptable at MVP time)
- Phase 9.1-9.3: Modern minimalist design with subtle visual hierarchy
- Phase 9.2: Settings are grouped by function (Reset Baseline in Paths tab)

**Key files to reference:**
- SettingsView.swift - Current settings implementation (5 tabs, GroupBox-based layout)
- MenuBarView.swift - Reference for modern visual patterns (hover states, spacing, colors)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign General Tab with modern layout</name>
  <files>Prunr/Views/SettingsView.swift</files>
  <action>Redesign GeneralSettingsTab to use modern macOS patterns:
1. Replace GroupBox with Form-style layout (label + control pairs)
2. Use Toggle(.switch) instead of Toggle(.checkbox) for modern appearance
3. Update spacing: 16pt between sections, 12pt between rows
4. Use proper typography: 13pt for labels, 11pt for secondary text
5. Add proper visual hierarchy with section headers
6. Remove unnecessary padding (use Form's built-in spacing)

Keep the same functionality:
- Launch at Login toggle
- Drill-Down Threshold slider (50-95%) with percentage display
- Description text below slider

Do NOT change functionality, only visual design.</action>
  <verify>Build succeeds, General tab displays with modern Form-style layout, slider works correctly</verify>
  <done>General tab uses Form layout, .switch toggles, proper spacing/typography</done>
</task>

<task type="auto">
  <name>Task 2: Redesign Paths Tab with improved list styling</name>
  <files>Prunr/Views/SettingsView.swift</files>
  <action>Redesign PathsSettingsTab with modern list styling:
1. Replace checkbox toggles with .switch style
2. Update list row styling: 44pt row height (macOS standard), 16pt horizontal padding
3. Improve path display: monospace font for paths, subtle secondary color
4. Update button styling: use .bordered instead of custom styling
5. Improve "paths changed" warning banner: use proper background, icon spacing
6. Update footer button layout: consistent 8pt padding

Keep the same functionality:
- Default paths with enable/disable toggles
- Custom paths with remove buttons
- Add path button
- Reset Baseline button
- Save button (when paths changed)

Do NOT change functionality, only visual design.</action>
  <verify>Build succeeds, Paths tab displays with modern list styling, all buttons work</verify>
  <done>Paths tab uses 44pt rows, .switch toggles, modern button styling</done>
</task>

<task type="auto">
  <name>Task 3: Redesign Boundaries Tab with modern patterns</name>
  <files>Prunr/Views/SettingsView.swift</files>
  <action>Redesign BoundariesSettingsTab with modern patterns:
1. Replace checkbox toggles with .switch style
2. Update list row styling: 44pt row height, consistent with Paths tab
3. Improve input field: .roundedBorder, proper font size (13pt)
4. Update add button: consistent with other buttons
5. Improve visual hierarchy: section headers, proper spacing

Keep the same functionality:
- Standard boundaries with enable/disable toggles
- Custom boundaries with remove buttons
- Add custom boundary field and button

Do NOT change functionality, only visual design.</action>
  <verify>Build succeeds, Boundaries tab displays with modern styling, add/remove work</verify>
  <done>Boundaries tab uses 44pt rows, .switch toggles, consistent styling</done>
</task>

<task type="auto">
  <name>Task 4: Redesign Debug Tab with improved visual hierarchy</name>
  <files>Prunr/Views/SettingsView.swift</files>
  <action>Redesign DebugSettingsTab with modern patterns:
1. Replace GroupBox with section-style layout
2. Improve button styling: use .bordered, consistent sizing
3. Add proper spacing: 16pt between sections
4. Improve visual hierarchy: section headers with proper typography
5. Use icon + text buttons for better affordance

Keep the same functionality:
- Create Test Data button
- Open in Finder button
- Clean Up button
- Path display
- Status messages

Do NOT change functionality, only visual design.</action>
  <verify>Build succeeds, Debug tab displays with improved visual hierarchy</verify>
  <done>Debug tab uses section-style layout, modern buttons, proper spacing</done>
</task>

<task type="auto">
  <name>Task 5: Redesign About Tab and window frame</name>
  <files>Prunr/Views/SettingsView.swift</files>
  <action>Redesign AboutSettingsTab and update window frame:
1. Replace placeholder icon with app icon from asset catalog
2. Improve layout: proper spacing, centered content
3. Update typography: modern font weights and sizes
4. Update window frame: increase from 450x400 to 500x450 for better proportions
5. Add proper visual hierarchy

Keep the same functionality:
- App name and version display
- Quit button

Do NOT change functionality, only visual design.</action>
  <verify>Build succeeds, About tab displays with app icon, window size is 500x450</verify>
  <done>About tab uses app icon, modern layout, window size updated</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Build succeeds: `xcodebuild -project Prunr.xcodeproj -scheme Prunr build`
- [ ] All 5 tabs display correctly with modern styling
- [ ] All controls (toggles, sliders, buttons) work as before
- [ ] Visual consistency across all tabs
- [ ] Settings window matches visual quality of main popup
</verification>

<success_criteria>
- All 5 tabs redesigned with modern macOS patterns
- Form-style layouts replace GroupBox where appropriate
- .switch toggles replace .checkbox toggles
- Consistent spacing (16pt sections, 12pt rows, 44pt list row height)
- Proper typography (13pt labels, 11pt secondary text)
- Window frame updated to 500x450
- No functionality changes
</success_criteria>

<output>
After completion, create `.planning/phases/9.4-settings-redesign/9.4-01-SUMMARY.md`:

# Phase 9.4 Plan 01: Settings Redesign Summary

**Modernized Settings window with macOS 26 design patterns**

## Accomplishments
- Redesigned all 5 tabs with modern Form-style layouts
- Replaced GroupBox with section-style layouts
- Updated toggles from .checkbox to .switch style
- Improved spacing, typography, and visual hierarchy
- Updated window frame to 500x450
- Added app icon to About tab

## Files Created/Modified
- `Prunr/Views/SettingsView.swift` - Complete redesign of all tabs

## Decisions Made
- Form-style layout for General tab (native macOS pattern)
- .switch toggles throughout for modern appearance
- 44pt row height for list items (macOS standard)
- Consistent spacing across all tabs

## Issues Encountered
[Problems and resolutions, or "None"]

## Next Step
Phase complete, ready for next phase
</output>
