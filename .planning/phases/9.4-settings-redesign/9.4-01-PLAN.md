---
phase: 9.4-settings-redesign
plan: 01
type: execute
domain: swift-ui
---

<objective>
Simplify Settings to 3 tabs: General, Folders, About. Remove dead code and improve clarity.

Purpose: Current settings has 6 tabs with confusing options (drill-down slider is dead code, boundaries need better UX). Consolidate to essential settings only.
Output: Clean 3-tab settings window with intuitive controls.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@Prunr/Views/SettingsView.swift
@Prunr/ViewModels/SettingsStore.swift
@Prunr/Models/BoundaryConfig.swift

**Current tabs (6):** General, Paths, Boundaries, Data, Debug, About
**New tabs (3):** General, Folders, About

**Key findings:**
- drillDownThreshold setting is DEAD CODE (BaselineService hardcodes 0.70, never reads setting)
- Boundaries concept is useful but needs better naming ("Folder Limits") and clearer explanation
- Data/Debug tabs not needed for production

**Design patterns from recent phases:**
- .switch toggles for modern appearance
- 44pt row height for list items
- 16pt horizontal padding, 12pt between rows
- .help() modifiers for tooltips
</context>

<tasks>

<task type="auto">
  <name>Task 1: Restructure SettingsView to 3 tabs</name>
  <files>Prunr/Views/SettingsView.swift</files>
  <action>Update the main SettingsView TabView structure:

1. Remove tabs: Data (.tag(3)), Debug (.tag(4))
2. Rename tabs:
   - "Paths" -> "Folders" with systemImage "folder"
   - "Boundaries" -> Keep in Folders tab (merge)
3. Final structure:
   - General (.tag(0)) - systemImage "gear"
   - Folders (.tag(1)) - systemImage "folder"
   - About (.tag(2)) - systemImage "info.circle"
4. Update window frame to 480x420 (slightly smaller with fewer tabs)
5. Remove DataSettingsTab and DebugSettingsTab structs entirely (delete the code)</action>
  <verify>Build succeeds, only 3 tabs visible: General, Folders, About</verify>
  <done>SettingsView has 3 tabs, Data/Debug tabs removed</done>
</task>

<task type="auto">
  <name>Task 2: Simplify General tab - remove drill-down slider</name>
  <files>Prunr/Views/SettingsView.swift</files>
  <action>Simplify GeneralSettingsTab:

1. Remove the entire "Scanning" GroupBox section (lines ~63-84) containing:
   - Drill-Down Threshold slider
   - Description text
   - This is dead code - BaselineService hardcodes 0.70

2. Keep only the "Startup" section with:
   - Launch Prunr at Login toggle

3. Update layout:
   - Use Form instead of VStack for native macOS styling
   - Use Toggle with .switch style
   - Add descriptive label or help text

Final General tab should be minimal - just the login toggle.</action>
  <verify>Build succeeds, General tab shows only Login toggle, no drill-down slider</verify>
  <done>General tab simplified to Login toggle only</done>
</task>

<task type="auto">
  <name>Task 3: Create combined Folders tab with Paths and Folder Limits</name>
  <files>Prunr/Views/SettingsView.swift</files>
  <action>Create new FoldersSettingsTab combining Paths and Boundaries:

1. Create new struct FoldersSettingsTab with two sections:

**Section 1: "Scan Paths"**
- List of tracked paths (default + custom) with .switch toggles
- Blue folder icon for defaults, orange for custom
- Path display: name on top, path below in secondary color
- Add (+) button to add custom path
- Remove button (red minus) for custom paths only
- Reset Baseline button at bottom

**Section 2: "Folder Limits"** (renamed from Boundaries)
- Clear header explanation: "Stop drilling down into these folders. Useful for dependencies and build artifacts that don't need detailed tracking."
- List of folder names with .switch toggles
- Gray stop icon for standard, orange for custom
- Monospace font for folder names
- Add field + button for custom folder limits
- Common examples shown: node_modules, .git, build, DerivedData

Layout:
- Use List with two Section headers
- Divider between sections
- Footer with action buttons

2. Delete old PathsSettingsTab and BoundariesSettingsTab structs</action>
  <verify>Build succeeds, Folders tab shows both Scan Paths and Folder Limits sections</verify>
  <done>Folders tab created with combined Paths and Folder Limits sections</done>
</task>

<task type="auto">
  <name>Task 4: Redesign Folder Limits section for clarity</name>
  <files>Prunr/Views/SettingsView.swift</files>
  <action>Make Folder Limits section more intuitive:

1. Add clear explanation text at top of section:
   "When you drill into a folder, scanning stops at these boundaries. This keeps node_modules, .git, and build folders as single items instead of showing thousands of files inside."

2. Group standard limits by category with subtle labels:
   - "Package Managers" - node_modules, Pods, vendor, .cargo
   - "Version Control" - .git
   - "Build Output" - build, DerivedData, target, .build
   - "Virtual Environments" - .venv, venv, .conda

   OR simpler: just show all in alphabetical order with the explanation

3. Use .help() tooltip on section header: "Folders matching these names won't be expanded when drilling down"

4. Make toggle behavior clear:
   - ON = drilling stops at this folder (shows as single item)
   - OFF = drill through this folder (shows contents)

5. Custom folder limits at bottom with add field</action>
  <verify>Build succeeds, Folder Limits has clear explanation, toggles are intuitive</verify>
  <done>Folder Limits section redesigned with clear explanation and intuitive toggles</done>
</task>

<task type="auto">
  <name>Task 5: Polish About tab with app icon</name>
  <files>Prunr/Views/SettingsView.swift</files>
  <action>Update AboutSettingsTab:

1. Replace placeholder icon (systemImage "internaldrive") with actual app icon:
   - Use Image(nsImage: NSApp.applicationIconImage) for the app icon
   - Size: 64x64 or 80x80

2. Update layout:
   - App icon at top, centered
   - "Prunr" title below icon
   - Version text (keep "Version 1.0")
   - Spacer
   - Quit button at bottom, centered

3. Clean typography:
   - Title: .title, .bold
   - Version: .caption, .secondary

4. Remove unnecessary divider</action>
  <verify>Build succeeds, About tab shows app icon from asset catalog</verify>
  <done>About tab shows app icon, clean layout with Quit button</done>
</task>

<task type="auto">
  <name>Task 6: Clean up SettingsStore - remove drillDownThreshold</name>
  <files>Prunr/ViewModels/SettingsStore.swift</files>
  <action>Remove dead drillDownThreshold code from SettingsStore:

1. Remove from Keys enum:
   - static let drillDownThreshold = "drillDownThreshold"

2. Remove property:
   - var drillDownThreshold: Double { didSet... }

3. Remove from init():
   - let threshold = UserDefaults.standard.double(forKey: Keys.drillDownThreshold)
   - self.drillDownThreshold = threshold > 0 ? threshold : 0.7

This is safe cleanup - the value was never read by BaselineService.</action>
  <verify>Build succeeds, no references to drillDownThreshold remain in Settings</verify>
  <done>drillDownThreshold removed from SettingsStore</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Build succeeds: `xcodebuild -project Prunr.xcodeproj -scheme Prunr build`
- [ ] Only 3 tabs: General, Folders, About
- [ ] General tab: Only Login toggle (no drill-down slider)
- [ ] Folders tab: Scan Paths section + Folder Limits section with clear explanation
- [ ] About tab: App icon displayed, version, Quit button
- [ ] All functionality preserved (paths, folder limits still work)
- [ ] No compiler warnings about unused drillDownThreshold
</verification>

<success_criteria>
- 3 tabs: General, Folders, About
- Dead code removed (drillDownThreshold, Data tab, Debug tab)
- Folder Limits has clear explanation of what it does
- App icon visible in About tab
- All path and boundary functionality preserved
- Clean, intuitive settings experience
</success_criteria>

<output>
After completion, create `.planning/phases/9.4-settings-redesign/9.4-01-SUMMARY.md`
</output>
