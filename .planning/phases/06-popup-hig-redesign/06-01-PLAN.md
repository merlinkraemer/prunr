---
phase: 06-popup-hig-redesign
plan: 01
type: execute
---

<objective>
Redesign the main popup UI (MenuBarView and GrowthListView) to comply with Apple Human Interface Guidelines (HIG) for macOS.

Purpose: Ensure the main popup feels native and follows Apple's design standards for proper spacing, typography, row heights, and list styling.
Output: Main popup with HIG-compliant spacing (20pt margins, 12-24pt spacing), list row heights (24pt small / 32pt large), and proper icon sizing (16pt small / 24pt large).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-menubar-ui/04-02-FIX-SUMMARY.md
@.planning/phases/05-settings-polish/05-01-SUMMARY.md
@documentation/macos_design_guide2.md
@Prunr/Views/MenuBarView.swift
@Prunr/Views/GrowthListView.swift
@Prunr/Views/DriveBarView.swift

**Tech stack available:** SwiftUI, AppKit, NSStatusItem
**Established patterns:** Menu bar popover, .transient behavior, @Observable store, checkbox toggles
**Constraining decisions:**
- [Phase 04-02-FIX]: MenuBarView layout with DriveBar, GrowthListView, footer actions
- [Phase 05-01]: macOS design patterns: 6pt radius, 5pt inset, 28pt rows (needs HIG update to 20pt margins, 24/32pt rows)
- [Phase 05-01]: SettingsView with TabView, 5-tab organization

**HIG Standards to Apply:**
- Margins: 20pt standard (left, right, bottom)
- Spacing: 12pt minimum, 16-20pt standard, 24pt maximum
- List row heights: 24pt (small) or 32pt (large)
- Icon sizes: 16pt (small) or 24pt (large)
- Popover width: 300-400pt typical (current 320pt is fine)
- Inset tables: 10pt vertical inset before first/after last row
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MenuBarView spacing and margins to HIG standards</name>
  <files>Prunr/Views/MenuBarView.swift</files>
  <action>Update all padding and spacing values in MenuBarView to comply with HIG standards:
- Drive bar section: Change from `.padding(.horizontal, 16)` and `.padding(.top, 16)` to `.padding(20)` for top/bottom/left/right (HIG standard)
- Growth list section: Change from `.padding(.horizontal, 16)` and `.padding(.top, 12)` to `.padding(.horizontal, 20)` and `.padding(.top, 16)` (12-24pt range)
- Footer rows: Update from `.padding(.horizontal, 5)` to `.padding(.horizontal, 20)` (HIG 20pt margin)
- Vertical spacing between footer elements: Update from `.padding(.vertical, 4)` to `.padding(.vertical, 8)` (within 12-24pt range)

Do NOT change the overall frame size (320×420) - this is within HIG typical popover width (300-400pt).
Do NOT change the footer row height from 28pt - this is custom and will be addressed in Task 2.
Do NOT change the DriveBarView internal spacing - that's a separate component.</action>
  <verify>Build succeeds and visual inspection shows proper 20pt margins and 12-20pt spacing throughout MenuBarView</verify>
  <done>All padding values in MenuBarView updated to HIG standards: 20pt margins, 12-20pt spacing between elements</done>
</task>

<task type="auto">
  <name>Task 2: Redesign GrowthListView with HIG list standards</name>
  <files>Prunr/Views/GrowthListView.swift</files>
  <action>Update GrowthListView to use HIG-compliant list row heights and icon sizing:

1. **GrowthItemRow height and spacing:**
   - Change `.padding(.horizontal, 12)` to `.padding(.horizontal, 20)` (HIG standard margin)
   - Change `.padding(.vertical, 10)` to `.padding(.vertical, 12)` (minimum spacing for regular controls)
   - Set explicit `.frame(minHeight: 24)` for rows (HIG small sidebar row height)

2. **Icon sizing:**
   - Change icon container from `.frame(width: 32, height: 32)` to `.frame(width: 24, height: 24)` (HIG small sidebar icon size)
   - Change icon font from `.font(.system(size: 14))` to `.font(.system(size: 16))` (HIG small sidebar symbol size)

3. **List spacing:**
   - Add `.listRowSpacing(12)` to the ScrollView VStack (standard spacing between related elements)
   - Change divider inset from `.padding(.leading, 16)` to `.padding(.leading, 20)` (align with 20pt margin)

4. **Empty state spacing:**
   - Change `.padding(.vertical, 32)` to `.padding(.vertical, 20)` (within HIG 12-24pt range)

Do NOT change the color coding or severity indicators - keep the existing visual design.
Do NOT change the maximum height of 300pt - this is appropriate for scrollable content.</action>
  <verify>Build succeeds and growth list rows display at 24pt height with 16pt icons and proper 20pt margins</verify>
  <done>GrowthListView rows use HIG standards: 24pt row height, 16pt icons, 20pt margins, 12pt spacing</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>MenuBarView and GrowthListView with HIG-compliant spacing, margins, row heights, and icon sizing</what-built>
  <how-to-verify>
    1. Build and run the app: `xcodebuild -project Prunr.xcodeproj -scheme Prunr -configuration Debug build`
    2. Click the menu bar icon to open the popup
    3. Verify 20pt margins: Content should have consistent breathing room from edges (visually compare to System Settings)
    4. Verify list row height: Growth items should be approximately 24pt tall (compact but readable)
    5. Verify icon size: Folder icons should be 16pt (small but clear)
    6. Verify spacing: Elements should feel neither cramped nor too sparse
    7. Check that footer rows (Scan Now, Reset Baseline, Settings) align properly with new margins
    8. Test with growth items loaded to verify list looks correct
    9. Test empty state to verify spacing is correct
  </how-to-verify>
  <resume-signal>Type "approved" if the popup looks native and properly spaced, or describe specific spacing issues to adjust</resume-signal>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Build succeeds without errors
- [ ] MenuBarView uses 20pt margins throughout
- [ ] GrowthListView rows use 24pt height with 16pt icons
- [ ] All spacing values fall within HIG ranges (12-24pt)
- [ ] Visual inspection confirms native macOS feel
</verification>

<success_criteria>

- MenuBarView spacing updated to HIG standards (20pt margins, 12-20pt spacing)
- GrowthListView uses HIG list row heights (24pt) and icon sizes (16pt)
- Popup feels native and follows Apple design guidelines
- No visual regressions in layout or functionality
- All padding/spacing values documented in code comments for future reference
  </success_criteria>

<output>
After completion, create `.planning/phases/06-popup-hig-redesign/06-01-SUMMARY.md`:

# Phase 06-01 Summary

**Main popup redesigned with Apple HIG compliance: 20pt margins, 24pt list rows, 16pt icons**

## Accomplishments

- Updated MenuBarView to use 20pt margins throughout (HIG standard)
- Applied HIG spacing ranges (12-20pt) to all sections
- Redesigned GrowthListView with 24pt row heights (HIG small sidebar)
- Updated icon sizing to 16pt (HIG small sidebar standard)
- Maintained existing visual design while improving spacing compliance

## Files Created/Modified

- `Prunr/Views/MenuBarView.swift` - Updated to HIG spacing standards (20pt margins, 12-20pt spacing)
- `Prunr/Views/GrowthListView.swift` - Updated to HIG list standards (24pt rows, 16pt icons)

## Decisions Made

- Kept 320pt popover width (within HIG 300-400pt typical range)
- Kept custom footer row height at 28pt (balance between HIG 24pt small and 32pt large)
- Maintained existing color coding and severity indicators
- Applied minimum spacing (12pt) for regular controls per HIG

## Issues Encountered

[Document any issues or "None"]

## Next Step

Ready for 06-02-PLAN.md (Settings & About HIG compliance)
</output>
