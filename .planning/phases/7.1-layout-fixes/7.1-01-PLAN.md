---
phase: 7.1-layout-fixes
plan: 01
type: execute
domain: swiftui
---

<objective>
Fix category list layout: nest big files inline, use distinct category colors, stabilize layout

Purpose: The current category list shows item counts but doesn't show big files inline. Icons use severity colors (red/orange/green) instead of distinct category colors, and variable-width "X items" text causes layout jitter.

Output: Category list with nested big files (max 3 + "more"), distinct category icon colors, stable layout with fixed-width item count text.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-category-growth-view/07-CONTEXT.md
@.planning/phases/07-category-growth-view/07-04-SUMMARY.md
@Prunr/Views/CategoryGrowthListView.swift
@Prunr/Models/GrowthCategory.swift
@Prunr/Models/CategoryGrowthItem.swift

**Tech stack available:**
- SwiftUI views and animations
- GrowthCategory.color property (distinct colors per category)
- CategoryGrowthItem.bigItems array (already computed)

**Established patterns:**
- Expandable rows with disclosure indicators
- HStack for horizontal layout with Spacer()
- Fixed-width frames for layout stability

**Constraining decisions:**
- Phase 07-04: Drill-down navigation replaces expand-in-place
- Phase 07-04: 100MB threshold hardcoded for big files
- Phase 07-03: Complete replacement (no toggle between folder/category views)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Show big files as children in main category list</name>
  <files>Prunr/Views/CategoryGrowthListView.swift</files>
  <action>Modify CategoryListRow to show nested big files inline under each category. Display up to 3 big items as child rows (indented, smaller font). If more than 3, show "X more" text at same indentation level. Use CategoryGrowthItem.bigItems array (already computed). Each nested item should show: icon, filename, size on right. Make category row tap-only for drill-down, nested items tap for reveal-in-finder. Add visual separator (thin gray line or spacer) between categories when showing nested items.</action>
  <verify>Build and run: Category list shows categories with nested big files (max 3). Categories with >3 big files show "X more" indicator.</verify>
  <done>Category list displays big files as nested children. Drill-down still works on category row. Nested items are clickable.</done>
</task>

<task type="auto">
  <name>Task 2: Use distinct category colors for icons</name>
  <files>Prunr/Views/CategoryGrowthListView.swift</files>
  <action>Replace growthSeverityColor with item.category.color in CategoryListRow icon Image. This uses the distinct color defined per category (orange for Homebrew, green for node_modules, blue for caches, etc.) instead of the severity-based color. Keep severity color for the growth amount text (+X GB). This creates visual distinction between categories while still showing growth severity via the amount text.</action>
  <verify>Build and run: Each category icon shows its distinct color (Homebrew=orange, node_modules=green, etc.) while growth text remains severity-colored (red/orange/green based on size).</verify>
  <done>Category icons use distinct category colors. Growth text uses severity colors. Visual distinction between categories is clear.</done>
</task>

<task type="auto">
  <name>Task 3: Fix layout stability for variable-width "X items" text</name>
  <files>Prunr/Views/CategoryGrowthListView.swift</files>
  <action>Add .frame(width: 60, alignment: .trailing) to the "X items" Text in CategoryListRow. This prevents layout jitter when item counts change (e.g., "8 items" vs "156 items"). Use 60pt width which accommodates up to 4 digits with "items" suffix. Center-align or trailing-align the text within this frame so it looks balanced.</action>
  <verify>Build and run: Item count text doesn't shift position when counts change. Layout remains stable.</verify>
  <done>"X items" text has fixed 60pt width. Layout doesn't jitter when scrolling through categories with different item counts.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build succeeds: xcodebuild -project Prunr.xcodeproj -scheme Prunr build
- [ ] Category list shows nested big files (max 3 per category)
- [ ] Categories with >3 big files show "X more" indicator
- [ ] Category icons use distinct category colors
- [ ] Growth text still uses severity colors
- [ ] "X items" text has stable layout (no jitter)
- [ ] Drill-down navigation still works
- [ ] Nested big items are clickable
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Big files appear as nested children in main category list
- Category icons have distinct colors (not severity-based)
- Layout is stable with fixed-width item count text
- No functionality broken from Phase 7
  </success_criteria>

<output>
After completion, create `.planning/phases/7.1-layout-fixes/7.1-01-SUMMARY.md`:

# Phase 7.1 Plan 1: Big File Nesting + Category Colors Summary

**Category list now shows nested big files, distinct icon colors, stable layout**

## Accomplishments

- Big files display as nested children under each category (max 3 + "more" indicator)
- Category icons use distinct category colors instead of severity colors
- Item count text has fixed width for layout stability
- Visual hierarchy improved with indentation and separators

## Files Created/Modified

- `Prunr/Views/CategoryGrowthListView.swift` - Modified CategoryListRow to show nested big items, use category colors, fixed item count width

## Decisions Made

- Show max 3 big items inline to avoid overwhelming the list
- Use 60pt fixed width for item count text (accommodates 4 digits)
- Keep severity colors for growth text (shows urgency)

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 7.1-02-PLAN.md - Slide-in navigation and drill-down polish
</output>
