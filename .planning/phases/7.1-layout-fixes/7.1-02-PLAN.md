---
phase: 7.1-layout-fixes
plan: 02
type: execute
domain: swiftui
---

<objective>
Implement slide-in navigation for drill-down and polish visual hierarchy

Purpose: Current drill-down uses simple if/else toggle with no animation. Implement Finder-style slide-in from right that pushes the main list left, creating clear navigation hierarchy.

Output: Smooth slide-in transition for drill-down, improved header visual hierarchy, polished navigation UX.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-category-growth-view/07-CONTEXT.md
@.planning/phases/07-category-growth-view/07-04-SUMMARY.md
@Prunr/Views/CategoryGrowthListView.swift

**Tech stack available:**
- SwiftUI transitions and animations
- @State for navigation state management
- ZStack for layering views

**Established patterns:**
- selectedCategory @State for drill-down state
- CategoryDetailHeader with back button
- Simple if/else view switching

**Constraining decisions:**
- Phase 07-04: Drill-down navigation pattern established (click category → see all items)
- Phase 07-03: macOS design compliance (6pt radius, 5pt inset, 28pt rows)

**Issues being addressed:**
- Current navigation lacks visual feedback (no transition)
- Flat drill-down header doesn't communicate hierarchy clearly
- Back button functional but not visually integrated
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement slide-in animation for drill-down</name>
  <files>Prunr/Views/CategoryGrowthListView.swift</files>
  <action>Replace simple if/else view switching with ZStack + offset animation. Structure: Create ZStack containing both categoryListView and categoryDetailView. Use conditional opacity and offset based on selectedCategory state. When user taps category: animate categoryListView offset to -80% left (pushed out), animate categoryDetailView offset from +80% right to 0 (slides in). When back button tapped: reverse animations. Use .easeInOut(duration: 0.3) for smooth Finder-like motion. Add .transition(.move(edge: .trailing)) as fallback. Ensure views overlap properly during animation (ZStack layering). Main view should dim slightly during drill-down (overlay with black.opacity(0.1)).</action>
  <verify>Build and run: Clicking category slides detail view in from right, pushing main view left. Back button reverses animation smoothly. No flicker or layout issues.</verify>
  <done>Drill-down uses slide-in animation from right. Main view pushes left. Back button slides back with reverse animation. 300ms duration with easeInOut curve.</done>
</task>

<task type="auto">
  <name>Task 2: Improve drill-down header visual hierarchy</name>
  <files>Prunr/Views/CategoryGrowthListView.swift</files>
  <action>Enhance CategoryDetailHeader to look like a navigation bar. Add: (1) Bottom border with Divider() or 1pt gray line, (2) Subtle background gradient or solid color to distinguish from content, (3) Increase padding slightly (vertical 12pt instead of 8pt) for more breathing room, (4) Make category name + icon larger (15pt font instead of 13pt), (5) Add subtle shadow to header for depth. Keep back button on left, category name center (visually balanced), growth amount right. Ensure header stays fixed at top (not scrollable with content).</action>
  <verify>Build and run: Drill-down header looks like native navigation bar. Visual hierarchy is clear. Header stays fixed while content scrolls.</verify>
  <done>Detail header has bottom border, enhanced background, larger font, subtle shadow. Looks like native navigation bar. Fixed position, content scrolls beneath.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete slide-in navigation with polished drill-down header</what-built>
  <how-to-verify>
    1. Run app and create baseline with test data
    2. Click a category with items
    3. Verify: Detail view slides in from right, main view pushes left
    4. Verify: Animation is smooth (~300ms), no stuttering
    5. Verify: Header looks like navigation bar (border, background, shadow)
    6. Click back button
    7. Verify: Reverse animation plays smoothly
    8. Verify: Main view returns to original position
    9. Test on multiple categories to ensure consistency
  </how-to-verify>
  <resume-signal>Type "approved" or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build succeeds: xcodebuild -project Prunr.xcodeproj -scheme Prunr build
- [ ] Slide-in animation works in both directions
- [ ] Main view pushes left, detail view slides in from right
- [ ] Animation duration is ~300ms with easeInOut
- [ ] Detail header has navigation bar styling
- [ ] Header stays fixed, content scrolls
- [ ] Back button reverses animation correctly
- [ ] No visual glitches or layout issues
- [ ] User verification checkpoint passed
</verification>

<success_criteria>

- All tasks completed
- All verification checks pass
- Slide-in navigation works smoothly like Finder
- Drill-down header has proper visual hierarchy
- Animation feels native and polished
- User approved UX at checkpoint
- Phase 7.1 complete (both plans finished)
  </success_criteria>

<output>
After completion, create `.planning/phases/7.1-layout-fixes/7.1-02-SUMMARY.md`:

# Phase 7.1 Plan 2: Slide-In Navigation Summary

**Finder-style slide-in navigation implemented, drill-down header polished**

## Accomplishments

- Slide-in animation from right for drill-down (pushes main view left)
- Reverse animation for back button
- Enhanced detail header with navigation bar styling
- Fixed header position with scrollable content
- Smooth 300ms easeInOut transitions

## Files Created/Modified

- `Prunr/Views/CategoryGrowthListView.swift` - Added ZStack layout, offset animations, enhanced CategoryDetailHeader

## Decisions Made

- 80% offset for push/slide effect (leaves peek of main view visible)
- 300ms easeInOut animation for natural feel
- Slight dim overlay on main view during drill-down
- Bottom border and shadow on header for depth

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

**Phase 7.1 complete!** All layout fixes implemented:
- 7.1-01: Big file nesting, distinct category colors, layout stability
- 7.1-02: Slide-in navigation, polished drill-down header

**Phase 7.1 deliverables complete:**
- Big files show as children in main view (max 3 + "more")
- Drill-down has slide-in navigation (Finder-style)
- Category icons use distinct colors
- Layout stable with fixed-width item count text
- Visual hierarchy improved in drill-down

**Ready for:**
- User testing and feedback
- Distribution build
- Or continue to Phase 6 (Popup HIG Redesign) if desired
</output>
