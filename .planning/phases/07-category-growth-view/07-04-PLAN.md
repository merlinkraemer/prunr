---
phase: 07-category-growth-view
plan: 07-04
type: execute
---

<objective>
Implement category pattern matching logic and verify the complete category-based growth view.

This plan implements the categorization patterns for all 10 categories (Homebrew, node_modules, Downloads, etc.) and performs comprehensive testing to ensure the feature works end-to-end.

Purpose: Complete the categorization layer and validate the entire feature.
Output: Working category detection for all 10 categories, verified feature
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-category-growth-view/07-CONTEXT.md
@documentation/grouping_feature_spec.md
@Prunr/Models/GrowthCategory.swift
@Prunr/Views/CategoryGrowthListView.swift

**Auto-selected based on dependency graph:**
@.planning/phases/05-settings-polish/05-01-SUMMARY.md
@.planning/phases/07-category-growth-view/07-01-SUMMARY.md
@.planning/phases/07-category-growth-view/07-02-SUMMARY.md
@.planning/phases/07-category-growth-view/07-03-SUMMARY.md

**Key files from frontmatter:**
@Prunr/Models/GrowthCategory.swift
@Prunr/Views/CategoryGrowthListView.swift
@Prunr/Services/BaselineService.swift

**Tech stack available:**
- String pattern matching
- Path expansion (~/ expansion)
- Test data creation from Phase 05-01

**Established patterns:**
- Context menu test data creation (Phase 05-01)
- Manual scan trigger for testing
- Reveal in Finder integration

**Constraining decisions:**
- Phase 05-01: Test data generation in MenuBarManager
- Spec defines 10 categories with specific patterns
- Priority order: specific paths first, then general patterns

**Issues being addressed:**
None

**Concerns being verified:**
None from Phase 05
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement category pattern matching</name>
  <files>Prunr/Models/GrowthCategory.swift</files>
  <action>
Implement the categorize(path:) method with pattern matching for all 10 categories:

1. **Path normalization:**
   - Expand tilde: ~ to /Users/username
   - Use NSString.expandingTildeInPath or FileManager.default.homeDirectoryForCurrentUser

2. **Category patterns (in priority order):**

   **Homebrew:**
   - "/usr/local/Cellar"
   - "/opt/homebrew"
   - "/Library/Caches/Homebrew"
   - "Caches/Homebrew"

   **node_modules:**
   - "node_modules" (anywhere in path)

   **Library Caches:**
   - "Library/Caches"
   - "/Library/Caches"
   - "~/.cache"

   **Downloads:**
   - "/Downloads"
   - "~/Downloads"

   **Docker:**
   - "/var/lib/docker"
   - "Library/Containers/com.docker"
   - ".docker"

   **Spotify cache:**
   - "Library/Application Support/Spotify"
   - ".spotify"

   **Browser cache:**
   - "Library/Caches/Google/Chrome"
   - "Library/Caches/Mozilla/Firefox"
   - "Library/Safari"

   **Mail attachments:**
   - "Library/Mail"
   - "~/Library/Mail"

   **Trash:**
   - ".Trash"
   - "~/.Trash"

   **Other (fallback):**
   - Default if no patterns match

3. **Priority order implementation:**
   - Check specific paths first (Homebrew, Docker, Spotify, etc.)
   - Then check general patterns (node_modules, Caches)
   - Fall back to `.other`

4. **Add patterns property to each enum case:**
   - `var patterns: [String] { [...] }`
   - Used by categorize(path:) for matching

5. **Test path verification:**
   - "/usr/local/Cellar/python/3.11" → homebrew
   - "~/dev/myproject/node_modules" → nodeModules
   - "~/Library/Caches/com.apple.Safari" → libraryCaches
   - "~/Downloads/big-video.mov" → downloads
   - "/var/lib/docker/volumes/..." → docker
   - "~/Music/Spotify/podcast" → spotifyCache
   - "~/.Trash/deleted-file.txt" → trash
   - "/unknown/random/path" → other

DO NOT use regex - simple string.contains() is sufficient and faster.
  </action>
  <verify>All test paths categorize correctly, no build warnings</verify>
  <done>Category patterns implemented for all 10 categories with priority ordering</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete category-based growth view with 10 categories, big file nesting, and pattern matching</what-built>
  <how-to-verify>
    1. Build and run the app (Xcode → Run)
    2. Create a baseline:
       - Right-click menu bar icon → "Create Test Data"
       - This generates ~10MB of test data in various categories
    3. Verify categories appear:
       - Open popup (click menu bar icon)
       - Should see categories sorted by size (largest first)
       - Categories: Documents, Downloads, Images, Cache, Logs (from test data)
    4. Verify big file nesting:
       - Test data includes a 4MB "big file" in Downloads
       - Should appear nested under Downloads category
    5. Verify small files collapsed:
       - Should see "X files under 100MB" row
       - Click to expand and see individual small files
    6. Verify expand/collapse:
       - Click any category to expand/collapse
       - Chevron indicator should rotate
    7. Verify reveal in Finder:
       - Right-click any big item → "Reveal in Finder"
       - Finder should open with file selected
    8. Verify no toggle:
       - No "Folders/Categories" toggle switch in UI
       - Only category view visible (complete replacement)
    9. Verify macOS design:
       - Row heights ~28pt
       - Spacing and padding look native
       - No layout issues
    10. Create additional test data:
       - Right-click → "Create Test Data" again
       - Categories should update with new totals
  </how-to-verify>
  <resume-signal>Type "approved" if categories work correctly, or describe issues to fix</resume-signal>
</task>

</tasks>

<verification>
**Build Verification:**
- [ ] Build succeeds with no errors
- [ ] All 10 categories have patterns
- [ ] categorize(path:) returns correct category for test paths

**Functional Testing:**
- [ ] Create test data generates files in multiple categories
- [ ] Categories appear sorted by size (largest first)
- [ ] Big files (>100MB) appear nested under categories
- [ ] Small items collapsed into "X files under 100MB" row
- [ ] Expand/collapse works on categories
- [ ] Reveal in Finder works on big items
- [ ] No toggle switch visible (complete replacement)

**Integration Testing:**
- [ ] Baseline creation still works
- [ ] Manual scan trigger works
- [ ] FSEvents still trigger updates
- [ ] Error messages display correctly
- [ ] Empty state shows when no baseline
- [ ] Settings window still works
</verification>

<success_criteria>

- All 10 categories have working pattern matching
- Category detection works for test paths
- UI displays categories sorted by size
- Big files nested correctly
- Small files collapsed correctly
- Expand/collapse functional
- Reveal in Finder works
- No toggle (complete replacement per CONTEXT.md)
- Manual testing approved
  </success_criteria>

<output>
After completion, create `.planning/phases/07-category-growth-view/07-04-SUMMARY.md`:

# Phase 07-04 Summary

**Category patterns implemented, feature verified and working**

## Accomplishments

- Implemented pattern matching for all 10 categories
- Priority ordering: specific paths first, general patterns last
- Path normalization (tilde expansion)
- Category detection tested and verified
- Manual UAT testing passed
- Complete category-based growth view functional

## Files Modified

- `Prunr/Models/GrowthCategory.swift` - Implemented categorize() with patterns

## Decisions Made

- Used simple string.contains() instead of regex (faster, simpler)
- Priority ordering prevents mis-categorization
- Tilde expansion for accurate ~/ paths matching
- 100MB threshold hardcoded (v1)

## Issues Encountered

None

## Next Phase Readiness

**Phase 07 complete!** Category-based growth view fully implemented:
- 10 categories with pattern matching
- Big file nesting (>100MB)
- Small file collapsing
- Expandable categories
- macOS design compliance
- No toggle (complete replacement)

**Potential future enhancements:**
- Configurable big file threshold
- Category-specific drill-down
- Category-based cleanup actions
- Additional categories based on user feedback

**Ready for:**
- User testing and feedback
- Distribution build if MVP complete
</output>
