---
phase: 07-category-growth-view
plan: 07-02
type: execute
---

<objective>
Add category growth aggregation to BaselineService and prepare GrowthItem for category compatibility.

This plan integrates the category models with the existing baseline system: adding getCategoryGrowthList() to BaselineService for aggregated category data, and updating GrowthItem with category-aware properties.

Purpose: Connect category models to existing baseline/delta tracking.
Output: BaselineService.getCategoryGrowthList(), GrowthItem.isBigFile computed property
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-category-growth-view/07-CONTEXT.md
@Prunr/Services/BaselineService.swift
@Prunr/Services/CategoryDetectionService.swift
@Prunr/Models/CategoryGrowthItem.swift

**Auto-selected based on dependency graph:**
@.planning/phases/05-settings-polish/05-01-SUMMARY.md
@.planning/phases/07-category-growth-view/07-01-SUMMARY.md

**Key files from frontmatter:**
@Prunr/Services/BaselineService.swift
@Prunr/ViewModels/SettingsStore.swift

**Tech stack available:**
- Existing BaselineService actor
- CategoryDetectionService from 07-01
- GRDB for database queries

**Established patterns:**
- Actor-based services
- GrowthItem struct in BaselineService
- getGrowthList() pattern for fetching deltas

**Constraining decisions:**
- Phase 05-01: BaselineService.GrowthItem struct for growth data
- Phase 07-01: CategoryDetectionService.categorizeDeltas() pattern
- Keep existing getGrowthList() for backward compatibility

**Issues being addressed:**
None

**Concerns being verified:**
None from Phase 05
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add getCategoryGrowthList() to BaselineService</name>
  <files>Prunr/Services/BaselineService.swift</files>
  <action>
Add method to build category growth list:

1. **New method:**
   ```swift
   func getCategoryGrowthList() async throws -> [CategoryGrowthItem]
   ```

2. **Implementation:**
   - Fetch current deltas using existing `getChangesSinceBaseline()`
   - Use CategoryDetectionService.shared.categorizeDeltas(deltas) to group by category
   - For each category with deltas:
     * Calculate total growth: sum of all growthBytes
     * Calculate current size: sum of all currentSizeBytes
     * Separate big items (>100MB) using filterBigItems()
     * Count small items and sum their sizes
     * Calculate percent of total growth
   - Sort categories by total growth bytes (descending)
   - Return [CategoryGrowthItem]

3. **Integration:**
   - Keep existing `getGrowthList()` method unchanged
   - New method is parallel API for category-based view
   - Reuse existing snapshot comparison logic
   - Use CategoryGrowthItem.bigFileThreshold for filtering

4. **Error handling:**
   - Throw BaselineError.noBaseline if no baseline exists
   - Return empty array if no changes detected
   - Log category totals for debugging

DO NOT modify getGrowthList() - maintain backward compatibility.
  </action>
  <verify>Build succeeds, method returns [CategoryGrowthItem], categories sorted by size</verify>
  <done>getCategoryGrowthList() added to BaselineService, returns sorted category items</done>
</task>

<task type="auto">
  <name>Task 2: Update GrowthItem model for category compatibility</name>
  <files>Prunr/Services/BaselineService.swift</files>
  <action>
Ensure BaselineService.GrowthItem works with category display:

1. **Review existing GrowthItem struct:**
   - Check if `path`, `growthBytes`, `currentSizeBytes`, `percentOfParent` are sufficient
   - Located in BaselineService.swift (not separate file)

2. **Add computed property:**
   ```swift
   var isBigFile: Bool {
       growthBytes >= CategoryGrowthItem.bigFileThreshold
   }
   ```

3. **Add category property (optional but useful):**
   ```swift
   var category: GrowthCategory {
       GrowthCategory.categorize(path: path)
   }
   ```

4. **Ensure compatibility:**
   - Both folder view and category view use same base GrowthItem
   - No duplication required
   - Category detection is lazy (computed property)

DO NOT create separate GrowthItem.swift file - keep nested in BaselineService.
  </action>
  <verify>Build succeeds, isBigFile and category computed properties work correctly</verify>
  <done>GrowthItem has isBigFile and category computed properties</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] BaselineService.build() succeeds without errors
- [ ] getCategoryGrowthList() returns [CategoryGrowthItem]
- [ ] GrowthItem.isBigFile property works
- [ ] GrowthItem.category property works
- [ ] No regressions in getGrowthList()
</verification>

<success_criteria>

- getCategoryGrowthList() method working in BaselineService
- Categories sorted by total growth bytes
- Big items separated from small items correctly
- GrowthItem has category-aware computed properties
- No regressions in existing folder view
  </success_criteria>

<output>
After completion, create `.planning/phases/07-category-growth-view/07-02-SUMMARY.md`:

# Phase 07-02 Summary

**Category data aggregation integrated with BaselineService**

## Accomplishments

- Added getCategoryGrowthList() to BaselineService
- Added isBigFile computed property to GrowthItem
- Added category computed property to GrowthItem
- Categories sorted by size descending

## Files Modified

- `Prunr/Services/BaselineService.swift` - Added getCategoryGrowthList(), updated GrowthItem

## Decisions Made

- Kept getGrowthList() for backward compatibility
- Used CategoryGrowthItem.bigFileThreshold for consistency
- Computed category property (lazy evaluation)

## Issues Encountered

None

## Next Step

Ready for 07-03-PLAN.md - Category UI implementation
</output>
