---
phase: 07-category-growth-view
plan: 07-01
type: execute
---

<objective>
Create the core category models and detection service that power the category-based growth view.

This plan establishes the foundational data structures: GrowthCategory enum with 10 categories (Homebrew, node_modules, Downloads, etc.), CategoryGrowthItem model for displaying category data, and CategoryDetectionService for intelligent file categorization.

Purpose: Build the categorization layer that transforms scattered files into logical groupings.
Output: GrowthCategory enum, CategoryGrowthItem model, CategoryDetectionService
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-category-growth-view/07-CONTEXT.md
@documentation/grouping_feature_spec.md
@Prunr/Services/BaselineService.swift
@Prunr/Models/DeltaCategory.swift (reference only - legacy)

**Auto-selected based on dependency graph:**
@.planning/phases/05-settings-polish/05-01-SUMMARY.md

**Key files from frontmatter:**
@Prunr/Services/BaselineService.swift
@Prunr/ViewModels/SettingsStore.swift

**Tech stack available:**
- SwiftUI for UI (Color, Image)
- Foundation for file path handling
- Existing actor patterns (BaselineService)

**Established patterns:**
- Actor-based services (BaselineService)
- Singleton pattern with shared instance
- Enum-based categorization (DeltaCategory reference)

**Constraining decisions:**
- Phase 05-01: BaselineService.GrowthItem struct pattern for growth data
- Phase 07-CONTEXT: "No toggle" - complete replacement of folder view

**Issues being addressed:**
None

**Concerns being verified:**
None from Phase 05
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create GrowthCategory model</name>
  <files>Prunr/Models/GrowthCategory.swift</files>
  <action>
Create GrowthCategory enum with all v1 categories from the spec:

1. **Enum cases:**
   - homebrew
   - nodeModules
   - libraryCaches
   - downloads
   - docker
   - spotifyCache
   - browserCache
   - mailAttachments
   - trash
   - other

2. **Display properties:**
   - `displayName: String` - Human-readable name for UI
   - `icon: String` - SF Symbol name (use Image(systemName:))
   - `color: Color` - Accent color for category (optional)

3. **Pattern matching:**
   - `patterns: [String]` - Path substrings for this category
   - `static func categorize(path: String) -> GrowthCategory`
   - Check patterns in priority order (specific paths first)
   - Fall back to `.other` if no match

4. **Icon mapping:**
   - Homebrew: "mug.fill"
   - node_modules: "circle.grid.2x2.fill"
   - Caches: "folder.fill"
   - Downloads: "arrow.down.circle.fill"
   - Docker: "shippingbox.fill"
   - Spotify: "music.note"
   - Browser: "globe"
   - Mail: "envelope.fill"
   - Trash: "trash.fill"
   - Other: "doc.fill"

Reference DeltaCategory.swift for patterns but create new implementation from scratch.

DO NOT use the existing DeltaCategory.swift - it's legacy code.
  </action>
  <verify>File compiles without errors, all 10 enum cases have displayName and icon</verify>
  <done>GrowthCategory.swift created with 10 categories, display properties, and categorize() method stub</done>
</task>

<task type="auto">
  <name>Task 2: Create CategoryGrowthItem model</name>
  <files>Prunr/Models/CategoryGrowthItem.swift</files>
  <action>
Create CategoryGrowthItem struct to represent a category with its growth data:

1. **Properties:**
   - `category: GrowthCategory`
   - `totalGrowthBytes: Int64` - Sum of all growth in this category
   - `currentSizeBytes: Int64` - Current total size
   - `bigItems: [BaselineService.GrowthItem]` - Individual items >100MB
   - `smallItemCount: Int` - Number of items <=100MB
   - `smallItemTotalBytes: Int64` - Total size of small items
   - `percentOfTotal: Double` - Percentage of total growth

2. **Computed properties:**
   - `formattedGrowth: String` - Human-readable size (e.g., "+4.1 GB")
   - `itemCount: Int` - Total items (big + small)
   - `hasSmallItems: Bool` - Whether there are small items to collapse

3. **Identifiable conformance:**
   - `id: String { category.displayName }`

4. **Helper property:**
   - `static let bigFileThreshold: Int64 = 100 * 1024 * 1024` // 100MB hardcoded

Use BaselineService.GrowthItem type for bigItems to maintain consistency.
  </action>
  <verify>File compiles, conforms to Identifiable, has all computed properties</verify>
  <done>CategoryGrowthItem.swift created with properties, computed values, and 100MB threshold</done>
</task>

<task type="auto">
  <name>Task 3: Create CategoryDetectionService</name>
  <files>Prunr/Services/CategoryDetectionService.swift</files>
  <action>
Create CategoryDetectionService actor for categorizing deltas:

1. **Singleton service:**
   - `static let shared = CategoryDetectionService()`
   - Private init()
   - Use actor for thread safety

2. **Main method:**
   - `func categorizeDeltas(_ deltas: [BaselineService.GrowthItem]) -> [GrowthCategory: [BaselineService.GrowthItem]]`
   - Iterate through all deltas
   - Use GrowthCategory.categorize(path:) for each delta
   - Return dictionary mapping category to its deltas

3. **Helper methods:**
   - `func filterBigItems(_ deltas: [BaselineService.GrowthItem]) -> [BaselineService.GrowthItem]`
   - `func filterSmallItems(_ deltas: [BaselineService.GrowthItem]) -> [BaselineService.GrowthItem]`
   - `func calculateTotalGrowth(_ deltas: [BaselineService.GrowthItem]) -> Int64`
   - `func calculateCurrentSize(_ deltas: [BaselineService.GrowthItem]) -> Int64`

4. **Logging:**
   - Log category assignment for debugging
   - Log unmatched paths for future category additions

DO NOT add pattern matching logic in this service - that belongs in GrowthCategory.categorize().
  </action>
  <verify>File compiles, actor pattern matches BaselineService, categorizeDeltas() returns dictionary</verify>
  <done>CategoryDetectionService actor created with categorization and helper methods</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] All three files compile without errors
- [ ] GrowthCategory has 10 cases with display properties
- [ ] CategoryGrowthItem conforms to Identifiable
- [ ] CategoryDetectionService uses actor pattern
- [ ] No build warnings
</verification>

<success_criteria>

- GrowthCategory enum with 10 categories created
- CategoryGrowthItem model with properties and computed values
- CategoryDetectionService actor with categorization methods
- All files compile without errors
- Foundation ready for data aggregation (next plan)
  </success_criteria>

<output>
After completion, create `.planning/phases/07-category-growth-view/07-01-SUMMARY.md`:

# Phase 07-01 Summary

**Core category models and detection service created**

## Accomplishments

- Created GrowthCategory enum with 10 categories
- Created CategoryGrowthItem model for display data
- Created CategoryDetectionService actor for categorization

## Files Created

- `Prunr/Models/GrowthCategory.swift` - Category enum with display properties
- `Prunr/Models/CategoryGrowthItem.swift` - Category growth data model
- `Prunr/Services/CategoryDetectionService.swift` - Categorization service

## Decisions Made

- Used actor pattern for CategoryDetectionService (consistent with BaselineService)
- 100MB threshold hardcoded in CategoryGrowthItem
- Reused BaselineService.GrowthItem type for consistency

## Issues Encountered

None

## Next Step

Ready for 07-02-PLAN.md - Category data aggregation
</output>
