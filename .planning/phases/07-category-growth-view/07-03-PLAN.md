---
phase: 07-category-growth-view
plan: 07-03
type: execute
---

<objective>
Implement the category-based UI and replace the folder list view.

This plan creates CategoryGrowthListView with expandable categories and big file nesting, then replaces GrowthListView in MenuBarView (complete replacement per CONTEXT.md - no toggle).

Purpose: Transform the UI from folder-based to category-based growth display.
Output: CategoryGrowthListView, updated MenuBarView
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-category-growth-view/07-CONTEXT.md
@Prunr/Views/MenuBarView.swift
@Prunr/Views/GrowthListView.swift
@Prunr/Services/BaselineService.swift
@Prunr/Models/CategoryGrowthItem.swift

**Auto-selected based on dependency graph:**
@.planning/phases/05-settings-polish/05-01-SUMMARY.md
@.planning/phases/07-category-growth-view/07-01-SUMMARY.md
@.planning/phases/07-category-growth-view/07-02-SUMMARY.md

**Key files from frontmatter:**
@Prunr/Views/MenuBarView.swift
@Prunr/Views/GrowthListView.swift
@Prunr/Services/BaselineService.swift
@Prunr/ViewModels/SettingsStore.swift

**Tech stack available:**
- SwiftUI for UI
- BaselineService.getCategoryGrowthList() from 07-02
- Existing GrowthListView pattern for reference

**Established patterns:**
- SwiftUI views with @State for expansion
- GrowthListView with ScrollView + VStack
- MenuBarView with popover content

**Constraining decisions:**
- Phase 05-01: macOS design guide (6pt radius, 5pt inset, 28pt rows)
- Phase 07-CONTEXT: "No toggle" - complete replacement, not hybrid view
- Follow GrowthListView structure for consistency

**Issues being addressed:**
None

**Concerns being verified:**
None from Phase 05
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create CategoryGrowthListView</name>
  <files>Prunr/Views/CategoryGrowthListView.swift</files>
  <action>
Create new SwiftUI view for category-based growth display:

1. **View structure:**
   - ScrollView with VStack of category rows
   - Each category row shows:
     * Category icon (left) - 20pt SF Symbol
     * Category name (bold)
     * Total growth with arrow indicator (e.g., "+4.1 GB ↗")
     * Chevron for expand/collapse
   - Big items nested under category (indented 24pt)
   - Collapsed "X files under 100MB" row (expandable)

2. **Category row design:**
   - Height: 32pt (standard list row per Phase 05-01)
   - Growth text: monospaced for alignment
   - Color-coded by severity:
     * Green: < 1GB
     * Orange: 1GB - 5GB
     * Red: >= 5GB

3. **Big item rows:**
   - Indented by 24pt
   - Show file name (not full path)
   - Show size with percentage
   - Smaller text (.caption) than category row

4. **Collapsed row:**
   - "X files under 100MB +Y MB"
   - Chevron to expand
   - When expanded: show all small items

5. **State:**
   - `@State private var expandedCategories: Set<String>`
   - `@State private var expandedSmallItems: Set<String>`
   - `@State private var categoryItems: [CategoryGrowthItem] = []`
   - `@State private var isLoading: Bool = false`

6. **Data loading:**
   - `private func loadCategories()` async
   - Call `try await BaselineService.shared.getCategoryGrowthList()`
   - Handle BaselineError.noBaseline (show empty state)

7. **Interactions:**
   - Tap category row to toggle expand/collapse
   - Tap big item to reveal in Finder (NSWorkspace.shared.activateFileViewerSelectingURL)
   - Drill-down support: DEFER for future phase

8. **Empty state:**
   - Icon + "No growth detected" message
   - "Create a baseline to track changes" prompt
   - Centered in ScrollView

9. **macOS design compliance (Phase 05-01):**
   - 6pt corner radius
   - 5pt inset padding
   - 28pt row heights
   - Native spacing

Reference GrowthListView.swift for structure, but adapt for category hierarchy.
  </action>
  <verify>View compiles, categories render, expand/collapse works, reveal in Finder works</verify>
  <done>CategoryGrowthListView.swift created with expandable categories and big file nesting</done>
</task>

<task type="auto">
  <name>Task 2: Replace GrowthListView with CategoryGrowthListView</name>
  <files>Prunr/Views/MenuBarView.swift</files>
  <action>
Replace the folder-based growth list with category-based view:

1. **Remove GrowthListView:**
   - Delete GrowthListView from the hierarchy
   - Keep GrowthListView.swift file (may be useful for reference or future use)

2. **Add CategoryGrowthListView:**
   - Replace GrowthListView with CategoryGrowthListView
   - Pass same @State variables (isScanning, errorMessage)

3. **Update data loading:**
   - CategoryGrowthListView handles its own async loading
   - No changes to MenuBarViewModel needed

4. **DO NOT add toggle:**
   - Per CONTEXT.md: "The transformation is complete: the entire folder list becomes a category list. No hybrid view, no toggle."
   - This is a complete replacement, not an optional view

5. **Keep other UI elements:**
   - DriveBarView (unchanged)
   - Scan progress overlay (unchanged)
   - Error messages (unchanged)
   - Reset/Settings/Quit buttons (unchanged)

DO NOT create a view mode toggle - this is a complete replacement per CONTEXT.md.
  </action>
  <verify>Build succeeds, app launches, category view appears in popover, no toggle visible</verify>
  <done>GrowthListView replaced with CategoryGrowthListView, no toggle added</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build succeeds without errors
- [ ] CategoryGrowthListView renders in popover
- [ ] Categories expand/collapse correctly
- [ ] Big files nested under categories
- [ ] Reveal in Finder works
- [ ] No toggle switch in UI
- [ ] macOS design guidelines followed (28pt rows, 6pt radius)
</verification>

<success_criteria>

- CategoryGrowthListView created and functional
- Folder list completely replaced with category view
- No view mode toggle (per CONTEXT.md)
- Categories expandable with big file nesting
- Reveal in Finder works on big items
- macOS design compliance maintained
  </success_criteria>

<output>
After completion, create `.planning/phases/07-category-growth-view/07-03-SUMMARY.md`:

# Phase 07-03 Summary

**Category UI implemented, folder view replaced**

## Accomplishments

- Created CategoryGrowthListView with expandable categories
- Replaced GrowthListView in MenuBarView (complete replacement)
- Big files nested under categories
- Small files collapsed into expandable row
- Color-coded severity (green/orange/red)
- macOS design compliance (28pt rows, 6pt radius)

## Files Created

- `Prunr/Views/CategoryGrowthListView.swift` - Category list UI

## Files Modified

- `Prunr/Views/MenuBarView.swift` - Replaced GrowthListView with CategoryGrowthListView

## Decisions Made

- Complete replacement (no toggle) per CONTEXT.md
- Deferred category-specific drill-down for future
- Color-coded severity for quick visual scan
- Big file threshold: 100MB (from 07-01)

## Issues Encountered

None

## Next Step

Ready for 07-04-PLAN.md - Category patterns and verification
</output>
