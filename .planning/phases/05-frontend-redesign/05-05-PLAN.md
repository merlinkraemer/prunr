---
phase: 05-frontend-redesign
plan: 05
type: execute
---

<objective>
Add visual growth bars to delta items, making size changes obvious at a glance.

Purpose: Visual feedback makes big changes immediately obvious—users can spot space hogs without reading individual numbers.

Output: Growth bars rendered alongside size changes in ItemColumn and DetailColumn, with proportional width based on the largest change in the set.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-frontend-redesign/05-CONTEXT.md
@.planning/phases/05-frontend-redesign/05-03-SUMMARY.md
@.planning/phases/05-frontend-redesign/05-04-SUMMARY.md
@Prunr/Views/ItemColumn.swift
@Prunr/Views/DetailColumn.swift
@Prunr/Models/Delta.swift
@Prunr/Models/DeltaCategory.swift

**Tech stack available:** SwiftUI, GRDB v7+, @Observable @MainActor pattern

**Established patterns:**
- @State for local view state management
- Color-coded growth indicators (green = growth, red = shrinkage)
- Percentage badges with rounded capsule shape
- Path truncation for readability

**Constraining decisions:**
- 05-04: Used manual UserDefaults for @Observable compatibility
- 05-03: Category-based navigation with @State selection management
- 05-02: @State for PathManager to resolve @MainActor constraints

**Issues being addressed:** None (feature enhancement)

**Target UX from CONTEXT.md:**
```
│ ▸ Desktop                 +45GB ████│
│ ▸ Homebrew: postgresql    +12GB ███ │
│ ▸ tmp-videos-folder       +25GB 🆕  │
│ ▸ node_modules            +8GB  ██  │
│ └─ ~/Library/Caches       +3GB  █   │
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add GrowthBarView component</name>
  <files>Prunr/Views/GrowthBarView.swift</files>
  <action>Create a new SwiftUI view component for rendering growth bars:

1. Input: `changeBytes: Int64` and `maxBytes: Int64` (for proportional width)
2. Visual: Horizontal bar using `GeometryReader` or fixed-width segments
3. Color: Green for growth, red for shrinkage, gray for neutral
4. Segments: Use rounded rectangles or a `ProgressView` style—avoid text characters (█) as native SwiftUI rendering is cleaner
5. Width: Calculate ratio `abs(changeBytes) / maxBytes` for bar width (capped at 100%)
6. Include a "new" indicator (🆕) for items where `oldSizeBytes == nil`

Do NOT use text characters—use native SwiftUI shapes.
</action>
  <verify>Build succeeds, GrowthBarView renders with varying widths based on input ratios</verify>
  <done>GrowthBarView.swift created with proportional bar rendering and new-item indicator</done>
</task>

<task type="auto">
  <name>Task 2: Integrate GrowthBarView into ItemColumn</name>
  <files>Prunr/Views/ItemColumn.swift</files>
  <action>Update ItemRow to display growth bars:

1. Add `@Binding var maxChangeBytes: Int64` to ItemColumn (passed from parent)
2. Calculate `maxChangeBytes` in ItemColumn from `filteredDeltas.map { abs($0.changeBytes) }.max() ?? 1`
3. Add `GrowthBarView` below the size change/percentage row
4. Pass `changeBytes: delta.changeBytes` and `maxBytes: maxChangeBytes`
5. Ensure bar width is proportional to the largest change in the filtered set

Keep existing colored text and percentage badges—add bar as additional visual element.
</action>
  <verify>Items show growth bars with proportional widths; largest item has full-width bar</verify>
  <done>ItemColumn displays growth bars for each delta item</done>
</task>

<task type="auto">
  <name>Task 3: Add growth bar to DetailColumn</name>
  <files>Prunr/Views/DetailColumn.swift</files>
  <action>Add a prominent growth bar to DetailColumn:

1. Add `GrowthBarView` above the detailed size breakdown
2. Use a larger height for the bar (e.g., 8pt vs 4pt in list)
3. Pass the selected delta's `changeBytes` with an appropriate max value
4. Add text label below bar showing the absolute change amount (e.g., "+12.5 GB")

This makes the selected item's change visually prominent.
</action>
  <verify>DetailColumn shows large growth bar with change label</verify>
  <done>DetailColumn displays prominent growth indicator</done>
</task>

<task type="auto">
  <name>Task 4: Add GrowthBarView to Xcode project</name>
  <files>Prunr.xcodeproj/project.pbxproj</files>
  <action>Add `Prunr/Views/GrowthBarView.swift` to the Xcode project under the Views group. Use the existing project structure as reference for file insertion.
</action>
  <verify>Build succeeds without errors</verify>
  <done>GrowthBarView included in build</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Prunr.xcodeproj -scheme Prunr build` succeeds
- [ ] Growth bars render in ItemColumn with proportional widths
- [ ] Growth bar color matches change direction (green/red)
- [ ] "New" indicator appears for items without oldSizeBytes
- [ ] DetailColumn shows prominent growth bar
</verification>

<success_criteria>

- GrowthBarView component created with native SwiftUI rendering
- ItemColumn displays proportional growth bars for each item
- DetailColumn shows prominent growth indicator
- Largest change in set = full-width bar
- Color coding consistent with existing green/red pattern
  </success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-redesign/05-05-SUMMARY.md`:

# Phase 5 Plan 05: Growth Bars and Visual Indicators Summary

**[One-liner]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.ts` - Description
- `path/to/another.ts` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 05-06-PLAN.md (Smart Grouping)
</output>
