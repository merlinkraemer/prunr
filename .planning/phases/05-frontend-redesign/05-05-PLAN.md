---
phase: 05-frontend-redesign
plan: 05
type: execute
---

<objective>
Replace the 3-column view with a DaisyDisk-style scan results screen.

Purpose: Simple, visual scan results showing growth grouped by logical categories (apps, packages, projects, filetypes, homebrew, docker, npm, media files).

Output: ScanResultsView showing category cards with growth bars; clicking drills down to file list.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@Prunr/Views/RootView.swift
@Prunr/Views/ColumnContainerView.swift
@Prunr/Models/Delta.swift
@Prunr/Models/DeltaCategory.swift
@Prunr/ViewModels/MainViewModel.swift

**Current state:** ColumnContainerView with 3-column navigation (CategoryColumn → ItemColumn → DetailColumn) - to be replaced.

**New UX: DaisyDisk-style results**
- Categories as cards/rows with growth bars
- Click category → drill down to file list (Finder-like)
- Each file shows: size, "NEW" badge if added since last scan
- Sorted by size (largest first)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ScanResultsView with category grid</name>
  <files>Prunr/Views/ScanResultsView.swift</files>
  <action>Create a new view showing scan results as category cards:

1. Accept `[Delta]` as input
2. Group deltas by extended category (apps, packages, projects, homebrew, docker, npm, media, other)
3. Display each category as a card/row with:
   - Category icon and name
   - Total growth (e.g., "+12.5 GB")
   - Growth bar (horizontal, proportional)
   - Item count
4. Use LazyVGrid or VStack with cards
5. Click card → navigate to CategoryDetailView

This replaces ColumnContainerView with a simpler DaisyDisk-style layout.
</action>
  <verify>Build succeeds, categories display with growth bars</verify>
  <done>ScanResultsView created with category cards</done>
</task>

<task type="auto">
  <name>Task 2: Create CategoryDetailView for file list drill-down</name>
  <files>Prunr/Views/CategoryDetailView.swift</files>
  <action>Create a Finder-like file list view for a selected category:

1. Accept category filter and `[Delta]` as input
2. Display filtered deltas as a list with columns:
   - Filename (with parent path in secondary text)
   - Size (current)
   - Change (+/- amount)
   - "NEW" badge if oldSizeBytes == nil
3. Sort by newSizeBytes descending (largest first)
4. Use Table or List with custom row layout
5. Back button to return to ScanResultsView

This is the detail view showing all files in a category.
</action>
  <verify>Build succeeds, file list shows sorted by size with NEW badges</verify>
  <done>CategoryDetailView created with file list</done>
</task>

<task type="auto">
  <name>Task 3: Extend category detection for smart grouping</name>
  <files>Prunr/Models/DeltaCategory.swift</files>
  <action>Add pattern detection for logical categories:

1. Extend DeltaCategory with new cases: .homebrew, .docker, .npm, .media
2. Add path patterns:
   - homebrew: /usr/local/Cellar, /opt/homebrew, ~/Library/Caches/Homebrew
   - docker: /var/lib/docker, ~/Library/Containers/com.docker.*, ~/.docker
   - npm: contains "node_modules" anywhere in path
   - media: extensions .mp4, .mov, .avi, .mkv, .jpg, .png, .psd, .ai (large files only)
3. Update categorize(path:) to detect these patterns
4. Keep existing categories (apps, packages, containers, caches, developer, other)

This enables smart grouping by source (homebrew, docker, npm, etc.).
</action>
  <verify>Path detection returns correct category for known patterns</verify>
  <done>DeltaCategory extended with source-based categories</done>
</task>

<task type="auto">
  <name>Task 4: Create GrowthBarView component</name>
  <files>Prunr/Views/GrowthBarView.swift</files>
  <action>Create visual growth bar component:

1. Input: changeBytes, maxBytes (for proportional width)
2. Render: Horizontal bar using RoundedRectangle
3. Color: Green for growth, red for shrinkage, gray for neutral
4. Width: (abs(changeBytes) / maxBytes) * availableWidth
5. Animate: .animation(.easeInOut, value: changeBytes)

Use native SwiftUI shapes (not text characters).
</action>
  <verify>Build succeeds, bars render with correct width and color</verify>
  <done>GrowthBarView component created</done>
</task>

<task type="auto">
  <name>Task 5: Update RootView to use ScanResultsView</name>
  <files>Prunr/Views/RootView.swift</files>
  <action>Replace ColumnContainerView with ScanResultsView:

1. Update DetailContentView to show ScanResultsView instead of ColumnContainerView
2. Pass viewModel.deltas to ScanResultsView
3. Remove CategoryColumn, ItemColumn, DetailColumn imports (no longer used)

This switches from 3-column navigation to the 2-screen DaisyDisk-style flow.
</action>
  <verify>App launches, selecting path shows scan results with categories</verify>
  <done>RootView uses new ScanResultsView</done>
</task>

<task type="auto">
  <name>Task 6: Add navigation between results and detail</name>
  <files>Prunr/Views/RootView.swift, Prunr/Views/ScanResultsView.swift</files>
  <action>Add navigation flow:

1. Add @State var selectedCategory: DeltaCategory? to RootView
2. Pass binding to ScanResultsView
3. When category selected, show CategoryDetailView in place of ScanResultsView
4. Add back button in CategoryDetailView to clear selection
5. Use NavigationStack or conditional view switching

This enables drill-down from category to file list.
</action>
  <verify>Clicking category shows file list, back button returns to results</verify>
  <done>Navigation flow between views working</done>
</task>

<task type="auto">
  <name>Task 7: Add new views to Xcode project</name>
  <files>Prunr.xcodeproj/project.pbxproj</files>
  <action>Add ScanResultsView.swift, CategoryDetailView.swift, and GrowthBarView.swift to the Xcode project under the Views group.
</action>
  <verify>Build succeeds without errors</verify>
  <done>New views included in build</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Prunr.xcodeproj -scheme Prunr build` succeeds
- [ ] Selecting path shows scan results with category cards
- [ ] Each category shows growth bar and total change
- [ ] Clicking category drills down to file list
- [ ] File list shows sorted by size with NEW badges
- [ ] Back button returns to category overview
</verification>

<success_criteria>

- ScanResultsView displays categories with growth bars (DaisyDisk-style)
- CategoryDetailView shows file list sorted by size
- NEW badge appears for files added since last scan
- Smart grouping detects homebrew, docker, npm, media files
- Navigation flows: sidebar → results → detail → back
  </success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-redesign/05-05-SUMMARY.md`:

# Phase 5 Plan 05: DaisyDisk-Style Scan Results Summary

**Replaced 3-column view with DaisyDisk-style scan results showing categories with growth bars.**

## Accomplishments

- Created ScanResultsView with category cards showing growth bars
- Created CategoryDetailView for Finder-like file list drill-down
- Extended DeltaCategory with smart grouping (homebrew, docker, npm, media)
- Created GrowthBarView component for visual feedback
- Updated RootView navigation flow

## Files Created/Modified

- `Prunr/Views/ScanResultsView.swift` - Category grid with growth bars
- `Prunr/Views/CategoryDetailView.swift` - File list with NEW badges
- `Prunr/Views/GrowthBarView.swift` - Visual growth bar component
- `Prunr/Models/DeltaCategory.swift` - Extended with source categories
- `Prunr/Views/RootView.swift` - Updated navigation flow

## Decisions Made

- Replaced 3-column navigation with simpler 2-screen flow (per user feedback)
- DaisyDisk-style category cards instead of Finder-style columns
- File list sorted by current size (largest first)

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 5 complete. Ready for Phase 6: Cleanup Actions (future).
</output>
