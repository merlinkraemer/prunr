---
phase: 05-frontend-redesign
plan: 02
type: execute
---

<objective>
Redesign delta list rows with improved information density, visual encoding, and readability.

Purpose: Make it easier to scan and understand what changed at a glance with better use of space and visual cues.
Output: Redesigned DeltaRowView with improved layout, better visual encoding, and optional detail disclosure.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/phase-prompt.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-redesign/05-01-SUMMARY.md
@Prunr/Views/DeltaRowView.swift
@Prunr/Models/Delta.swift
@Prunr/Models/Snapshot.swift

**Tech stack available:**
- SwiftUI List with .inset or .insetAlternated style
- Delta model with path, oldSizeBytes, newSizeBytes, changeBytes, percentChange

**Established patterns:**
- Path-based ID for SwiftUI stability
- Color coding: green for growth, red for shrinkage
- ByteCountFormatter for human-readable sizes

**Key files:**
- Prunr/Views/DeltaRowView.swift - Current row design (123 lines)
- Prunr/Models/Delta.swift - Data model with all delta properties
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign DeltaRowView with improved layout and visual encoding</name>
  <files>Prunr/Views/DeltaRowView.swift</files>
  <action>Completely redesign the row layout for better information density:

1. New layout structure (horizontal):
   - Left: Icon/indicator (16x16) showing change type visually
     - Up arrow with circle background for growth (green)
     - Down arrow with circle background for shrinkage (red)
     - Circle dot for new items (blue)
     - X mark for deleted items (gray)
   - Middle: Path and metadata (expanded width)
     - Primary: Path in .body.monospaced (truncated from left if >60 chars)
     - Secondary: "Old → New" size info in .caption.secondary (only if both exist)
     - For new items: "New item" badge in .caption2
     - For deleted: "Deleted" badge in .caption2
   - Right: Change values (right-aligned, ~80pt width)
     - Primary: Change amount with sign (+1.2 GB, -500 MB) in .semibold.monospaced
     - Secondary: Percentage in .caption2 (if available)

2. Visual improvements:
   - Add .padding(.vertical, 8) for better row height
   - Use .listRowSeparator(.hidden) and add custom dividers between groups
   - Add hover effect: .background(.quaternary.opacity(0.5)) when hovered
   - Use proper Color semantic colors (not .green/.red directly)

3. Size formatting improvements:
   - For bytes < 1MB: show "123 KB" (always show unit)
   - For bytes >= 1MB: show "1.2 GB" with one decimal
   - For large changes: use appropriate scale automatically

DO NOT:
- Don't change the Delta model
- Don't break path truncation logic (keep left-truncation)</action>
  <verify>Build succeeds. Rows display correctly with new layout. All delta types (growth, shrinkage, new, deleted) show appropriate icons.</verify>
  <done>Each delta row shows icon, path with metadata, and change values in a clean, scannable layout. Visual encoding makes change types immediately apparent.</done>
</task>

<task type="auto">
  <name>Task 2: Add disclosure for folder contents and detail view</name>
  <files>Prunr/Views/DeltaRowView.swift, Prunr/Views/DeltaDetailView.swift</files>
  <action>Add expandable rows for items that have child deltas:

1. Create DeltaDetailView component:
   - Shows full path (not truncated)
   - Table with: property | value format
   - Properties: path, old size, new size, change bytes, percent change
   - Use .form or Grid for layout
   - Add "Reveal in Finder" button if path exists

2. Add disclosure logic:
   - Expand on double-click or Chevron button tap
   - Use @State var isExpanded: Bool
   - Animate expansion with default animation
   - Show DeltaDetailView when expanded

3. Folder children exploration (future-proofing):
   - Add placeholder for "View child items" when path has children
   - For now, show grayed-out text "Child items not yet explored"
   - This prepares for future drill-down feature

DO NOT:
- Don't implement actual child fetching in this plan
- Don't change the delta calculation logic
- Keep it simple - detail view only, no nested list yet</action>
  <verify>Build succeeds. Rows expand on click to show detail view. Detail view displays all properties correctly.</verify>
  <done>Clicking a row (or chevron) expands to show DeltaDetailView with full path, all size values, and Reveal in Finder button.</done>
</task>

<task type="auto">
  <name>Task 3: Add list grouping and section dividers</name>
  <files>Prunr/Views/MainView.swift</files>
  <action>Group deltas by change category with visual separators:

1. Add computed property to MainViewModel:
   - enum DeltaCategory { growth, shrinkage, new, deleted }
   - var groupedDeltas: [(category: DeltaCategory, deltas: [Delta])]

2. Update List to display sections:
   - Use ForEach over groupedDeltas
   - Each section has a header: "Growth (+X items)", "Shrinkage (-X items)", etc.
   - Use .listSectionSeparator(.hidden) with custom dividers

3. Section header design:
   - Left-aligned section title in .caption.bold
   - Section summary (total change for category) in .secondary
   - Subtle background: .quaternary.opacity(0.3)
   - Collapsible sections (toggle chevron)

4. Sorting within sections:
   - Growth: sorted by magnitude descending (biggest growth first)
   - Shrinkage: sorted by magnitude descending (biggest shrinkage first)
   - New: sorted by size descending
   - Deleted: sorted by size descending

DO NOT:
- Don't break the existing compareSnapshots logic
- Don't change the overall sort order logic (still by magnitude within sections)</action>
  <verify>Build succeeds. Deltas are grouped into sections. Section headers show correct counts and totals.</verify>
  <done>List is grouped by change type with collapsible sections. Each section has a header showing category name and count.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build succeeds: `xcodebuild -project Prunr.xcodeproj -scheme Prunr build`
- [ ] Delta rows display with new layout: icon, path, change values
- [ ] All change types show correct icons and colors
- [ ] Rows expand to show detail view
- [ ] Sections group deltas correctly by category
- [ ] Section headers are collapsible and show correct counts
</verification>

<success_criteria>

- DeltaRowView redesigned with icon, path metadata, and change values
- Visual encoding makes change types immediately apparent (color + icon)
- Rows expand on click to show DeltaDetailView with full details
- List is grouped by change category with collapsible sections
- All existing functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-redesign/05-02-SUMMARY.md`:

# Phase 5 Plan 02: Delta List Redesign Summary

**Delta rows redesigned with visual encoding, expandable details, and section grouping**

## Accomplishments

- DeltaRowView redesigned with icon indicators, improved layout, and better visual encoding
- DeltaDetailView created showing full details with Reveal in Finder button
- List grouped by change category with collapsible section headers
- Section headers show category counts and totals

## Files Created/Modified

- `Prunr/Views/DeltaRowView.swift` - Redesigned with icon, better layout, disclosure
- `Prunr/Views/DeltaDetailView.swift` - New detail view component
- `Prunr/Views/MainView.swift` - Updated to use grouped sections
- `Prunr/ViewModels/MainViewModel.swift` - Added groupedDeltas computed property

## Decisions Made

[Document any UI design decisions made during implementation]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 05-03-PLAN.md (Polish & Visual Feedback)
</output>
