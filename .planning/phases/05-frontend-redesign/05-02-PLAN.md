---
phase: 05-frontend-redesign
plan: 02
type: execute
domain: swiftui-macos
---

<objective>
Create Finder-style sidebar with NavigationSplitView and path management.

Purpose: Replace the current single-pane layout with a sidebar that allows users to manage and select scan paths.
Output: NavigationSplitView container, sidebar view with add/remove path functionality.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-redesign/05-RESEARCH.md
@.planning/phases/05-frontend-redesign/05-CONTEXT.md

# Auto-selected based on dependency graph:
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/04-ui-polish/04-01-SUMMARY.md
@.planning/phases/04-ui-polish/04-02-SUMMARY.md

# Key files from frontmatter:
@Prunr/Views/MainView.swift
@Prunr/ViewModels/MainViewModel.swift
@Prunr/Actions/AppActions.swift

**Tech stack available:** NavigationSplitView (macOS 13+), @AppStorage, @Observable @MainActor
**Established patterns:**
- @Observable @MainActor ViewModel pattern (Phase 04)
- FocusedValue for menu commands (Phase 04-02)
- NavigationStack for view hierarchy (Phase 04)

**Constraining decisions:**
- Phase 04-02: FocusedValue pattern for menu-to-view action binding
- RESEARCH.md: Use NavigationSplitView for macOS sidebar, not NavigationView (deprecated)

**Issues being addressed:** None
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create RootView with NavigationSplitView</name>
  <files>Prunr/Views/RootView.swift</files>
  <action>Create RootView struct with NavigationSplitView. Add @State private var selectedPath: TrackedPath? and @State private var columnVisibility: NavigationSplitViewVisibility = .all. Use NavigationSplitView(columnVisibility: $columnVisibility) with sidebar column and detail placeholder. Configure sidebar width with .navigationSplitViewColumnWidth(min: 150, ideal: 200, max: 300). Use .navigationSplitViewStyle(.balanced). Move PrunrApp window configuration to RootView. This replaces the current NavigationStack in MainView.</action>
  <verify>swiftc -parse Prunr/Views/RootView.swift compiles without errors</verify>
  <done>RootView with NavigationSplitView setup, configurable column visibility</done>
</task>

<task type="auto">
  <name>Task 2: Create SidebarView with path list and add/remove</name>
  <files>Prunr/Views/SidebarView.swift</files>
  <action>Create SidebarView struct accepting @Binding var selectedPath: TrackedPath?. Use @State private var pathManager = PathManager() for path state. Create List(selection: $selectedPath) with two Sections: "Favorites" (default paths) and "Custom" (user-added). Each row shows displayName with SF folder icon. Add .toolbar with "+" button to add path using .fileImporter(isPresented:) for folder selection. Add delete support via .onDelete(perform:) on custom paths only (isDefault == false). Use .listStyle(.sidebar) for native macOS translucent sidebar appearance.</action>
  <verify>swiftc -parse Prunr/Views/SidebarView.swift compiles, has List with two sections</verify>
  <done>SidebarView with default/custom sections, add/delete path functionality</done>
</task>

<task type="auto">
  <name>Task 3: Integrate RootView into app hierarchy</name>
  <files>Prunr/PrunrApp.swift, Prunr/Views/MainView.swift</files>
  <action>Update PrunrApp to display RootView instead of MainView. Move NavigationSplitView window configuration from PrunrApp to RootView. Update MainView to become the detail view content - remove its NavigationStack wrapper, keep the delta list and toolbar content. Pass selectedPath binding from RootView to MainView as a parameter. Update ContentView to display RootView. This maintains the menu command integration via AppActions.</action>
  <verify>xcodebuild -project Prunr.xcodeproj -scheme Prunr build succeeds</verify>
  <done>App opens with sidebar + detail layout, MainView appears as detail content</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild` succeeds without errors
- [ ] Sidebar appears on left with default paths
- [ ] Add path button opens file picker
- [ ] Delete removes custom paths only (not defaults)
- [ ] Sidebar uses translucent .sidebar style
</verification>

<success_criteria>

- NavigationSplitView displays sidebar on left, detail on right
- Sidebar shows default paths (Home, Documents, etc.) in Favorites section
- User can add custom paths via file picker
- User can remove custom paths (not defaults)
- Detail area shows MainView content (to be updated in 05-03)
  </success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-redesign/05-02-SUMMARY.md`:

# Phase 5 Plan 02: Sidebar View Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `Prunr/Views/RootView.swift` - Description
- `Prunr/Views/SidebarView.swift` - Description
- `Prunr/PrunrApp.swift` - Updated to use RootView
- `Prunr/Views/MainView.swift` - Refactored as detail view

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 05-03-PLAN.md (Three-Column View)
</output>
