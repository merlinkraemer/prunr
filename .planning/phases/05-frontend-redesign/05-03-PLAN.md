---
phase: 05-frontend-redesign
plan: 03
type: execute
---

<objective>
Add visual feedback, animations, and polish for a professional, responsive app experience.

Purpose: Complete the UX overhaul with smooth transitions, hover effects, and refined interactions.
Output: Polished UI with animations, hover states, and refined visual details throughout.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/phase-prompt.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-redesign/05-01-SUMMARY.md
@.planning/phases/05-frontend-redesign/05-02-SUMMARY.md
@Prunr/Views/MainView.swift
@Prunr/Views/DeltaRowView.swift
@Prunr/Views/DeltaDetailView.swift

**Tech stack available:**
- SwiftUI animation system (.animation, .transition, withAnimation)
- @Observable @MainActor ViewModels for state-driven animations

**Established patterns:**
- Section-based layout with summary stats
- Grouped delta list with disclosure
- Color-coded change indicators

**Key files:**
- Prunr/Views/MainView.swift - Main window with sections
- Prunr/Views/DeltaRowView.swift - Row component
- Prunr/Views/DeltaDetailView.swift - Detail view
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add smooth transitions and animations</name>
  <files>Prunr/Views/MainView.swift, Prunr/Views/DeltaRowView.swift</files>
  <action>Add SwiftUI animations for state changes:

1. List content changes:
   - Wrap List content with .animation(.easeInOut(duration: 0.3), value: viewModel.deltas)
   - Add .id() to List for proper diffing when content changes
   - Use .transition(.opacity.combined(with: .move(edge: .top))) for new items

2. Section collapse/expand:
   - Animate section header chevron rotation: .rotationEffect(.degrees(isExpanded ? 90 : 0))
   - Animate section content visibility: default spring animation

3. Row disclosure:
   - Animate detail view expansion: .animation(.spring(response: 0.3, dampingFraction: 0.7), value: isExpanded)
   - Add .transition(.asymmetric(insertion: .opacity, removal: .opacity)) for detail view

4. Scanning progress:
   - Animate progress banner appearance: .transition(.move(edge: .top).combined(with: .opacity))
   - Add .animation(.easeInOut, value: viewModel.isScanning)

5. Snapshot picker changes:
   - Animate comparison results: .animation(.easeInOut(duration: 0.2), value: viewModel.selectedBeforeSnapshot)
   - Animate summary stats changes with number transition effect

DO NOT:
- Don't over-animate - keep animations subtle (200-350ms)
- Don't animate every single property independently
- Avoid animations that interfere with picker selection</action>
  <verify>Build succeeds. All state changes have smooth transitions. Animations don't feel janky or slow.</verify>
  <done>State changes (deltas loading, sections expanding, rows disclosing) have smooth, subtle animations. Scanning banner animates in/out gracefully.</done>
</task>

<task type="auto">
  <name>Task 2: Add hover effects and interactive feedback</name>
  <files>Prunr/Views/DeltaRowView.swift, Prunr/Views/DeltaDetailView.swift, Prunr/Views/MainView.swift</files>
  <action>Add hover and interactive feedback for better UX:

1. Delta row hover effects:
   - Add @State private var isHovered: Bool = false
   - On hover: .background(.quaternary.opacity(0.5)) with .clipShape(.rect(cornerRadius: 6))
   - On hover: Scale chevron icon slightly: .scaleEffect(isHovered ? 1.1 : 1.0)
   - Change cursor to .clickable on hover (if possible in SwiftUI for macOS)

2. Button hover states:
   - Quick action buttons: .buttonStyle(.bordered) with hover brightness adjustment
   - Scan button: Emphasize on hover with subtle scale
   - Use .background(.hover) for interactive elements

3. Section header hover:
   - Highlight on hover to indicate clickability
   - Chevron rotates immediately on header click

4. Reveal in Finder button:
   - Prominent button style with hover glow
   - Add .help("Open this folder in Finder") tooltip

5. Copy actions:
   - Add "Copy Path" button on row hover (subtle appearance)
   - Copy to clipboard with brief confirmation haptic/visual feedback

DO NOT:
- Don't add hover effects that feel jarring
- Keep hover states subtle and professional
- Don't break existing click handlers</action>
  <verify>Build succeeds. Hover effects work on all interactive elements. Buttons clearly show interactability.</verify>
  <done>Interactive elements have clear hover feedback. Rows highlight on hover. Buttons show affordance. Cursor indicates clickable areas.</done>
</task>

<task type="auto">
  <name>Task 3: Refine colors, typography, and visual polish</name>
  <files>Prunr/Views/MainView.swift, Prunr/Views/DeltaRowView.swift, Prunr/Views/DeltaDetailView.swift</files>
  <action>Apply final visual polish with refined colors and typography:

1. Color refinement:
   - Replace literal colors with semantic colors:
     - Growth: .green (system green, not custom)
     - Shrinkage: .red (system red)
     - New items: .blue (system blue)
     - Deleted: .secondary.opacity(0.6)
   - Use .foregroundStyle(.secondary) for metadata
   - Use .foregroundStyle(.tertiary) for less important info
   - Add subtle opacity variations for depth

2. Typography refinement:
   - Use .monospaced() only for numbers and paths
   - Use .body.weight(.medium) for emphasis (not .bold which is too heavy)
   - Use .caption for metadata, .caption2 for very small text
   - Maintain consistent line heights

3. Visual details:
   - Add subtle shadows to key elements: .shadow(color: .black.opacity(0.05), radius: 2, y: 1)
   - Use .clipShape(.rect(cornerRadius: 8)) for containers
   - Add .background(.ultraThinMaterial) for overlays/banners
   - Refine divider opacity: .opacity(0.2)

4. Icon refinement:
   - Ensure all SF Symbols are .scaledToFit
   - Use .fontWeight(.medium) for icons (not .bold)
   - Consistent icon sizing: 16x16 for small, 20x20 for standard

5. Window chrome polish:
   - Ensure window title appears correctly
   - Verify toolbar buttons are properly aligned
   - Check standard window controls (close/minimize/maximize) behavior

DO NOT:
- Don't introduce custom colors that don't adapt to dark/light mode
- Don't use hard-coded font sizes - respect system dynamic type
- Don't add shadows that look heavy or dated</action>
  <verify>Build succeeds. App looks professional in both light and dark mode. Typography is consistent. Colors adapt properly to system appearance.</verify>
  <done>Visual polish complete. Colors are semantic and adapt to theme. Typography hierarchy is clear. Icons are consistent. Overall appearance is professional and refined.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Complete Phase 5 frontend redesign with polished, professional UI including:
- Redesigned main window layout with sections
- Summary statistics header
- Redesigned delta rows with visual encoding
- Expandable detail views
- Grouped sections with collapsible headers
- Smooth animations throughout
- Hover effects on interactive elements
- Refined colors and typography</what-built>
  <how-to-verify>
    1. Build and run: `xcodebuild -project Prunr.xcodeproj -scheme Prunr build && open build/Build/Products/Debug/Prunr.app`
    2. Test scan workflow: Click Scan, observe progress animation
    3. Compare two snapshots: Check summary stats accuracy
    4. Scroll delta list: Verify grouping and section headers
    5. Click section headers: Verify collapse/expand animation
    6. Click a delta row: Verify disclosure animation and detail view
    7. Hover over elements: Verify hover effects appear
    8. Test in both light and dark mode (System Settings > Appearance)
    9. Verify "Reveal in Finder" works for existing paths
    10. Check menu shortcuts still work: Cmd+R, Cmd+Shift+R
  </how-to-verify>
  <resume-signal>Type "approved" when the redesign looks professional and all features work correctly</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build succeeds in both Debug and Release configurations
- [ ] All animations are smooth (300ms or less)
- [ ] Hover effects work on all interactive elements
- [ ] Colors adapt properly to light/dark mode
- [ ] Typography respects system dynamic type
- [ ] Window behaves correctly (resize, minimize/maximize, close)
- [ ] All existing features still work (scan, compare, refresh)
</verification>

<success_criteria>

- All animations are smooth and subtle (200-350ms)
- Hover effects clearly indicate interactivity
- Colors are semantic and adapt to system appearance
- Typography is consistent with proper hierarchy
- Window chrome is polished
- Phase 5 complete - app feels professional and ready to ship
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-redesign/05-03-SUMMARY.md`:

# Phase 5 Plan 03: Polish & Visual Feedback Summary

**Complete frontend redesign with animations, hover effects, and refined visual polish**

## Accomplishments

- Smooth transitions for all state changes (list updates, sections, disclosure)
- Hover effects on interactive elements (rows, buttons, sections)
- Refined semantic colors that adapt to light/dark mode
- Consistent typography hierarchy with proper weight and size
- Professional window chrome and visual details

## Files Created/Modified

- `Prunr/Views/MainView.swift` - Animations and visual polish
- `Prunr/Views/DeltaRowView.swift` - Hover effects and refined styling
- `Prunr/Views/DeltaDetailView.swift` - Polish and interactions

## Decisions Made

[Document any design decisions made during implementation]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Phase 5 complete! Milestone 1 (Core App) is complete. App is ready for distribution.

Before shipping:
1. Resolve ISS-001 (scan failure bug) if still present
2. Test on target macOS version (14+ Sonoma)
3. Create distribution build with Developer ID
4. Notarize with Apple
</output>
