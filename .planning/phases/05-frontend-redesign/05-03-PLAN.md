---
phase: 05-frontend-redesign
plan: 03
type: execute
domain: swiftui-macos
---

<objective>
Create three-column view for category-based navigation of delta data.

Purpose: Enable users to drill down through Categories → Items → Details, replacing the flat delta list with Finder-style navigation.
Output: ColumnContainerView with CategoryColumn, ItemColumn, and DetailColumn.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-redesign/05-RESEARCH.md
@.planning/phases/05-frontend-redesign/05-CONTEXT.md

# Auto-selected based on dependency graph:
@.planning/phases/03-delta-engine/03-01-SUMMARY.md
@.planning/phases/04-ui-polish/04-01-SUMMARY.md
@.planning/phases/05-frontend-redesign/05-01-SUMMARY.md

# Key files from frontmatter:
@Prunr/Models/Delta.swift
@Prunr/Models/DeltaCategory.swift
@Prunr/Views/MainView.swift
@Prunr/ViewModels/MainViewModel.swift

**Tech stack available:** NavigationSplitView, List with selection binding, @Observable @MainActor
**Established patterns:**
- @Observable @MainActor for view state (Phase 04)
- Path-based Identifiable ID for SwiftUI stability (Phase 03)
- Delta model with sizeBytes and changeBytes (Phase 03)

**Constraining decisions:**
- Phase 03: Path-based ID for SwiftUI stability (not UUID or hashValue)
- RESEARCH.md: Use custom HStack of Lists, not NSBrowser (simpler, sufficient for needs)
- CONTEXT.md: Fixed categories only - no custom categories

**Issues being addressed:** None
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ColumnContainerView with three-column layout</name>
  <files>Prunr/Views/ColumnContainerView.swift</files>
  <action>Create ColumnContainerView struct accepting deltas: [Delta] binding. Add @State private var selectedCategory: DeltaCategory? and @State private var selectedItem: Delta?. Build HStack(spacing: 0) with three columns separated by Divider(). Column 1: CategoryColumn (150-200px width). Column 2: ItemColumn (200-300px width). Column 3: DetailColumn (250px min width). Each column passes its selection state to the next. Use .frame(minWidth:max:) for responsive sizing. When no category selected, show empty state hint "Select a category".</action>
  <verify>swiftc -parse Prunr/Views/ColumnContainerView.swift succeeds</verify>
  <done>ColumnContainerView with HStack of three columns, responsive sizing</done>
</task>

<task type="auto">
  <name>Task 2: Create CategoryColumn for category selection</name>
  <files>Prunr/Views/CategoryColumn.swift</files>
  <action>Create CategoryColumn struct accepting @Binding var selectedCategory: DeltaCategory? and deltas: [Delta]. Computed property groups deltas by category using DeltaCategory.categorize(path:). Create List(selection: $selectedCategory) with ForEach(DeltaCategory.allCases) showing Label(category.displayName, systemImage: category.icon). Show item count per category using grouped deltas. Use .listStyle(.inset) for clean appearance. Add "All" option at top with doc.text.magnifyingglass icon to show all items unfiltered.</action>
  <verify>swiftc -parse Prunr/Views/CategoryColumn.swift succeeds</verify>
  <done>CategoryColumn showing all 6 categories with item counts</done>
</task>

<task type="auto">
  <name>Task 3: Create ItemColumn and DetailColumn</name>
  <files>Prunr/Views/ItemColumn.swift, Prunr/Views/DetailColumn.swift</files>
  <action>ItemColumn: Accept @Binding var selectedItem: Delta?, selectedCategory: DeltaCategory?, deltas: [Delta]. Filter deltas by selectedCategory (or show all if nil). Create List(selection: $selectedItem) with ForEach showing row with path (left-truncated), size change (colored green/red), and percentage badge. DetailColumn: Accept item: Delta? binding. Show empty state "Select an item" when nil. When set, display: full path, before/after sizes using ByteCountFormatter, absolute change, percentage change, and category badge. Use VStack with spacing for clean layout.</action>
  <verify>swiftc -parse Prunr/Views/ItemColumn.swift and DetailColumn.swift succeed</verify>
  <done>ItemColumn filters by category, DetailColumn shows full delta details</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild` succeeds without errors
- [ ] Three columns appear with proper spacing
- [ ] Category selection updates ItemColumn content
- [ ] Item selection updates DetailColumn content
- [ ] "All" option shows unfiltered items
</verification>

<success_criteria>

- Three-column layout displays properly with dividers
- CategoryColumn shows all 6 categories with counts
- ItemColumn filters items by selected category
- DetailColumn shows full item details when selected
- Empty states guide user to make selections
  </success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-redesign/05-03-SUMMARY.md`:

# Phase 5 Plan 03: Three-Column View Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `Prunr/Views/ColumnContainerView.swift` - Description
- `Prunr/Views/CategoryColumn.swift` - Description
- `Prunr/Views/ItemColumn.swift` - Description
- `Prunr/Views/DetailColumn.swift` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 05-04-PLAN.md (Simplified Comparison)
</output>
