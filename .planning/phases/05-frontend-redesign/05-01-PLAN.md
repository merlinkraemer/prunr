---
phase: 05-frontend-redesign
plan: 01
type: execute
domain: swiftui-macos
---

<objective>
Create data models and persistence layer for the Finder-style sidebar.

Purpose: Establish the foundation for tracking user-configurable scan paths with persistent storage.
Output: TrackedPath model, DeltaCategory enum, and @AppStorage-based path persistence.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-redesign/05-RESEARCH.md
@.planning/phases/05-frontend-redesign/05-CONTEXT.md

# Auto-selected based on dependency graph:
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/04-ui-polish/04-01-SUMMARY.md

# Key files from frontmatter:
@Prunr/Models/Delta.swift
@Prunr/ViewModels/MainViewModel.swift

**Tech stack available:** GRDB.swift 7.0+, @Observable @MainActor pattern, @AppStorage
**Established patterns:**
- @Observable @MainActor ViewModel pattern for SwiftUI (Phase 04)
- DatabaseManager.shared for all database operations (Phase 01)
- FetchableRecord/PersistableRecord for models (Phase 01)

**Constraining decisions:**
- Phase 01: GRDB v7.0+ minimum for latest Swift concurrency features
- Phase 04: @Observable @MainActor pattern for thread-safe UI state management

**Issues being addressed:** None
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create TrackedPath model with persistence</name>
  <files>Prunr/Models/TrackedPath.swift</files>
  <action>Create TrackedPath struct conforming to Codable, Identifiable, Equatable, Hashable. Properties: id (UUID), url (URL), displayName (String), isDefault (Bool). Use url.bookmarkData() for security-scoped bookmark storage (future-proofing for sandbox). Add static defaultPaths property with Home, Documents, Downloads, Desktop, Developer paths.</action>
  <verify>swiftc -parse Prunr/Models/TrackedPath.swift succeeds, struct has all required properties</verify>
  <done>TrackedPath model compiles with Codable, Identifiable, Equatable, Hashable conformances</done>
</task>

<task type="auto">
  <name>Task 2: Create DeltaCategory enum for classification</name>
  <files>Prunr/Models/DeltaCategory.swift</files>
  <action>Create enum DeltaCategory: String, CaseIterable, Identifiable. Cases: apps (".app"), packages (".pkg", ".mpkg"), containers ("~/Library/Containers"), caches ("~/Library/Caches"), developer ("node_modules", "DerivedData", ".build"), other (default). Each case has: displayName (String), icon (String SF Symbol), patterns ([String]) for path matching. Add computed property `func categorize(path: String) -> DeltaCategory` that matches paths to categories.</action>
  <verify>swiftc -parse Prunr/Models/DeltaCategory.swift succeeds, enum has 6 cases</verify>
  <done>DeltaCategory enum with 6 cases, each with displayName, icon, and matching patterns</done>
</task>

<task type="auto">
  <name>Task 3: Create PathManager with @AppStorage persistence</name>
  <files>Prunr/ViewModels/PathManager.swift</files>
  <action>Create @Observable @MainActor PathManager class. Use @AppStorage("trackedPaths") private var pathData: Data = Data(). Implement computed var paths: [TrackedPath] with JSONEncoder/JSONDecoder for Data persistence. Add addPath(url: URL), removePath(_ path: TrackedPath), and static defaultPaths property. Follow the @Observable @MainActor pattern established in Phase 04. Use @AppStorage for persistence - this is the standard SwiftUI approach (per RESEARCH.md).</action>
  <verify>swiftc -parse Prunr/ViewModels/PathManager.swift succeeds, compiles with MainViewModel</verify>
  <done>PathManager with @AppStorage persistence, add/remove methods, default paths</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `swiftc -parse` succeeds for all new files
- [ ] TrackedPath has Codable for @AppStorage serialization
- [ ] DeltaCategory.categorize() correctly classifies common paths
- [ ] PathManager uses @AppStorage with JSON encoding
</verification>

<success_criteria>

- All three models created and compile
- TrackedPath supports security-scoped bookmarks
- DeltaCategory classifies all 6 categories correctly
- PathManager persists paths via @AppStorage
  </success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-redesign/05-01-SUMMARY.md`:

# Phase 5 Plan 01: Models and Persistence Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `Prunr/Models/TrackedPath.swift` - Description
- `Prunr/Models/DeltaCategory.swift` - Description
- `Prunr/ViewModels/PathManager.swift` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 05-02-PLAN.md (Sidebar View)
</output>
