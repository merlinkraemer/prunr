---
phase: 05-frontend-redesign
plan: 01
type: execute
---

<objective>
Redesign main window layout with improved visual hierarchy, better spacing, and clear section organization.

Purpose: Create a professional, organized interface that guides the user through scanning and comparison workflow.
Output: MainView with rethought layout using proper sections, improved spacing, and clearer visual hierarchy.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/phase-prompt.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-ui-polish/04-01-SUMMARY.md
@.planning/phases/04-ui-polish/04-02-SUMMARY.md
@Prunr/Views/MainView.swift
@Prunr/Views/DeltaRowView.swift
@Prunr/ViewModels/MainViewModel.swift

**Tech stack available:**
- SwiftUI with NavigationStack
- @Observable @MainActor ViewModel pattern
- FocusedValue pattern for menu commands
- ByteCountFormatter for size formatting

**Established patterns:**
- @Observable @MainActor ViewModels for UI state
- FocusedValue pattern for menu-to-view communication
- Empty state handling with contextual messages
- Color coding: green for growth, red for shrinkage

**Key files:**
- Prunr/Views/MainView.swift - Current main window (251 lines)
- Prunr/Views/DeltaRowView.swift - Delta row component (123 lines)
- Prunr/ViewModels/MainViewModel.swift - State management (203 lines)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Redesign snapshot selector section with better visual hierarchy</name>
  <files>Prunr/Views/MainView.swift</files>
  <action>Replace current snapshotPickers HStack with a proper section container:

- Use LabeledContent or custom GroupBox-style container for the picker section
- Add a section header "Compare Snapshots" with SF Symbol icon
- Improve picker layout: stack vertically with labels above each picker
- Add visual separation (background, border) around the selector area
- Include a "Quick Actions" subsection with buttons for "Scan Now" and "Compare"
- Use consistent padding: .padding(16) for section, .padding(8) for elements
- Add keyboard shortcuts indicator in button labels

DO NOT:
- Don't change ViewModel logic (UI-only change)
- Don't break FocusedValue bindings for menu commands
- Don't remove the auto-select behavior

Reference macOS apps like Finder, Disk Utility for picker section styling.</action>
  <verify>Build succeeds. Pickers still work, menu shortcuts (Cmd+R, Cmd+Shift+R) still function.</verify>
  <done>Snapshot section has clear visual hierarchy, labeled pickers, quick action buttons with keyboard shortcuts visible.</done>
</task>

<task type="auto">
  <name>Task 2: Add summary stats header above delta list</name>
  <files>Prunr/Views/MainView.swift, Prunr/ViewModels/MainViewModel.swift</files>
  <action>Add a summary section above the delta list that shows:

1. Computed property in MainViewModel:
   - totalGrowth: Int64 (sum of all positive changes)
   - totalShrinkage: Int64 (absolute sum of all negative changes)
   - netChange: Int64 (totalGrowth - totalShrinkage)
   - itemsChanged: Int (count of deltas)

2. New SummaryHeaderView component showing:
   - "Net Change" as prominent large value (color-coded: green for positive, red for negative)
   - "Growth" and "Shrinkage" as smaller secondary values
   - Items count
   - Use grid layout with proper alignment

3. Place above delta list, only show when deltas exist
   - Use .listSectionHeader style or custom container
   - Add subtle background to distinguish from list

DO NOT:
- Don't modify the Delta model
- Don't change the sorting logic (still by magnitude)</action>
  <verify>Build succeeds. Summary shows correct totals when deltas displayed. Matches sum of individual delta changes.</verify>
  <done>Summary header displays above list with net change, growth, shrinkage, and item count. Values are accurate and color-coded.</done>
</task>

<task type="auto">
  <name>Task 3: Improve spacing, typography, and overall layout polish</name>
  <files>Prunr/Views/MainView.swift</files>
  <action>Apply systematic spacing and typography improvements:

1. Typography hierarchy:
   - Section headers: .headline with .secondary foreground color
   - Picker labels: .caption.bold with .tertiary color
   - List content: .body (current)
   - Empty states: .title3 for headline, .body for subheadline

2. Spacing system (use consistent values):
   - Section spacing: 24pt between major sections
   - Element spacing: 8-12pt within groups
   - Horizontal padding: 16pt standard
   - Vertical padding: 12pt standard

3. Visual improvements:
   - Add subtle divider between snapshot section and list
   - Use .listStyle(.insetAlternated) for better row differentiation
   - Add .frame(minWidth:, idealWidth:) for proper sizing
   - Improve empty state with more engaging icon and better copy

4. Error/progress banner refinements:
   - Make banners more prominent with better background opacity
   - Add .clipShape(.roundedRectangle) with cornerRadius
   - Improve typography for better readability

DO NOT:
- Don't add animations yet (deferred to plan 05-03)
- Don't change the core functionality
- Keep color coding (green=good growth, red=bad growth, etc.) consistent</action>
  <verify>Build succeeds. Window layout looks organized with consistent spacing throughout. All sections properly aligned.</verify>
  <done>Typography hierarchy is clear. Spacing is consistent. Window has professional, organized appearance.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Build succeeds: `xcodebuild -project Prunr.xcodeproj -scheme Prunr build`
- [ ] Snapshot pickers work: can select different snapshots
- [ ] Summary stats are accurate: match sum of delta changes
- [ ] Menu shortcuts still work: Cmd+R scans, Cmd+Shift+R refreshes
- [ ] Visual hierarchy is clear: sections are distinguishable
</verification>

<success_criteria>

- MainView has clearly defined sections with proper visual separation
- Snapshot selector uses proper container with labeled pickers and quick action buttons
- Summary header displays accurate growth/shrinkage/net change stats
- Typography hierarchy is clear and consistent
- Spacing follows a consistent system
- All existing functionality preserved
</success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-redesign/05-01-SUMMARY.md`:

# Phase 5 Plan 01: Layout & Visual Hierarchy Summary

**Main window redesigned with clear sections, summary statistics, and consistent spacing system**

## Accomplishments

- Snapshot selector section with labeled pickers and quick action buttons
- Summary stats header showing net change, growth, shrinkage, and item count
- Consistent spacing and typography system applied throughout
- Improved empty states with better copy and iconography

## Files Created/Modified

- `Prunr/Views/MainView.swift` - Redesigned layout with sections
- `Prunr/ViewModels/MainViewModel.swift` - Added computed stats properties
- `Prunr/Views/SummaryHeaderView.swift` - New summary stats component

## Decisions Made

[Document any UI design decisions made during implementation]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Step

Ready for 05-02-PLAN.md (Delta List Redesign)
</output>
