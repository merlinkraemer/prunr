---
phase: 05-frontend-redesign
plan: 06
type: execute
---

<objective>
Implement smart grouping to detect and link scattered paths from the same logical source (Homebrew, npm, apps).

Purpose: Homebrew, npm, and apps scatter files across multiple directories. Smart grouping links these together so users see "Homebrew: 12GB" instead of 50 tiny entries.

Output: Extended DeltaCategory with smart grouping patterns for Homebrew, npm, Xcode, and apps; grouped display in CategoryColumn.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/05-frontend-redesign/05-CONTEXT.md
@.planning/phases/05-frontend-redesign/05-01-SUMMARY.md
@.planning/phases/05-frontend-redesign/05-03-SUMMARY.md
@Prunr/Models/DeltaCategory.swift
@Prunr/Views/CategoryColumn.swift
@Prunr/Views/ItemColumn.swift
@Prunr/ViewModels/MainViewModel.swift

**Tech stack available:** SwiftUI, GRDB v7+, @Observable @MainActor pattern

**Established patterns:**
- DeltaCategory enum with pattern-based categorization
- CategoryColumn displays categories with item counts
- ItemColumn shows filtered items by category

**Constraining decisions:**
- 05-01: DeltaCategory has 6 categories (apps, packages, containers, caches, developer, other)
- 05-01: Pattern matching via `categorize(path:)` static method
- 05-03: CategoryColumn shows "All" + individual categories with counts

**Issues being addressed:** None (feature enhancement)

**Smart grouping patterns from CONTEXT.md:**
- Homebrew: `/usr/local/Cellar/*`, `/usr/local/var/*`, `~/Library/Caches/Homebrew/*`
- npm: `node_modules/` folders (across multiple projects)
- Apps: `.app` bundles with `~/Library/Containers/*` and `~/Library/Caches/*` grouped together
- Xcode: `DerivedData/`, `.build/`, Archives

**Target UX from CONTEXT.md:**
```
│ ▸ Desktop                 +45GB ████│
│ ▸ Homebrew: postgresql    +12GB ███ │
│ ▸ tmp-videos-folder       +25GB 🆕  │
│ ▸ node_modules            +8GB  ██  │
│ └─ ~/Library/Caches       +3GB  █   │
```
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend DeltaCategory with smart grouping patterns</name>
  <files>Prunr/Models/DeltaCategory.swift</files>
  <action>Extend DeltaCategory to support smart grouping:

1. Add a new case `.grouped` to the enum (or extend existing categories)
2. Add a `GroupedCategory` enum with cases: `.homebrew, .npm, .xcode, .appBundles`
3. For each grouped category, define comprehensive path patterns:
   - Homebrew: `/usr/local/Cellar`, `/usr/local/var`, `/opt/homebrew`, `~/Library/Caches/Homebrew`
   - npm: Any `node_modules` folder (use path component detection)
   - Xcode: `DerivedData`, `.build`, `Archives` (in `~/Library/Developer/Xcode`)
   - App bundles: `.app` paths + their corresponding `~/Library/Containers` and `~/Library/Caches`

4. Add `detectGroupedCategory(path:) -> GroupedCategory?` static method
5. For npm specifically: detect if path contains `node_modules` at any level
6. For Homebrew: detect if path is under any Homebrew prefix

Do NOT modify the existing 6-category system - smart grouping is an additional layer that aggregates items already categorized.
</action>
  <verify>Build succeeds, detectGroupedCategory returns appropriate GroupedCategory for known patterns</verify>
  <done>DeltaCategory extended with grouped category detection</done>
</task>

<task type="auto">
  <name>Task 2: Create GroupedDelta model for aggregating related items</name>
  <files>Prunr/Models/GroupedDelta.swift</files>
  <action>Create a new model to represent grouped delta items:

1. `GroupedDelta` struct with:
   - `category: GroupedCategory`
   - `displayName: String` (e.g., "Homebrew", "node_modules", "Xcode")
   - `items: [Delta]` (all individual deltas in this group)
   - Computed property `totalChangeBytes: Int64` (sum of all items)
   - Computed property `totalOldSize: Int64?` (sum of old sizes)
   - Computed property `totalNewSize: Int64?` (sum of new sizes)

2. Add a static method `groupDeltas(_ deltas: [Delta]) -> [GroupedDelta]` that:
   - Filters deltas where `detectGroupedCategory` returns non-nil
   - Groups by GroupedCategory
   - For Homebrew, optionally subgroup by formula name (e.g., "postgresql", "node")
   - Returns array of GroupedDelta sorted by totalChangeBytes (descending)

3. Make `GroupedDelta` conform to `Identifiable` (id = category.rawValue)

This enables showing aggregated items in the UI while preserving individual item details.
</action>
  <verify>GroupedDelta model compiles, groupDeltas produces aggregated results</verify>
  <done>GroupedDelta model created with aggregation logic</done>
</task>

<task type="auto">
  <name>Task 3: Add GroupedCategory to MainViewModel</name>
  <files>Prunr/ViewModels/MainViewModel.swift</files>
  <action>Extend MainViewModel with grouped delta support:

1. Add `@Published var groupedDeltas: [GroupedDelta] = []`
2. Add a method `updateGroupedDeltas()` that calls `GroupedDelta.groupDeltas(deltas)`
3. Call `updateGroupedDeltas()` after delta calculation is complete
4. Ensure grouped deltas are recalculated when comparison interval changes

This makes grouped deltas available to the UI layer.
</action>
  <verify>Build succeeds, groupedDeltas updates when deltas change</verify>
  <done>MainViewModel supports grouped deltas</done>
</task>

<task type="auto">
  <name>Task 4: Create GroupedItemRow component for grouped display</name>
  <files>Prunr/Views/GroupedItemRow.swift</files>
  <action>Create a reusable row component for displaying grouped items:

1. Input: `GroupedDelta` with total change and item count
2. Display format:
   - Primary: Category icon + display name (e.g., "Homebrew", "node_modules")
   - Secondary: Total change (e.g., "+12.5 GB") in green/red
   - Badge: Item count (e.g., "23 items") in secondary color
   - Growth bar: Reuse GrowthBarView from 05-05
3. Allow drill-down: When clicked, expand to show individual items
4. Use disclosure indicator (▸) when collapsed, (▾) when expanded

This matches the target UX where grouped items show aggregated totals with optional expansion.
</action>
  <verify>GroupedItemRow renders with icon, name, total change, and item count badge</verify>
  <done>GroupedItemRow component created</done>
</task>

<task type="auto">
  <name>Task 5: Integrate grouped display into CategoryColumn</name>
  <files>Prunr/Views/CategoryColumn.swift</files>
  <action>Update CategoryColumn to display grouped categories:

1. Accept both `[Delta]` and `[GroupedDelta]` as inputs (or add groupedDeltas parameter)
2. Add a "Grouped" section at the top of the category list
3. Display grouped categories using `GroupedItemRow` (or simplified category row)
4. Show total change for each grouped category
5. When a grouped category is selected, filter ItemColumn to show only items in that group

This gives users immediate visibility into the biggest space hogs by logical source.
</action>
  <verify>CategoryColumn shows grouped categories with aggregated changes</verify>
  <done>CategoryColumn integrates grouped display</done>
</task>

<task type="auto">
  <name>Task 6: Add GroupedDelta and GroupedItemRow to Xcode project</name>
  <files>Prunr.xcodeproj/project.pbxproj</files>
  <action>Add `Prunr/Models/GroupedDelta.swift` and `Prunr/Views/GroupedItemRow.swift` to the Xcode project under their respective groups.
</action>
  <verify>Build succeeds without errors</verify>
  <done>New files included in build</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Prunr.xcodeproj -scheme Prunr build` succeeds
- [ ] GroupedDelta model aggregates related items correctly
- [ ] CategoryColumn displays grouped categories (Homebrew, npm, Xcode, apps)
- [ ] Grouped items show total change and item count
- [ ] Selecting a grouped category filters ItemColumn to show constituent items
</verification>

<success_criteria>

- GroupedDelta model aggregates scattered paths by logical source
- CategoryColumn shows "Grouped" section with Homebrew, npm, Xcode, apps
- Grouped items display aggregated total change and item count
- Drill-down reveals individual items within each group
- Existing 6-category system remains functional
  </success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-redesign/05-06-SUMMARY.md`:

# Phase 5 Plan 06: Smart Grouping Summary

**Implemented smart grouping for scattered paths from Homebrew, npm, Xcode, and app bundles.**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `path/to/file.swift` - Description
- `path/to/another.swift` - Description

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

Phase 5 complete. Growth journal UX delivered:
- Sidebar path selection with scan + compare workflow ✓
- "Compare Since X ago" dropdown ✓
- Three-column view with category navigation ✓
- Visual growth bars ✓
- Smart grouping for scattered paths ✓

Ready for Phase 6: Cleanup Actions (future work).
</output>
