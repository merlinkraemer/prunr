---
phase: 05-frontend-redesign
plan: 04
type: execute
domain: swiftui-macos
---

<objective>
Implement simplified "Compare Since" dropdown and integrate sidebar scanning with column view.

Purpose: Replace the dual-snapshot picker with a simpler "Compare Since X ago" dropdown and connect sidebar path selection to scan/compare workflow.
Output: ComparisonPicker component, updated MainViewModel with "since" comparison logic, end-to-end integration.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-frontend-redesign/05-RESEARCH.md
@.planning/phases/05-frontend-redesign/05-CONTEXT.md

# Auto-selected based on dependency graph:
@.planning/phases/02-scanner-storage/02-02-SUMMARY.md
@.planning/phases/03-delta-engine/03-01-SUMMARY.md
@.planning/phases/04-ui-polish/04-01-SUMMARY.md
@.planning/phases/05-frontend-redesign/05-02-SUMMARY.md
@.planning/phases/05-frontend-redesign/05-03-SUMMARY.md

# Key files from frontmatter:
@Prunr/ViewModels/MainViewModel.swift
@Prunr/Views/MainView.swift
@Prunr/Views/RootView.swift
@Prunr/Database/DatabaseManager.swift

**Tech stack available:** @AppStorage, @Observable @MainActor, ScanService/DeltaService actors
**Established patterns:**
- @Observable @MainActor for UI state (Phase 04)
- ScanService.shared.scan() for scanning (Phase 02)
- DeltaService.shared.compare() for delta calculation (Phase 03)
- FocusedValue for menu commands (Phase 04-02)

**Constraining decisions:**
- CONTEXT.md: "Since X ago" comparison always vs current state (no 2-snapshot selection)
- CONTEXT.md: Simplified top bar with Rescan + comparison picker
- RESEARCH.md: Auto-scan if no recent scan exists (< 1 hour old), otherwise use cached

**Issues being addressed:**
- ISS-001: Scan fails with "failed to create snapshot" - may resurface during integration
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create ComparisonPicker with time presets</name>
  <files>Prunr/Views/ComparisonPicker.swift</files>
  <action>Create ComparisonPicker struct with @AppStorage("comparisonInterval") private var selectedInterval: TimeInterval = 86400 (24h default). Define presets: 1h (3600s), 12h (43200s), 24h (86400s), 3d (259200s), 7d (604800s). Add Divider() and "Custom..." option (-1). Use Picker("Compare Since", selection: $selectedInterval) with .pickerStyle(.menu) for native macOS dropdown. Add computed property to format display text. This replaces the dual-snapshot pickers from Phase 04.</action>
  <verify>swiftc -parse Prunr/Views/ComparisonPicker.swift succeeds</verify>
  <done>ComparisonPicker with 5 presets + custom option, @AppStorage persistence</done>
</task>

<task type="auto">
  <name>Task 2: Extend MainViewModel with "since" comparison logic</name>
  <files>Prunr/ViewModels/MainViewModel.swift</files>
  <action>Add comparisonInterval: TimeInterval property with @AppStorage backing. Replace selectedBeforeSnapshot/selectedAfterSnapshot with selectedPath: TrackedPath?. Add compareSince() method that: 1) Finds snapshot from selectedPath with createdAt >= (Date.now - comparisonInterval), 2) Scans current state if no recent snapshot (< 5min old), 3) Calls DeltaService with historical snapshot ID and current snapshot ID. Add scanCurrentState(path: URL) method. Update autoSelectSnapshots() to work with new "since" logic. Keep scan progress and error handling from Phase 04.</action>
  <verify>swiftc -parse Prunr/ViewModels/MainViewModel.swift compiles with existing services</verify>
  <done>MainViewModel with "since X ago" comparison, auto-scan on demand</done>
</task>

<task type="auto">
  <name>Task 3: Integrate sidebar selection with scanning and column view</name>
  <files>Prunr/Views/RootView.swift, Prunr/Views/MainView.swift</files>
  <action>Update RootView to pass selectedPath binding to MainView. In MainView, remove snapshot pickers UI, replace with toolbar containing: Rescan button (triggers scanCurrentState for selected path) and ComparisonPicker. Add .onChange(of: selectedPath) to trigger compareSince() when sidebar selection changes. Add .onChange(of: comparisonInterval) to re-run compareSince() with new interval. Replace deltasList with ColumnContainerView, passing deltas and selection bindings. Update error/scanning banners to work with new flow. This completes the Finder-style redesign.</action>
  <verify>xcodebuild -project Prunr.xcodeproj -scheme Prunr build succeeds</verify>
  <done>Full integration: sidebar selection → scan → compare → column view display</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild` succeeds without errors
- [ ] ComparisonPicker shows all 5 presets + custom
- [ ] Selecting sidebar path triggers scan + comparison
- [ ] Changing comparison interval re-runs comparison
- [ ] Three-column view displays filtered by selected category
- [ ] Rescan button scans selected path
</verification>

<success_criteria>

- "Compare Since" dropdown replaces snapshot pickers
- Selecting sidebar path auto-scans and compares
- Column view shows categorized delta results
- All UI elements functional from sidebar to detail view
- Phase 5 complete: Finder-style redesign ready
  </success_criteria>

<output>
After completion, create `.planning/phases/05-frontend-redesign/05-04-SUMMARY.md`:

# Phase 5 Plan 04: Simplified Comparison Summary

**[Substantive one-liner - what shipped]**

## Accomplishments

- [Key outcome 1]
- [Key outcome 2]

## Files Created/Modified

- `Prunr/Views/ComparisonPicker.swift` - Description
- `Prunr/ViewModels/MainViewModel.swift` - Updated with "since" logic
- `Prunr/Views/RootView.swift` - Integration with sidebar
- `Prunr/Views/MainView.swift` - Replaced pickers with column view

## Decisions Made

[Key decisions and rationale, or "None"]

## Issues Encountered

[Problems and resolutions, or "None"]

## Next Phase Readiness

Phase 5 complete. Finder-style redesign delivered.
Ready for Phase 6: Cleanup Actions (future work).
</output>
