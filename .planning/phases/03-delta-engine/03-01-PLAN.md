---
phase: 03-delta-engine
plan: 01
type: execute
---

<objective>
Compare two snapshots to calculate what grew or shrank between them.

Purpose: Enable users to see which folders consumed or freed storage between two points in time.
Output: Delta model and SQL-based delta calculation that returns sorted change data.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/templates/phase-prompt.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Research findings (MANDATORY - informs implementation):
@.planning/phases/03-delta-engine/03-RESEARCH.md

# Prior phases with relevant context:
@.planning/phases/01-foundation/01-01-SUMMARY.md
@.planning/phases/02-scanner-storage/02-01-SUMMARY.md
@.planning/phases/02-scanner-storage/02-02-SUMMARY.md

# Key files from prior phases:
@Prunr/Models/Snapshot.swift
@Prunr/Models/SnapshotEntry.swift
@Prunr/Database/DatabaseManager.swift
@Prunr/Services/ScanService.swift

**Tech stack available:**
- GRDB.swift 7.0+ with FetchableRecord/PersistableRecord patterns
- DatabaseManager singleton with async CRUD methods
- SnapshotEntry with path and sizeBytes fields

**Established patterns:**
- DatabaseManager.shared for all database operations
- Models conform to Codable and Identifiable
- Async/await for database operations
- Actor isolation for thread-safe services

**Constraining decisions:**
- Phase 01: Singleton DatabaseManager pattern for centralized DB access
- Phase 02: Batch size 2000 for performance
- Phase 02: Actor isolation for thread-safe state

**Research-informed decisions (from RESEARCH.md):**
- Use SQL FULL OUTER JOIN for delta calculation (not Swift dictionary merging)
- Performance: 50-100ms for 50k entries, low memory (~5MB)
- Use COLLATE NOCASE for case-insensitive path comparison on macOS
- Use COALESCE to handle NULL values for new/deleted files
- Filter unchanged items via SQL HAVING clause (before fetching)
- Sort via SQL ORDER BY (leverages indexes, supports LIMIT)
- Use path-based ID for SwiftUI Identifiable (not UUID, stable across launches)

**Issues being addressed:** None
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Delta model with FetchableRecord conformance</name>
  <files>Prunr/Models/Delta.swift</files>
  <action>Create a Delta struct with:
  - id: String (STABLE ID - use path itself, not UUID or hashValue)
  - path: String (the folder/file path)
  - oldSizeBytes: Int64? (size in previous snapshot, nil if new)
  - newSizeBytes: Int64? (size in current snapshot, nil if deleted)
  - changeBytes: Int64 (calculated property: (newSize ?? 0) - (oldSize ?? 0))

  Computed properties:
  - percentChange: Double? (changeBytes / oldSizeBytes * 100, nil if oldSize was 0 or nil)
  - isGrowth: Bool (changeBytes > 0)
  - isShrinkage: Bool (changeBytes < 0)

  Conform to:
  - Codable (for serialization)
  - Identifiable (id = path - STABLE across app launches)
  - Hashable (for Set operations)
  - FetchableRecord (for GRDB SQL fetching - init(row:) initializer)

  CRITICAL: DO NOT use UUID or hashValue for id - must be path for stable SwiftUI identity.
  DO NOT add display formatting logic - keep this as pure data model.</action>
  <verify>File compiles with swiftc -parse, id property returns path, FetchableRecord init(row:) exists</verify>
  <done>Delta.swift exists with all required properties, conforms to Codable/Identifiable/Hashable/FetchableRecord</done>
</task>

<task type="auto">
  <name>Task 2: Add SQL-based delta calculation to DatabaseManager</name>
  <files>Prunr/Database/DatabaseManager.swift</files>
  <action>Extend DatabaseManager with calculateDeltas(beforeId:afterId:) method:

  1. Add async method that returns [Delta]
  2. Use SQL FULL OUTER JOIN to compare snapshotEntry tables
  3. Key SQL clauses:
     - COALESCE for path selection: COALESCE(new.path, old.path)
     - COLLATE NOCASE for case-insensitive join on macOS
     - COALESCE for NULL handling in change calculation
     - HAVING changeBytes != 0 to filter unchanged items
     - ORDER BY ABS(changeBytes) DESC for sorting
  4. Use Delta.fetchAll(db, sql:arguments:) for GRDB integration

  SQL structure:
  ```sql
  SELECT
      COALESCE(new.path, old.path) as path,
      old.sizeBytes as oldSizeBytes,
      new.sizeBytes as newSizeBytes,
      COALESCE(new.sizeBytes, 0) - COALESCE(old.sizeBytes, 0) as changeBytes
  FROM snapshotEntry old
  FULL OUTER JOIN snapshotEntry new
      ON old.path = new.path COLLATE NOCASE
      AND old.snapshotId = ?
      AND new.snapshotId = ?
  WHERE old.snapshotId = ? OR new.snapshotId = ?
  HAVING changeBytes != 0
  ORDER BY ABS(changeBytes) DESC
  ```

  DO NOT load both snapshots into memory - let SQL do the work.</action>
  <verify>swiftc -parse succeeds on DatabaseManager.swift, FULL OUTER JOIN query present, COLLATE NOCASE used, HAVING filters unchanged</verify>
  <done>DatabaseManager.calculateDeltas() method exists with SQL FULL OUTER JOIN implementation</done>
</task>

<task type="auto">
  <name>Task 3: Create DeltaService orchestrator actor</name>
  <files>Prunr/Services/DeltaService.swift</files>
  <action>Create DeltaService actor with:
  - static let shared = DeltaService()
  - private init() {}
  - func compare(beforeId: Int64, afterId: Int64) async throws -> [Delta]

  The compare() method should:
  1. Delegate directly to DatabaseManager.shared.calculateDeltas()
  2. Return the sorted results (already sorted by SQL)

  Use actor isolation (not class) for thread safety consistent with Phase 02 patterns.
  DO NOT implement comparison logic - SQL handles it in DatabaseManager.
  DO NOT access DatabasePool directly - use DatabaseManager methods only.</action>
  <verify>swiftc -parse succeeds on DeltaService.swift, actor isolation used, no direct database access, delegates to DatabaseManager</verify>
  <done>DeltaService actor with compare() method that delegates to DatabaseManager.calculateDeltas()</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] swiftc -parse Prunr/Models/Delta.swift succeeds
- [ ] swiftc -parse Prunr/Database/DatabaseManager.swift succeeds
- [ ] swiftc -parse Prunr/Services/DeltaService.swift succeeds
- [ ] Delta.id returns path (stable ID, not UUID/hashValue)
- [ ] Delta conforms to FetchableRecord with init(row:)
- [ ] DatabaseManager.calculateDeltas() uses FULL OUTER JOIN
- [ ] SQL uses COLLATE NOCASE for case-insensitive comparison
- [ ] SQL uses COALESCE for NULL handling (new/deleted files)
- [ ] SQL uses HAVING to filter unchanged items
- [ ] SQL uses ORDER BY ABS(changeBytes) DESC
- [ ] DeltaService is actor (not class)
- [ ] DeltaService delegates to DatabaseManager (no direct db access)
</verification>

<success_criteria>

- Delta model exists with path-based stable ID, all required properties, FetchableRecord conformance
- DatabaseManager.calculateDeltas() implements SQL FULL OUTER JOIN with COLLATE NOCASE
- DeltaService actor delegates to DatabaseManager
- Comparison handles new items (oldSize = nil), deleted items (newSize = nil), and changed items
- Results filtered and sorted at SQL level (HAVING + ORDER BY)
- No snapshots loaded into memory during comparison
  </success_criteria>

<output>
After completion, create `.planning/phases/03-delta-engine/03-01-SUMMARY.md`:

# Phase 3 Plan 01: Delta Engine Summary

**SQL-based delta calculation using FULL OUTER JOIN for efficient snapshot comparison**

## Accomplishments
- Delta model with path-based stable ID, FetchableRecord conformance
- DatabaseManager.calculateDeltas() with SQL FULL OUTER JOIN
- Case-insensitive path comparison via COLLATE NOCASE
- Proper NULL handling for new/deleted files via COALESCE
- Filtering and sorting at SQL level for performance
- DeltaService actor following Phase 02 patterns

## Files Created/Modified
- `Prunr/Models/Delta.swift` - Delta value type with FetchableRecord
- `Prunr/Database/DatabaseManager.swift` - Added calculateDeltas() method
- `Prunr/Services/DeltaService.swift` - Delta service actor

## Decisions Made
- SQL FULL OUTER JOIN vs Swift dictionary merging (better performance, lower memory)
- Path-based ID for SwiftUI stability (not UUID/hashValue)
- COLLATE NOCASE for macOS case-insensitive filesystem
- Filter/sort at SQL level (not after fetching)
- Actor isolation consistent with Phase 02 patterns

## Issues Encountered
None

## Next Step
Ready for Phase 04-ui-polish (UI implementation using Delta results)
</output>
