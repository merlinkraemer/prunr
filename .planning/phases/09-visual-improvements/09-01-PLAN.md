---
phase: 09-visual-improvements
plan: 01
type: execute
---

<objective>
Implement monospace fonts for all numeric values to prevent visual jumps when numbers change length.

Purpose: Ensure consistent character width for numeric displays, eliminating layout shifts that occur when numbers with different digit counts are displayed.
Output: All numeric displays use SF Mono font with proper spacing and alignment.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Prunr/Views/MenuBarView.swift
@Prunr/Views/CategoryGrowthListView.swift
@Prunr/Views/SettingsView.swift
@Prunr/Views/DriveBarView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update MenuBarView numeric displays with SF Mono</name>
  <files>Prunr/Views/MenuBarView.swift</files>
  <action>Add .monospaced() modifier to all Text views displaying numeric values (file sizes, percentages, counts) in MenuBarView. Apply to:
 1. formattedBytes() method results (line 590-601)
 2. Progress percentage text (line 85)
 3. Path size badges (lines 299, 355)
 4. Scan progress indicators
 5. File count text (line 101)
 6. Category sizes in drill-down header (line 234)

Use .font(.system(.body, design: .monospaced)) to match system aesthetics.</action>
  <verify>Build project successfully, verify all numeric displays maintain consistent width when values change (e.g., 1 MB vs 999 MB)</verify>
  <done>All numeric text in MenuBarView uses monospace font, no visual jumps when numbers change</done>
</task>

<task type="auto">
  <name>Task 2: Update CategoryGrowthListView numeric displays</name>
  <files>Prunr/Views/CategoryGrowthListView.swift</files>
  <action>Add .monospaced() modifier to numeric displays in CategoryGrowthListView and its nested row views. Apply to:
 1. Growth item sizes
 2. Item counts
 3. Category totals
 4. File/folder sizes in detail view

Since this file shows row components that may be in separate files, check for imports of those files and update them as well.</action>
  <verify>Build project successfully, verify category list and detail view maintain consistent numeric spacing</verify>
  <done>All numeric displays in CategoryGrowthListView use monospace font</done>
</task>

<task type="auto">
  <name>Task 3: Update SettingsView numeric displays</name>
  <files>Prunr/Views/SettingsView.swift</files>
  <action>Add .monospaced() modifier to numeric displays in SettingsView. Apply to:
 1. Drill-down threshold percentage (line 69)
 2. Any other numeric values in settings tabs

Focus on the General and Paths tabs where numeric values are displayed.</action>
  <verify>Build project successfully, verify settings displays maintain consistent numeric spacing</verify>
  <done>All numeric displays in SettingsView use monospace font</done>
</task>

<task type="auto">
  <name>Task 4: Update DriveBarView numeric displays</name>
  <files>Prunr/Views/DriveBarView.swift</files>
  <action>Add .monospaced() modifier to numeric displays in DriveBarView. Apply to:
 1. File size labels
 2. Percentage displays
 3. Any other numeric components in the drive bar

This is the storage capacity visualization at the top of the popover.</action>
  <verify>Build project successfully, verify drive bar maintains consistent spacing when values change</verify>
  <done>All numeric displays in DriveBarView use monospace font</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Build project with `xcodebuild -project Prunr.xcodeproj -scheme Prunr -sdk macosx build` succeeds
- [ ] MenuBarView numeric displays show consistent spacing (test with 1 MB vs 999 MB)
- [ ] CategoryGrowthListView maintains column alignment with varying numbers
- [ ] SettingsView displays consistent numeric formatting
- [ ] DriveBarView shows consistent spacing when free space changes
</verification>

<success_criteria>

- All numeric displays use monospace fonts (SF Mono)
- No visual jumps when numbers change length
- Column alignment remains consistent regardless of content
- Builds successfully without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/09-visual-improvements/09-01-SUMMARY.md`
</output>