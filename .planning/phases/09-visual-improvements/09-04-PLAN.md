---
phase: 09-visual-improvements
plan: 04
type: execute
---

<objective>
Standardize spacing across all views to create a cohesive interface.

Purpose: Eliminate inconsistent padding, margins, and element spacing throughout the application, creating a polished, professional appearance that follows macOS design guidelines.
Output: Consistent spacing system applied to all views with proper visual rhythm.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Prunr/Views/MenuBarView.swift
@Prunr/Views/CategoryGrowthListView.swift
@Prunr/Views/SettingsView.swift
@Prunr/Views/DriveBarView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Define spacing constants for the application</name>
  <files>Prunr/Views/MenuBarView.swift</files>
  <action>Add a spacing constants section at the top of MenuBarView to establish a consistent spacing system:

```swift
// MARK: - Spacing Constants
private enum Spacing {
    static let tiny: CGFloat = 4
    static let small: CGFloat = 8
    static let medium: CGFloat = 12
    static let large: CGFloat = 16
    static let xLarge: CGFloat = 20
    static let xxLarge: CGFloat = 24
    static xxxLarge: CGFloat = 32
}

private enum Padding {
    static let horizontal: CGFloat = 16
    static let vertical: CGFloat = 12
    static let smallHorizontal: CGFloat = 8
    static let smallVertical: CGFloat = 6
}
```

Replace all hardcoded spacing values with these constants throughout the view.</action>
  <verify>Spacing constants defined and applied consistently</verify>
  <done>Application-wide spacing constants established</done>
</task>

<task type="auto">
  <name>Task 2: Standardize spacing in MenuBarView</name>
  <files>Prunr/Views/MenuBarView.swift</files>
  <action>Update all spacing in MenuBarView using the new constants:

1. Replace .padding(.horizontal, 16) with .padding(.horizontal, Padding.horizontal)
2. Replace .padding(.vertical, 12) with .padding(.vertical, Padding.vertical)
3. Update all hardcoded values in HStack/VStack spacing
4. Standardize button padding and margins
5. Apply consistent spacing to section dividers
6. Ensure loading overlay uses consistent spacing

Focus on the main layout areas: drive bar section, header section, category list, and footer buttons.</action>
  <verify>Build project successfully, verify all MenuBarView spacing uses consistent constants</verify>
  <done>MenuBarView spacing standardized using application constants</done>
</task>

<task type="auto">
  <name>Task 3: Standardize spacing in CategoryGrowthListView</name>
  <files>Prunr/Views/CategoryGrowthListView.swift</files>
  <files>Prunr/Views/CategoryListRow.swift</files>
  <files>Prunr/Views/NestedBigItemRow.swift</files>
  <files>Prunr/Views/FolderHeaderRow.swift</files>
  <files>Prunr/Views/ItemRow.swift</files>
  <action>Update spacing in CategoryGrowthListView and all its row components:

1. Apply the same spacing constants system
2. Standardize padding and margins in all row views
3. Ensure consistent vertical spacing between list items
4. Apply consistent horizontal indentation for hierarchy
5. Standardize button spacing and padding
6. Ensure proper spacing around badges and indicators

Update CategoryGrowthListView.swift first, then each row component file.</action>
  <verify>Build project successfully, verify all list view spacing is consistent</verify>
  <done>CategoryGrowthListView and row components spacing standardized</done>
</task>

<task type="auto">
  <name>Task 4: Standardize spacing in SettingsView</title>
  <files>Prunr/Views/SettingsView.swift</files>
  <action>Update spacing in SettingsView to match the application standards:

1. Apply consistent tab spacing and padding
2. Standardize GroupBox spacing and padding
3. Update Toggle and Slider spacing
4. Ensure consistent form element spacing
5. Apply consistent button spacing
6. Standardize section divider spacing

Focus on all five tabs (General, Paths, Boundaries, Debug, About) to maintain consistency across settings.</action>
  <verify>Build project successfully, verify SettingsView spacing matches application standards</verify>
  <done>SettingsView spacing standardized to match application-wide constants</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Build project successfully without errors
- [ ] All views use consistent spacing throughout the application
- [ ] Visual rhythm is maintained across different sections
- [ ] No hardcoded spacing values remain in UI components
- [ ] Spacing follows macOS design guidelines (6pt radius, proper margins)
- [ ] Interface appears cohesive and polished
</verification>

<success_criteria>

- All views use consistent spacing system
- No hardcoded spacing values in UI code
- Visual rhythm is maintained across the application
- Spacing follows macOS design patterns
- Interface appears cohesive and professional
  </success_criteria>

<output>
After completion, create `.planning/phases/09-visual-improvements/09-04-SUMMARY.md`
</output>