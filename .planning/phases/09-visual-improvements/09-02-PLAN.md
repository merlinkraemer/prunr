---
phase: 09-visual-improvements
plan: 02
type: execute
---

<objective>
Implement fixed-width columns for item counts and size differences in both main view and drill down view.

Purpose: Create stable column layout that prevents reflow when content changes, improving readability and professionalism of the data display.
Output: Static columns with consistent widths matching modern macOS design patterns (Finder-style).
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Prunr/Views/CategoryGrowthListView.swift
@Prunr/Views/CategoryListRow.swift
@Prunr/Views/NestedBigItemRow.swift
@Prunr/Views/FolderHeaderRow.swift
@Prunr/Views/ItemRow.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create static column sizing helpers</name>
  <files>Prunr/Views/CategoryGrowthListView.swift</files>
  <action>Add static column width calculations at the top of CategoryGrowthListView:

```swift
// Static column widths
private let countColumnWidth: CGFloat = 50  // Fits up to 9999 items
private let sizeColumnWidth: CGFloat = 80   // Fits up to 999.9 GB
private let nameColumnWidth: CGFloat = 120 // Flexible remaining space

private func columnWidths(for viewWidth: CGFloat) -> (count: CGFloat, size: CGFloat, name: CGFloat) {
    return (
        count: countColumnWidth,
        size: sizeColumnWidth,
        name: viewWidth - countColumnWidth - sizeColumnWidth - 40 // 40pt for padding
    )
}
```

Add view modifier to apply fixed sizing where needed.</action>
  <verify>Build project successfully, verify static column width constants are properly defined</verify>
  <done>Static column width helpers implemented with proper calculations</done>
</task>

<task type="auto">
  <name>Task 2: Update CategoryListRow with static columns</name>
  <files>Prunr/Views/CategoryListRow.swift</files>
  <action>Modify CategoryListRow to use static column widths. Apply .fixedSize() modifier to numeric displays and HStack with explicit widths:

1. Replace flexible HStack with explicit column structure
2. Apply .fixedSize() to Text views showing item counts and sizes
3. Use maximum expected widths for count and size columns
4. Ensure name column takes remaining space

Update the layout to match modern macOS list patterns with consistent spacing.</action>
  <verify>Build project successfully, verify category rows maintain consistent alignment</verify>
  <done>CategoryListRow uses static column sizing with proper alignment</done>
</task>

<task type="auto">
  <name>Task 3: Update nested item rows with static columns</name>
  <files>Prunr/Views/NestedBigItemRow.swift</files>
  <action>Update NestedBigItemRow to use static column widths:

1. Apply .fixedSize() to size and count displays
2. Use consistent indentation and spacing
3. Ensure alignment with parent category rows
4. Maintain proper hierarchy visual cues

This affects the big file items shown within expanded categories.</action>
  <verify>Build project successfully, verify nested items align properly with parent columns</verify>
  <done>NestedBigItemRow uses static columns with proper hierarchy alignment</done>
</task>

<task type="auto">
  <name>Task 4: Update drill-down view rows with static columns</name>
  <files>Prunr/Views/FolderHeaderRow.swift</files>
  <files>Prunr/Views/ItemRow.swift</files>
  <action>Update FolderHeaderRow and ItemRow in the drill-down detail view to use static column widths:

1. Apply .fixedSize() to all numeric displays in both files
2. Use consistent width calculations across detail view
3. Ensure proper alignment between folder headers and item rows
4. Maintain readable spacing for path names and sizes

These files handle the expanded view when drilling down into a category.</action>
  <verify>Build project successfully, verify drill-down view maintains column alignment</verify>
  <done>Drill-down view rows use static columns with proper alignment</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Build project successfully without errors
- [ ] Main category view maintains column alignment with varying item counts
- [ ] Drill-down view shows consistent column spacing
- [ ] No visual reflow when numbers change length (tested with 1 item vs 9999 items)
- [ ] Hierarchy indentation remains consistent across all views
</verification>

<success_criteria>

- All list views use static column widths
- Column alignment remains consistent regardless of content
- No visual reflow when data changes
- Proper hierarchy visual cues maintained
- Builds successfully without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/09-visual-improvements/09-02-SUMMARY.md`
</output>