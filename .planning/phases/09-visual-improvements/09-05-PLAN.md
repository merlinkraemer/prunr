---
phase: 09-visual-improvements
plan: 05
type: execute
---

<objective>
Redesign footer buttons to use icon-only design with macOS 26 aesthetics, positioned horizontally in lower right corner.

Purpose: Create a cleaner, more subtle footer that doesn't compete with the main content while maintaining accessibility and functionality.
Output: Simplified icon-only buttons with proper hover states and visual feedback.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@Prunr/Views/MenuBarView.swift
</context>

<tasks>

<task type="auto">
  <name>Task 1: Analyze current footer button structure</name>
  <files>Prunr/Views/MenuBarView.swift</files>
  <action>Study the current footerButtons implementation (lines 460-581). Analyze:

1. Current button layout and styling
2. Hover state implementation
3. Button dimensions and spacing
4. Text + icon combination usage
5. Visual weight and prominence

Define a new icon-only design that reduces visual weight while maintaining clear functionality and accessibility.</action>
  <verify>Current footer structure analyzed and new design specifications defined</verify>
  <done>Current footer buttons analyzed and icon-only design planned</done>
</task>

<task type="auto">
  <name>Task 2: Implement icon-only button design</name>
  <files>Prunr/Views/MenuBarView.swift</files>
  <action>Redesign the footerButtons implementation to use icon-only design:

1. Remove text labels, use only SF Symbols
2. Apply 28pt x 28pt button size (macOS standard)
3. Use proper SF Symbol sizes (16pt for icons)
4. Apply macOS 26 design language with subtle hover effects
5. Implement smooth 0.2s transition animations
6. Maintain proper spacing between buttons (8pt)
7. Keep existing functionality (scan, reset, settings)

Replace the entire footerButtons implementation with the new icon-only design.</action>
  <verify>Build project successfully, verify icon-only buttons render correctly</verify>
  <done>Icon-only footer buttons implemented with macOS 26 aesthetics</done>
</task>

<task type="auto">
  <name>Task 3: Improve hover states and visual feedback</name>
  <files>Prunr/Views/MenuBarView.swift</files>
  <action>Enhance the hover states for footer buttons:

1. Apply subtle background color changes on hover (gray opacity 0.1)
2. Add smooth scale transformation (1.05x) on hover
3. Implement proper color transitions using .animation()
4. Add visual feedback for disabled states
5. Ensure accessibility labels remain for screen readers
6. Maintain proper z-index for overlapping states

Update the button styling to provide clear interaction feedback without being intrusive.</action>
  <verify>Build project successfully, verify hover states work smoothly</verify>
  <done>Enhanced hover states and visual feedback implemented</done>
</task>

<task type="auto">
  <name>Task 4: Optimize layout and positioning</name>
  <files>Prunr/Views/MenuBarView.swift</files>
  <action>Optimize the footer layout:

1. Ensure buttons are positioned in lower right corner
2. Apply consistent bottom padding (8pt)
3. Use proper horizontal spacing (8pt between buttons)
4. Align buttons to the right side of the container
5. Ensure responsive layout maintains proper positioning
6. Add proper accessibility identifiers
7. Update any related animation timing to match new design

Update the VStack container that wraps the footer buttons to ensure proper positioning.</action>
  <verify>Build project successfully, verify footer positioning is correct</verify>
  <done>Footer layout optimized for icon-only design</done>
</task>

</tasks>

<verification>
Before declaring phase complete:
- [ ] Build project successfully without errors
- [ ] Footer buttons use icon-only design with SF Symbols
- [ ] Hover states provide clear visual feedback
- [ ] Buttons maintain all existing functionality
- [ ] Design follows macOS 26 aesthetics
- [ ] Footer is less visually prominent while remaining accessible
</verification>

<success_criteria>

- All footer buttons use icon-only design
- macOS 26 aesthetics properly applied
- Hover states work smoothly
- All functionality preserved (scan, reset, settings)
- Visual weight reduced while maintaining clarity
- Accessibility maintained
  </success_criteria>

<output>
After completion, create `.planning/phases/09-visual-improvements/09-05-SUMMARY.md`
</output>