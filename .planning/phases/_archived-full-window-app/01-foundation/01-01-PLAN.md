---
phase: 01-foundation
plan: 01
type: execute
---

<objective>
Set up macOS Xcode project with GRDB.swift for SQLite storage.

Purpose: Establish the foundation all other phases depend on - a buildable app with database access ready.
Output: Working Xcode project that builds, runs, and can create/access a SQLite database.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-phase.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

**Tech decisions (from PROJECT.md):**
- GRDB.swift for SQLite (type-safe, well-documented)
- SwiftUI + MVVM architecture
- macOS 14+ (Sonoma) minimum
- Direct distribution (no App Store sandbox)

**No prior phases** - this is the foundation.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Xcode project with GRDB.swift</name>
  <files>Prunr.xcodeproj, Prunr/PrunrApp.swift, Prunr/ContentView.swift, Package.swift</files>
  <action>
    1. Create new Xcode project: macOS App, SwiftUI, Swift, product name "Prunr", bundle ID "com.prunr.app"
    2. Set deployment target to macOS 14.0
    3. Add GRDB.swift package dependency via SPM: https://github.com/groue/GRDB.swift (latest stable, currently 7.x)
    4. Import GRDB in PrunrApp.swift to verify integration
    5. Create folder structure: Prunr/Models/, Prunr/Views/, Prunr/Database/

    Avoid: Don't use CocoaPods or manual framework embedding - SPM is cleaner for this project.
  </action>
  <verify>xcodebuild -project Prunr.xcodeproj -scheme Prunr -destination 'platform=macOS' build</verify>
  <done>Project builds without errors, GRDB package resolved and imported successfully</done>
</task>

<task type="auto">
  <name>Task 2: Create DatabaseManager with initial schema</name>
  <files>Prunr/Database/DatabaseManager.swift, Prunr/Models/Snapshot.swift</files>
  <action>
    1. Create DatabaseManager.swift as singleton managing database connection
    2. Database location: Application Support/Prunr/prunr.db (create directory if needed)
    3. Create Snapshot model conforming to GRDB's FetchableRecord and PersistableRecord:
       - id: Int64 (auto-increment primary key)
       - createdAt: Date
    4. Create SnapshotEntry model for individual folder sizes:
       - id: Int64 (auto-increment primary key)
       - snapshotId: Int64 (foreign key)
       - path: String
       - sizeBytes: Int64
    5. Set up database migrations via GRDB's DatabaseMigrator
    6. Add database initialization call in PrunrApp.swift init

    Avoid: Don't create the full scanner or delta logic yet - just the schema foundation.
  </action>
  <verify>Run app in Xcode, check ~/Library/Application Support/Prunr/prunr.db exists and has tables (use sqlite3 CLI to verify)</verify>
  <done>App launches, database file created, tables exist with correct schema</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Prunr.xcodeproj -scheme Prunr build` succeeds
- [ ] App launches without crashes
- [ ] Database file exists at expected location after launch
- [ ] `sqlite3 ~/Library/Application\ Support/Prunr/prunr.db ".schema"` shows snapshot and snapshotEntry tables
</verification>

<success_criteria>
- All tasks completed
- All verification checks pass
- No build errors or warnings from GRDB integration
- Database schema ready for Phase 2 (Scanner & Storage)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
