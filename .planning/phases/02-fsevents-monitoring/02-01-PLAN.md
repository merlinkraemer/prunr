---
phase: 02-fsevents-monitoring
plan: 01
type: execute
domain: macos
---

<objective>
Build permission detection and boundary folder configuration for file system monitoring.

Purpose: Full Disk Access is required for monitoring outside user home, and boundary folders prevent wasting scans on generated content.
Output: PermissionsService for FDA detection, BoundaryConfig with standard boundary folders.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
./summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-menubar-foundation/01-01-SUMMARY.md
@Prunr/Models/TrackedPath.swift
@Prunr/Services/ScanService.swift
@Prunr/Services/FileScanner.swift

**Tech stack available:**
- SwiftUI + MVVM (from 01-menubar-foundation)
- @MainActor + @Observable patterns (from 01-menubar-foundation)

**Established patterns:**
- @Observable for SwiftUI compatibility
- Services use singleton pattern (e.g., DiskSpaceService.shared)
- Actor-based isolation for concurrent work

**Constraining decisions:**
- Phase 01: LSUIElement = YES (no Dock icon)
- Phase 01: .transient popover behavior

**Key files to reference:**
- Prunr/Models/TrackedPath.swift - Understanding of path tracking model
- Prunr/Services/ScanService.swift - Existing scanning orchestration
- Prunr/Services/FileScanner.swift - Scanner that will use boundaries

**Issues being addressed:** None

**Phase context:**
This is the first plan of Phase 2. Phase 1 (01-menubar-foundation) established the menu bar app foundation with NSStatusItem, popover, and free space display. This phase adds file system monitoring capabilities.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create BoundaryConfig with known boundary folders</name>
  <files>Prunr/Models/BoundaryConfig.swift</files>
  <action>Create a BoundaryConfig struct with standard folder patterns that should stop drill-down scanning. Include:
  1. Static property `standardBoundaries: Set<String>` with patterns:
     - "node_modules" (npm packages)
     - ".git" (git repositories)
     - ".venv", "venv", "env" (Python virtual environments)
     - ".cargo", "target" (Rust)
     - "build", "DerivedData" (build artifacts)
     - ".cache" (cached data)
     - "vendor", "third_party" (third-party dependencies)
  2. Method `func matchesBoundary(_ path: String) -> Bool` that checks if any component of the path matches a boundary pattern
  3. Method `func shouldStopDrillDown(at url: URL) -> Bool` that returns true if the last path component matches a boundary
  Use `lastPathComponent` for extraction, case-sensitive matching (macOS filesystem is case-sensitive by default).</action>
  <verify>Build succeeds, BoundaryConfig.matchesBoundary("/Users/dev/project/node_modules") returns true, BoundaryConfig.matchesBoundary("/Users/dev/project/src") returns false</verify>
  <done>BoundaryConfig.swift created with standard boundary patterns, matchesBoundary() and shouldStopDrillDown() methods working correctly</done>
</task>

<task type="auto">
  <name>Task 2: Create PermissionsService for Full Disk Access detection</name>
  <files>Prunr/Services/PermissionsService.swift</files>
  <action>Create a PermissionsService class with:
  1. `@MainActor @Observable final class PermissionsService` for SwiftUI compatibility
  2. Singleton `static let shared` instance
  3. `var hasFullDiskAccess: Bool` computed property that tests access by attempting to read a restricted location (e.g., /Library or user's Library folder)
  4. `func requestFullDiskAccess() async` that opens System Settings > Privacy & Security > Full Disk Access using NSWorkspace.open(URL(string: "x-apple.systempreferences:com.apple.preference.security?Privacy_AllFiles")!)
  5. Use FileManager.default.fileExists(atPath: "/Library") to test FDA - this will fail without FDA
  6. Add `var isCheckingPermission: Bool = false` for async state tracking

  Note: macOS doesn't provide a direct API to check FDA status - we must test by attempting to access a protected location.</action>
  <verify>Build succeeds, PermissionsService.shared.hasFullDiskAccess returns false without FDA, requestFullDiskAccess() opens System Settings</verify>
  <done>PermissionsService.swift created with FDA detection via file access test, requestFullDiskAccess() opens System Settings correctly</done>
</task>

<task type="auto">
  <name>Task 3: Add permission helper methods and URL utility</name>
  <files>Prunr/Services/PermissionsService.swift</files>
  <action>Add to PermissionsService:
  1. `func testAccess(to path: String) -> Bool` - generic method to test read access to any path using FileManager.fileExists(atPath:)
  2. `func canAccessHomeLibrary() -> Bool` - specifically tests ~/Library (requires FDA)
  3. `func canAccessRootLibrary() -> Bool` - specifically tests /Library (requires FDA)
  4. `var permissionStatus: PermissionStatus` computed property returning enum: `.notDetermined`, `.granted`, `.denied`
  5. Add `enum PermissionStatus: String, CaseIterable` with cases for UI display

  Create PermissionStatus enum in same file with:
  - `case notDetermined, granted, denied`
  - `var displayName: String` for localized display
  - `var icon: String` returning SF Symbol names: "questionmark.circle", "checkmark.circle.fill", "xmark.circle.fill"</action>
  <verify>Build succeeds, PermissionStatus enum cases are accessible, permissionStatus returns correct state based on FDA</verify>
  <done>PermissionStatus enum created with display properties, testAccess() and specific path test methods working</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] `xcodebuild -project Prunr.xcodeproj -scheme Prunr -configuration Debug build` succeeds without errors
- [ ] BoundaryConfig matches standard boundary patterns correctly
- [ ] PermissionsService.canAccessRootLibrary() returns false without FDA
- [ ] PermissionsService.requestFullDiskAccess() opens System Settings
- [ ] PermissionStatus enum has all cases with display properties
</verification>

<success_criteria>

- BoundaryConfig created with standard folder patterns
- PermissionsService detects Full Disk Access status via file access test
- requestFullDiskAccess() opens System Settings at correct pane
- PermissionStatus enum provides UI-ready display properties
- All code builds without errors
  </success_criteria>

<output>
After completion, create `.planning/phases/02-fsevents-monitoring/02-01-SUMMARY.md`:

# Phase 2 Plan 01: Permissions + Boundaries Summary

**Added Full Disk Access detection and boundary folder configuration to prevent wasted scans on generated content.**

## Accomplishments

- Created BoundaryConfig with standard boundary folder patterns (node_modules, .git, venv, etc.)
- Created PermissionsService for FDA detection via file access test
- Added permission status enum with UI display properties
- Added System Settings integration for FDA request

## Files Created/Modified

- `Prunr/Models/BoundaryConfig.swift` - Boundary folder patterns and matching logic
- `Prunr/Services/PermissionsService.swift` - FDA detection and request handling

## Decisions Made

- Test FDA by attempting to access /Library (macOS doesn't provide direct API)
- Open System Settings via x-apple.systempreferences URL scheme
- Case-sensitive boundary matching (macOS default)

## Issues Encountered

None

## Next Step

Ready for 02-02-PLAN.md (FSEvents Watcher)
</output>
