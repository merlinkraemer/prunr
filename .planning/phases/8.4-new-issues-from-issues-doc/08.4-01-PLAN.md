---
phase: 8.4-new-issues-from-issues-doc
plan: 01
type: execute
---

<objective>
Verify that previously fixed issues remain resolved and fix the broken baseline creation functionality.

Purpose: Ensure ISS-043, ISS-044, ISS-050, ISS-034 (fixed in Phase 8.2-04) are working, and fix ISS-048 baseline creation bug.
Output: Verified UI functionality, working baseline creation, updated ISSUES.md status.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
~/.claude/get-shit-done/references/checkpoints.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/ISSUES.md

# Prior work that supposedly fixed these issues:
@.planning/phases/08.2-ui-improvements-realtime-updates/08.2-04-SUMMARY.md

# Key source files:
@Prunr/Services/MenuBarManager.swift
@Prunr/Services/BaselineService.swift
@Prunr/Views/MenuBarView.swift
@Prunr/Views/CategoryGrowthListView.swift
@Prunr/Views/SettingsView.swift

**Issues being verified:**
- ISS-043: Drilldown blank screen (supposedly fixed with forcedCategory pattern)
- ISS-044: Scan modal layout (supposedly fixed with 260x180 fixed size)
- ISS-050: Scan popup flash (has minimumDisplayDuration = 0.8)
- ISS-034: Settings tab navigation (sets settingsSelectedTab UserDefaults)

**Issue being fixed:**
- ISS-048: Create baseline doesn't work anymore (High priority)
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Previous phase fixes for ISS-043, ISS-044, ISS-050, ISS-034</what-built>
  <how-to-verify>
    Test each fix manually:

    **ISS-043 - Drilldown navigation:**
    1. Open Prunr popup from menu bar
    2. If no baseline, click "Create Baseline" and wait
    3. Click "Scan Now" to populate categories
    4. Click on any category row
    5. VERIFY: Detail view appears with file list (NOT blank screen)
    6. VERIFY: Back button works to return to category list

    **ISS-044 - Scan modal layout:**
    1. Click "Scan Now" button
    2. VERIFY: Modal appears with fixed size (doesn't resize)
    3. VERIFY: Progress bar shows percentage
    4. VERIFY: Layout is clean and organized

    **ISS-050 - Minimum display duration:**
    1. Delete test_data folder if exists
    2. Click "Scan Now" (should be fast with no data)
    3. VERIFY: Modal stays visible ~800ms minimum (no instant flash)

    **ISS-034 - Settings tab navigation:**
    1. Click the monitoring path row in popup header
    2. VERIFY: Settings opens to "Paths" tab (not General)
  </how-to-verify>
  <resume-signal>
    Report results:
    - "all-pass" if all 4 issues verified working
    - Or list which issues still have problems (e.g., "ISS-043 still broken: [description]")
  </resume-signal>
</task>

<task type="auto">
  <name>Task 2: Investigate and fix baseline creation bug (ISS-048)</name>
  <files>Prunr/Services/MenuBarManager.swift, Prunr/Services/BaselineService.swift, Prunr/Views/MenuBarView.swift</files>
  <action>
    Investigate why "Create Baseline" button doesn't work:

    1. **Check the call chain:**
       - MenuBarView.swift calls `manager.createBaseline()` on button tap
       - MenuBarManager.createBaseline() calls `baselineService.createBaseline(trackedPath:)`
       - BaselineService.createBaseline() runs scan and stores snapshot ID

    2. **Potential issues to check:**
       - Is `enabledTrackedPaths.first` returning nil? (no enabled paths)
       - Is the scan completing but not storing snapshot ID?
       - Is there an error being silently swallowed?
       - Is `isLoading` getting stuck true?

    3. **Add diagnostic logging** if needed to trace the issue.

    4. **Fix the root cause** once identified.

    5. **Common fixes to consider:**
       - Ensure at least one path is enabled in SettingsStore
       - Ensure snapshot ID is being stored to UserDefaults correctly
       - Ensure error handling shows user feedback
       - Ensure isLoading state resets properly

    Do NOT change working functionality. Only fix what's broken.
  </action>
  <verify>
    Build succeeds: `cd /Users/merlinkramer/dev/projects/prunr && xcodebuild -scheme Prunr -configuration Debug build 2>&1 | tail -5`
  </verify>
  <done>Baseline creation bug identified and fixed, app builds successfully</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>Fixed baseline creation functionality (ISS-048)</what-built>
  <how-to-verify>
    1. Run the app (Cmd+R in Xcode or `open` the built app)
    2. Right-click menu bar icon > Reset Baseline (to clear any existing)
    3. Click menu bar icon to open popup
    4. VERIFY: "No baseline yet" message appears
    5. Click "Create Baseline" button
    6. VERIFY: Loading indicator appears with progress
    7. VERIFY: After completion, baseline is created
    8. VERIFY: Category list or "Ready to scan" appears (not error)
    9. Click "Scan Now"
    10. VERIFY: Growth data loads (even if empty, no error)
  </how-to-verify>
  <resume-signal>Type "approved" if baseline creation works, or describe the issue</resume-signal>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] ISS-043 drilldown navigation verified working
- [ ] ISS-044 scan modal layout verified working
- [ ] ISS-050 minimum display duration verified working
- [ ] ISS-034 settings tab navigation verified working
- [ ] ISS-048 baseline creation fixed and verified
- [ ] App builds without errors
</verification>

<success_criteria>

- All previously fixed issues (ISS-043, ISS-044, ISS-050, ISS-034) confirmed working
- ISS-048 baseline creation bug fixed
- User can create baseline and scan for growth
- No regressions introduced
</success_criteria>

<output>
After completion, create `.planning/phases/8.4-new-issues-from-issues-doc/08.4-01-SUMMARY.md`:

# Phase 8.4 Plan 01: Verification & Baseline Fix Summary

**[Substantive one-liner]**

## Accomplishments

- Verified ISS-043, ISS-044, ISS-050, ISS-034 are resolved
- Fixed ISS-048 baseline creation bug

## Files Created/Modified

- `file.swift` - Description

## Decisions Made

[Key decisions]

## Issues Encountered

[Problems and resolutions]

## Next Step

Ready for 08.4-02-PLAN.md (Header & Multi-path Polish)
</output>
