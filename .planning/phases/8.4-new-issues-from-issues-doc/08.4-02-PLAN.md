---
phase: 8.4-new-issues-from-issues-doc
plan: 02
type: execute
---

<objective>
Polish header spacing and add multiple monitoring paths indicator.

Purpose: Reduce excessive header vertical spacing (ISS-049) and show users when multiple paths are configured (ISS-051).
Output: Compact headers, path count/switcher indicator in header.
</objective>

<execution_context>
~/.claude/get-shit-done/workflows/execute-phase.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

# Prior phase summary:
@.planning/phases/8.4-new-issues-from-issues-doc/08.4-01-SUMMARY.md

# Key source files:
@Prunr/Views/MenuBarView.swift
@Prunr/Views/CategoryGrowthListView.swift
@Prunr/Services/MenuBarManager.swift
@Prunr/Data/SettingsStore.swift

**Issues being addressed:**
- ISS-049: Headers too tall with excess spacing (Medium priority)
- ISS-051: Multiple monitoring paths not indicated in header (Medium priority)

**Current header padding:**
- driveBarSection: .padding(20)
- monitoringPathHeader: .padding(20)
- drillDownHeader: .padding(20)
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce excessive header spacing (ISS-049)</name>
  <files>Prunr/Views/MenuBarView.swift</files>
  <action>
    Reduce vertical padding in header sections to create a more compact layout:

    1. **Drive bar section** (driveBarSection):
       - Change `.padding(20)` to `.padding(.horizontal, 16).padding(.vertical, 12)`
       - This reduces vertical padding from 20pt to 12pt while maintaining horizontal spacing

    2. **Monitoring path header** (monitoringPathHeader):
       - Change `.padding(20)` to `.padding(.horizontal, 16).padding(.vertical, 10)`
       - Reduce to 10pt vertical padding for tighter header

    3. **Drill-down header** (drillDownHeader):
       - Change `.padding(20)` to `.padding(.horizontal, 16).padding(.vertical, 10)`
       - Match monitoring path header for consistency

    4. **Verify visual consistency:**
       - Both headers should feel compact but readable
       - No text truncation or cramped feeling
       - Maintain 16pt horizontal padding for alignment

    Keep changes minimal - only adjust padding values, don't restructure.
  </action>
  <verify>
    Build succeeds: `cd /Users/merlinkramer/dev/projects/prunr && xcodebuild -scheme Prunr -configuration Debug build 2>&1 | tail -5`
  </verify>
  <done>Header padding reduced, headers more compact, builds successfully</done>
</task>

<task type="auto">
  <name>Task 2: Add multiple paths indicator in header (ISS-051)</name>
  <files>Prunr/Views/MenuBarView.swift, Prunr/Services/MenuBarManager.swift</files>
  <action>
    Add visual indicator when multiple monitoring paths are configured:

    1. **Add computed property to MenuBarManager** for path count:
       ```swift
       var enabledPathCount: Int {
           SettingsStore.shared.enabledTrackedPaths.count
       }
       ```

    2. **Modify monitoringPathHeader in MenuBarView** to show path count:
       - After the path text, before the size badge
       - Only show if enabledPathCount > 1
       - Display as subtle badge: "1 of N" or just "1/N"
       - Use secondary/tertiary text style

       Example addition to HStack after path text:
       ```swift
       if manager.enabledPathCount > 1 {
           Text("1/\(manager.enabledPathCount)")
               .font(.system(size: 9))
               .foregroundStyle(.tertiary)
               .padding(.horizontal, 4)
               .padding(.vertical, 2)
               .background(
                   Capsule()
                       .fill(Color.gray.opacity(0.1))
               )
       }
       ```

    3. **Keep it simple** - No path switcher dropdown in this task. Just an indicator.
       The indicator tells users multiple paths exist and clicking opens Settings where they can manage them.

    Do NOT add path switching functionality - that's a future enhancement.
  </action>
  <verify>
    Build succeeds: `cd /Users/merlinkramer/dev/projects/prunr && xcodebuild -scheme Prunr -configuration Debug build 2>&1 | tail -5`
  </verify>
  <done>Path count indicator shows when multiple paths configured, builds successfully</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Header sections have reduced padding (12pt/10pt vertical)
- [ ] Headers feel compact but not cramped
- [ ] Path count indicator appears when multiple paths enabled
- [ ] Indicator hidden when only 1 path enabled
- [ ] App builds without errors
- [ ] No visual regressions
</verification>

<success_criteria>

- ISS-049: Headers are more compact with reduced vertical spacing
- ISS-051: Users can see "1/N" indicator when multiple paths configured
- Clicking header still opens Settings to Paths tab
- No regressions to existing functionality
</success_criteria>

<output>
After completion, create `.planning/phases/8.4-new-issues-from-issues-doc/08.4-02-SUMMARY.md`:

# Phase 8.4 Plan 02: Header & Multi-path Polish Summary

**[Substantive one-liner]**

## Accomplishments

- Reduced header padding for compact layout (ISS-049)
- Added path count indicator for multiple paths (ISS-051)

## Files Created/Modified

- `Prunr/Views/MenuBarView.swift` - Reduced header padding, added path indicator
- `Prunr/Services/MenuBarManager.swift` - Added enabledPathCount computed property

## Decisions Made

[Key decisions]

## Issues Encountered

[Problems and resolutions]

## Next Step

Phase 8.4 complete, ready for comprehensive Phase 8 polish work.
</output>
