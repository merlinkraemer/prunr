{
  "name": "prunr-website",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test:api": "node --test api/*.test.js"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
